# Spec 050.21: Steps end-to-end performance optimization

Status: **Implementation ready — review pending**
Type: **backend runtime performance / Steps**  
Branch: `feature/runtime-performance-optimization`  
Parent: [`050-runtime-performance-optimization.md`](050-runtime-performance-optimization.md)  
Profiling source: [`050.17-analysis-family-performance-profiling-and-prioritization.md`](050.17-analysis-family-performance-profiling-and-prioritization.md)  
Baseline implementation head when this child was authored: `71fc95f8f26f73b239ed587021164dc449ee71f4`  
Merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`  
Depends on: **050.20 review-clean**

## 1. Goal

Optimize the complete Steps analysis miss path using the 050.17 evidence, beginning with `step_blocks.per_block()` and then re-profiling to address every remaining stage whose route-level benefit is meaningful.

The child must preserve programmed-step/block ownership, CV classification, capacity/time totals, provenance, selected raw semantics and exact persisted cache hits.

Time/Capacity is out of scope.

## 2. Locked baseline

050.17 authoritative Steps measurements:

| Workload | p50 |
| --- | ---: |
| 1 Cell forced miss | **41.41 ms** |
| 6 Cells forced miss | **193.85 ms** |
| 6 Cells exact persisted hit | **14.17 ms** |
| dominant stage | `step_block_extraction` |
| dominant p50/share | **65.49 ms / 33.78%** |

The route is not critically slow, so production complexity must be proportionate. The objective is to remove obvious repeated work and then stop when the remaining gains are micro-scale.

## 3. Current production anchors

### Router/cache

`backend/app/routers/analyses.py:compute_steps_analysis(...)`

- request context/result key;
- direct stored-body hit;
- `engine.compute_steps(...)`;
- cache persistence/serialization.

### Steps science

`backend/app/services/step_blocks.py`

- `assign_blocks(...)`
- `per_block(...)`
- `_sum_step_time(...)`
- `_sum_step_capacity(...)`

### Shared engine/data

- `backend/app/services/analysis_engine.py:compute_steps(...)`
- `backend/app/services/stitch.py:stitch_raw_steps(...)`
- `backend/app/services/cache.py:load_raw_step_rows(...)`
- `backend/app/services/calc.py`, especially CV classification helpers
- `scripts/profile_analysis_families.py`

050.16 already established selective step-addressable raw access and safe full-raw fallback for Steps. Preserve it.

## 4. Current hot-path hypothesis

`per_block(...)` currently performs several overlapping forms of DataFrame work:

- `assign_blocks(...)` sorts/resets/copies and normalizes step/record columns;
- status strings and masks are inspected;
- a further copy may be used for CV calculations;
- CV charge classification delegates to the established calc kernel;
- block groups are materialized/iterated;
- time/capacity helpers group/slice again, sometimes per step.

The likely optimization is fewer repeated groupby/slice/copy/conversion passes while retaining the existing calc semantics.

## 5. Locked scientific semantics

Preserve exactly:

- record-index continuity and block boundaries;
- programmed step labels/ownership;
- cycle/source labels;
- status-derived charge/discharge/rest interpretation;
- CV classification and capacity semantics;
- step/block time aggregation;
- charge/discharge capacity aggregation;
- selected segment/series filtering;
- deterministic result order/IDs;
- provenance/parser versions;
- selective-read fallback behavior.

Do not duplicate or approximate the CV scientific kernel merely to avoid a function call.

## 6. Required opportunity inventory

Profile Steps only on the implementation head and split enough of `step_block_extraction` to distinguish:

1. raw selective planning/read/stitch;
2. sort/reset/copy normalization;
3. numeric step/record conversion;
4. boundary/block assignment;
5. status classification;
6. CV classification;
7. block group construction;
8. time aggregation;
9. capacity aggregation;
10. series/result assembly;
11. cache persistence;
12. serialization;
13. SQL/owner context;
14. complete route.

Use call/group/row counts.

Investigate stages around >=5% of six-Cell latency or >=10 ms, plus repeated passes whose combined cost is material.

## 7. Optimization tranche A — one normalized block representation

Preferred direction:

- sort/normalize the selected rows once;
- compute step/cycle/record arrays once;
- compute boundary/block IDs once;
- retain compact positional ranges or group codes rather than repeatedly copying group DataFrames;
- expose the exact data required by existing downstream science without rebuilding equivalent masks/groupings.

If `assign_blocks(...)` already produces all needed structure, refactor consumers to reuse it instead of recreating ownership.

## 8. Optimization tranche B — grouped time/capacity aggregation

After tranche A, re-profile.

Where material:

- combine time/capacity group reductions into fewer passes;
- avoid nested `groupby` when an already computed block/step code can drive vectorized/grouped reductions;
- reuse numeric capacity/time arrays;
- avoid repeated masks for the same charge/discharge step subset;
- preserve all NaN behavior and per-step semantics.

Use native Pandas/NumPy grouped operations already in the dependency set; do not add a new dataframe engine for this child.

## 9. Optimization tranche C — CV/status work and secondary route costs

Re-profile after grouped aggregation.

If CV/status classification is material:

- reuse normalized status/cycle arrays;
- call the existing CV kernel once at the broadest safe scope;
- avoid per-block recomputation of equivalent classification facts.

Then inspect:

- selective raw read/stitch cost;
- series filtering/planning;
- provenance/result assembly;
- serialization/SQL.

Do not optimize a sub-5 ms boundary merely to make the profile prettier.

## 10. Concurrency policy

The current six-Cell miss is under 200 ms and the dominant extraction stage is ~65 ms. Concurrency is **not expected** to be justified.

Only benchmark it if serial/vectorized cleanup still leaves >=100 ms of independent CPU-bound multi-Cell work. Otherwise keep Steps serial and simple.

## 11. Cache/version policy

Expected: no migration, no `CALC_VERSION`, no result-schema bump.

Exact result hits must not perform raw reads, block assignment, CV classification or aggregation.

## 12. Implementation order

1. Capture current Steps-only 1/6 baseline.
2. Add narrow `per_block` substage profiling if needed.
3. Implement tranche A and benchmark complete route.
4. Retain only if material.
5. Re-profile and implement tranche B.
6. Re-profile and implement tranche C only for remaining meaningful costs.
7. Avoid concurrency unless the explicit gate is met.
8. Run final benchmark, golden regression and preflight.
9. Record rejected changes.

## 13. Performance acceptance

Minimum expected target: **10%** reduction of the six-Cell forced-miss p50 from 193.85 ms, approximately **174.5 ms or better**, unless the deeper profile proves the 65.49 ms parent is already dominated by irreducible scientific work.

Continue past the target if low-risk measured savings remain; stop when remaining candidate savings are micro-scale or add disproportionate complexity.

Report 1/6 miss, hit, per-substage distribution, selected physical rows/row groups, SQL counts/time and response bytes.

## 14. Required tests

Cover:

- record-index gaps and continuity;
- repeated programmed steps;
- charge/discharge/rest/status combinations;
- CV and non-CV steps;
- multiple cycles/sources;
- time and capacity totals;
- NaN/missing quantities;
- selective-reader/fallback parity;
- deterministic ordering/provenance;
- exact-hit bypass;
- golden Steps results.

Before handoff:

```powershell
python scripts\preflight.py
```

## 15. Out of scope

- Time/Capacity;
- scientific CV redefinition;
- migration/cache schema work;
- speculative process pools;
- frontend redesign.

## 16. Handoff evidence

Provide baseline/final complete-route timings, substage before/after, retained and rejected optimization tranches, scientific parity, exact-hit proof and tests actually run.

### Implementation and benchmark evidence

The required current-head Steps-only baseline was captured at `f0eb401` after
050.20 became review-clean:

```powershell
python scripts\profile_analysis_families.py --families steps --skip-time-specialist --repetitions 5 --output tmp\050.21-baseline.json
```

The measured six-Cell forced-miss route was 203.69 ms p50 (197.94–216.58 ms),
with 1-Cell forced-miss latency of 38.00 ms p50 (35.94–47.85 ms) and a six-Cell
exact persisted hit of 15.57 ms p50. The six-Cell miss had six protocol
reconstructions, six selective raw loads, 14,724 rows materialized from 24,576
physical rows across six row groups, 17 SQL statements, six lazy source-header
loads, and a 7,961-byte response. The detailed extraction profile measured
`step_block_extraction` at 69.24 ms p50; its direct substage inventory was
6.87 ms block assignment, 1.66 ms CV classification, 7.38 ms time aggregation,
and 5.55 ms capacity aggregation, with the remaining work in block-group
construction and assembly.

The retained implementation has two request-local changes:

- `compute_steps` reuses an exact protocol reconstruction only when the pinned
  parser identity, nominal capacity, and immutable source header metadata all
  match. The cache is request-local, does not change result identity, and does
  not run on exact persisted hits.
- `step_blocks.per_block` keeps the sorted selected frame as one normalized
  positional representation. `_aggregate_block_rows` derives compact block
  ranges, converts numeric/timestamp columns once, and uses NumPy reductions for
  per-step time/capacity resets and block-level metadata. The established CV
  kernel, status classification, record-index continuity, provenance and
  selective-reader fallback remain unchanged.

The final five-repetition profile was:

```powershell
python scripts\profile_analysis_families.py --families steps --skip-time-specialist --repetitions 5 --output tmp\050.21-final.json
```

| forced-miss measurement | 1 Cell | 6 Cells |
| --- | ---: | ---: |
| complete route p50 (range) | **38.21 ms (33.88–48.76)** | **132.69 ms (123.09–168.95)** |
| exact persisted-hit p50 | — | **13.61 ms** |
| `step_block_extraction` p50 / calls | — | **40.53 ms / 6** |
| protocol reconstruction p50 / calls | — | **9.68 ms / 1** |
| block aggregation / assignment / CV p50 | — | **12.44 / 7.03 / 1.59 ms** |
| selective raw rows materialized / physical | — | **14,724 / 24,576** |

The six-Cell miss is a 31.5% reduction from the locked 050.17 baseline and a
34.9% reduction from the measured current-head baseline, exceeding the 174.5 ms
acceptance target. The remaining final stage distribution was 37.81 ms data
access, 51.89 ms inclusive raw-step stitching, 5.74 ms cache persistence,
0.06 ms JSON serialization, and 0.99 ms unattributed route residual. SQL stayed
at 17 statements and 0.46 ms cumulative p50; source-header lazy loads stayed at
six; the response was 7,955 bytes with six traces and six points.

Scientific miss/hit order parity was true. All five exact-hit controls reported
zero raw loads and zero scientific-compute calls. The focused block, protocol
cache, selective/fallback, and golden tests passed; the browser/manual check is
`NOT RUN` by explicit user instruction. A first all-pandas grouped aggregation
variant was rejected after measurement because it raised the six-Cell miss to
approximately 205.5 ms; the compact-range/NumPy implementation above is the
retained tranche. Concurrency was not justified because the optimized serial
route is below 100 ms of independent CPU work and the complete route is already
below the target. No migration, CALC_VERSION bump, cache-version change or
result-schema change was needed.
