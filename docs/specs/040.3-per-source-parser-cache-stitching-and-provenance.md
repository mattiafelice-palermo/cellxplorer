# 040.3 — Per-source parser cache, stitching and provenance

**Status:** Plan  
**Parent:** [040-canonical-cycler-data-architecture.md](040-canonical-cycler-data-architecture.md)  
**Implementation baseline / merge base:** `main` at `562c2edff1277fef71789244c95e3b17abc586fa`; reverify every anchor before editing  
**Shared branch:** `feature/spec-040-canonical-cycler-data-architecture`  
**Depends on:** 040.1 and 040.2 implemented, pushed and review-clean  
**Review document:** `reviews/040.3-per-source-parser-cache-stitching-and-provenance-review.md`

## Goal

Replace the remaining assumption that one process-global parser version describes every source with a **source-specific parser identity** used consistently by raw/cycle cache lookup, source preparation, multi-source stitching, analysis cache keys and saved-analysis provenance.

This is the highest-risk architectural child in Parent 040. It changes reproducibility/plumbing, not scientific formulas.

At the end of this child:

- each source cache is keyed by that source's effective parser identity;
- `SourceFile.parser_version` stores the effective identity that produced its current canonical raw cache;
- Neware binary and Neware Excel sources may carry different parser identities without invalidating one another;
- stitching loads each ordered source at its own parser identity;
- analysis provenance records parser identity per contributing source rather than one misleading global value;
- existing saved analyses with the legacy provenance shape remain readable and stable;
- current Neware numerical output remains unchanged;
- 040.2's transitional global parser-bundle compatibility is removed or reduced to a narrow backward-compatibility helper with no scientific ownership.

## Required reading

Read before editing:

- Parent 040;
- completed/review-clean 040.1 and 040.2 including reviews/implementation records;
- Parent 039 final review;
- `AGENTS.md`;
- `docs/specs/README.md`;
- `docs/agent-knowledge/canonical-cycling-data.md`;
- `docs/agent-knowledge/architecture.md`;
- `docs/agent-knowledge/state-and-performance.md`;
- `docs/agent-knowledge/change-playbooks.md`;
- `docs/agent-knowledge/scientific-regression-testing.md`;
- `docs/portable-analysis-html.md`;
- current `backend/app/services/cache.py`;
- current `backend/app/services/stitch.py`;
- current `backend/app/services/analysis_engine.py`;
- current `backend/app/services/analysis_cache.py`;
- current source-preparation/scanner/update services;
- current `backend/app/models.py` `SourceFile` fields;
- current analysis routers/portable-report code;
- current frontend provenance/version display code and API types;
- cache/stitch/analysis/portable tests.

## Current verified architecture to reverify

At `main@562c2edff1277fef71789244c95e3b17abc586fa`:

```text
parsing.PARSER_VERSION = v2026.06.11-cxp6
cache raw key          = (file hash, parsing.PARSER_VERSION)
cache cycle key        = (file hash, parsing.PARSER_VERSION, CALC_VERSION)
stitch_raw(...)        = ordered hashes + one parser version
stitch_cycles(...)     = ordered hashes + one parser version + calc version
CALC_VERSION            = 1.6.1
```

Concrete global-version consumers include:

- `cache.schedule_build`, `build`, `build_write_behind`, `raw_path` and `cycles_path` current defaults;
- `scanner._has_current_scientific_cache` and source preparation;
- `scanner.parse_file` / stable replacement, which persist the build result into `SourceFile.parser_version`;
- `analysis_engine.compute*`, which starts from `parsing.PARSER_VERSION`, optionally replaces it from legacy `provenance["parser_version"]`, and passes that one version to all selected sources;
- `analysis_cache.result_key`, which puts one scalar parser version into every scientific result key;
- `stitch_raw` / `stitch_cycles`, which cannot currently load different parser identities within one Cell chain.

`SourceFile.parser_version` already exists as `String(30)`. New identities must fit that field; this child must not solve naming length by adding a migration.

Parent 039 intentionally preserved one Neware bundle across binary/Excel. 040.3 replaces that temporary assumption while preserving old bundle caches/provenance as historical pinned data.

## Locked parser identity

Use the descriptor introduced in 040.2 to compute one deterministic effective identity per source format/adapter, including 040.1's canonical raw version.

Conceptually:

```text
format_id + adapter_revision + canonical_raw_version
```

For Neware binary, include the underlying NewareNDA version when that library can change decoded canonical output.

For Neware Excel, include the CellXplorer Excel parser revision.

The identity string must be:

- **30 characters or fewer** so it fits the existing `SourceFile.parser_version` field;
- deterministic;
- filesystem-safe after current cache sanitization;
- stable across runs/builds for identical parser semantics;
- changed deliberately when canonical parser output semantics change;
- independent of source content hash;
- independent of `CALC_VERSION`.

Do not include Python version, OS version, application patch version or other environment noise unless it actually changes canonical parser meaning.

Prefer a compact documented grammar, for example:

```text
nb:v2026.06.11:r1   # Neware binary + NewareNDA version + canonical raw version
nx:6:r1             # Neware Excel adapter revision + canonical raw version
```

The exact tokens are decided in implementation/review, but the maximum length and semantics above are locked.

Measured against the real values 040.2 exposes through `parsing.source_parser_descriptor()`
(`adapter_revision` = `NEWARE_NDA_VERSION` = `v2026.06.11` for binary, `str(EXCEL_PARSER_REVISION)` =
`6` for Excel), this grammar yields **17** and **7** characters, comfortably inside the 30-character
`SourceFile.parser_version` bound. Note that the NewareNDA version string carries a leading `v` and
zero-padded month — reverify it from `parsing.NEWARE_NDA_VERSION` rather than retyping it, and leave
headroom: a longer upstream version string is the one realistic way this budget gets tight.

## Cache API changes

### Paths remain content-addressed

Preserve the existing directory architecture:

```text
CACHE_DIR/<hash[:2]>/<hash>/...
```

Raw/cycle filenames continue to carry parser identity and calc version.

### Remove implicit current global parser defaults from scientific callers

Functions may retain optional defaults for compatibility only where safe, but production scientific/source code should pass the source-specific parser identity explicitly.

Target concepts:

```python
raw_path(file_hash, parser_identity)
cycles_path(file_hash, parser_identity, calc_version)
load_raw(file_hash, parser_identity)
load_cycles(file_hash, parser_identity, calc_version)
```

For build paths where the source path is available:

```python
identity = parsing.parser_identity(source_path)
```

and successful build result persists that identity into `SourceFile.parser_version` through the existing owner.

### Background build de-duplication

Current process-local deduplication keys include the global parser version. Replace with the effective parser identity for the source being built.

Two formats with the same content hash are extraordinarily unlikely but must not collide merely because one global version was assumed.

### Current-cache checks

Any helper such as `_has_current_scientific_cache` must determine the expected parser identity from the registered source's current path/format/metadata or from a stable source descriptor stored/persisted by existing fields.

Do not open/fully parse sources merely to answer list endpoint cache-status questions.

Where `SourceFile.parser_version` already records the successfully used parser identity and the file extension/format is stable, current-version comparison should be relational/cheap.

If the current adapter revision changes, a cheap expected identity can be derived from extension/recognized format policy without full parse.

## Source registration and update behavior

### New registration

On successful scientific preparation:

```text
SourceFile.parser_version = effective parser identity
```

### Existing source after application update

If the expected parser identity for its format has advanced:

- old cache remains a historical reproducibility artifact until normal cleanup policy removes it;
- current source is considered scientifically stale/pending under existing preparation rules;
- rebuild writes a new raw/cycle cache at the new identity;
- do not mutate old cache contents in place.

### Changed source bytes

Existing content-hash replacement rules remain authoritative. Parser-identity migration and source-content replacement are separate dimensions.

## Stitching redesign

Current `stitch.py` accepts only ordered hashes plus one parser version. Replace this with a source descriptor per position.

Preferred simple shape:

```python
@dataclass(frozen=True)
class CachedSourceRef:
    file_hash: str
    parser_version: str
```

or equivalent tuples/dicts already available from `SourceFile`.

Target calls conceptually:

```python
stitch_raw(ordered_sources)
stitch_cycles(ordered_sources, calc_version)
```

where each source includes its own hash/parser identity.

Preserve exactly:

- source order;
- dense observed-cycle mapping;
- `segment` column;
- `source_hash` provenance column;
- incomplete/missing-position fail-closed semantics;
- stable acquisition order within raw source;
- no fabricated local cycles.

Add `source_parser_version` to stitched frame/source-segment metadata only if it materially improves provenance/debugging. Avoid repeating one long parser string on every raw row unless needed; per-segment metadata may be preferable.

## Analysis resolver/provenance

### Ordered source descriptors

Where analysis code currently resolves ordered hashes, extend the resolver so scientific compute can also obtain the parser identity pinned/expected for each source.

Avoid N+1 queries. `preload_cell_sources` and bulk source loaders must retain the state/performance invariants documented in `state-and-performance.md`.

### New provenance shape

Use source-local provenance entries to record parser identity. Conceptually:

```json
{
  "calc_version": "...",
  "sources": [
    {
      "cell_id": 1,
      "file_hashes": ["..."],
      "files": [
        {
          "hash": "...",
          "position": 0,
          "parser_version": "neware-binary:..."
        }
      ]
    }
  ]
}
```

The exact nesting must fit the current provenance owner. Do not duplicate the same arrays in several places merely to preserve a legacy field.

Requirements:

- ordered source hashes remain available for current stale-source comparisons;
- each contributing source parser identity is pinned;
- `CALC_VERSION` remains separately pinned;
- new analysis cache keys change when a contributing source parser identity changes;
- a parser change for an unrelated format/source must not invalidate analyses that do not use it.

### Legacy provenance normalization

Existing saved analyses may carry a single legacy `parser_version` field or a Parent-039 global bundle.

Add deterministic load-time normalization:

- preserve the old value as the parser identity for all sources recorded by that legacy provenance when that is the only truthful historical information available;
- do not rewrite the persisted analysis merely by opening it unless current normalization policy already does so;
- once recomputed under 040.3, persist the new source-specific provenance shape;
- stale/newer-parser badges must compare using normalized provenance without forcing recomputation.

If `analysis_engine.SPEC_VERSION` advances, include focused tests for old-spec loading, normalization and re-save behavior.

## Analysis cache identity

Audit `backend/app/services/analysis_cache.py` and any query/cache key builder.

A scientific analysis result must vary when any contributing source parser identity changes.

Do not use the **current** adapter version blindly for a historical/pinned analysis that explicitly renders old provenance. Preserve the application's existing rule: analyses render from pinned parser/calc versions when the corresponding cache exists; “newer available” is a badge, not a silent recompute.

This means the compute path must be able to request each source's cache at the **pinned source parser identity** from analysis provenance.

## Historical cache loading

This child must preserve reproducibility across parser upgrades.

Required case:

```text
source currently expects parser identity P2
saved analysis provenance pins P1
P1 raw/cycle cache still exists
```

Result:

```text
saved analysis can render using P1 without silently rebuilding/relabeling as P2
```

If P1 cache is missing, preserve current missing-version behavior. Do not reparse current source with P2 and pretend it is P1.

## Portable reports and exports

Audit portable provenance serialization/import.

Portable output must preserve source-specific parser identities when provenance is embedded. Older portable reports with legacy one-parser provenance remain importable/readable according to current compatibility policy.

Do not redesign portable package security or source embedding in this child.

## Frontend/API types

If parser provenance is exposed to the frontend:

- update `frontend/src/api.ts` types;
- preserve existing “new parser available” or stale-analysis UI semantics;
- do not expose raw internal adapter objects unnecessarily;
- use user-facing format labels only where currently shown.

No new settings/UI surface is required.

## Database migration

No relational schema migration is expected because `SourceFile.parser_version` already exists and analysis specs/provenance are JSON.

The merge-base schema has `SourceFile.parser_version = String(30)`. Do not add a migration; keep the identity grammar within that existing bound. If implementation discovers a separate persistent fact that genuinely cannot be represented, stop for a parent amendment.

## `CALC_VERSION`

Do not bump it. This child changes which raw cache/provenance is selected, not the meaning of `calc.per_cycle`.

## Tests

Add focused coverage for:

### Cache identities

1. binary Neware and Excel effective identities differ;
2. building each source writes raw/cycle files under its own identity;
3. bumping only Excel adapter revision does not make a binary source cache current-version stale;
4. bumping canonical raw version changes both identities as expected;
5. background build deduplication keys use source-specific identity;
6. `SourceFile.parser_version` is updated to the actual identity after successful build.

### Stitching

7. two ordered synthetic/current sources with different parser identities stitch successfully when caches exist;
8. raw stitching loads each source at the specified identity;
9. cycle stitching does the same;
10. one missing source-specific cache still blocks later continuation segments under existing fail-closed semantics;
11. cycle/global/source provenance output remains identical apart from intended parser metadata.

### Saved analysis provenance

12. new compute pins parser identity per source;
13. changing one contributing source identity changes analysis scientific/cache key;
14. changing an unrelated format adapter identity does not change an analysis key;
15. legacy single-parser provenance normalizes deterministically;
16. legacy analysis renders from legacy pinned caches when present;
17. missing legacy cache does not get silently relabeled with current parser identity;
18. re-compute under current versions persists the new provenance shape;
19. stale/newer-parser detection compares source-by-source;
20. portable serialization/roundtrip preserves new provenance and accepts legacy shape.

### Existing regressions

21. all existing binary Neware scientific tests remain numerically unchanged;
22. Parent 039 Excel scientific tests remain numerically unchanged;
23. list endpoints remain relational/bounded and do not open source files merely to compute parser identity.

## Explicitly out of scope

- adding new source formats;
- three-electrode fields/UI — 040.4;
- changing formulas or statuses;
- removing multi-source protocol-analysis guard;
- relational migration;
- broad portable-report redesign;
- cache cleanup redesign.

## Implementation order

1. Reverify post-040.2 adapter descriptors and every global `PARSER_VERSION` consumer.
2. Introduce one helper for effective source parser identity and migrate cache build/current checks.
3. Refactor `stitch.py` to consume ordered source descriptors.
4. Refactor analysis source resolution and cache keys to use per-source parser identity.
5. Add source-specific provenance and legacy normalization.
6. Update portable/API types only where needed.
7. Add focused cache/stitch/provenance tests.
8. Run existing Neware binary/Excel scientific regressions and preflight.
9. Update implementation record, commit, push, review.

## Verification

At minimum run current canonical equivalents of:

```text
cache tests
stitch tests
analysis engine/cache tests
portable analysis tests
source preparation/scanner tests touching parser_version
040.1/040.2 tests
Parent 039 parser + analysis regression tests
existing golden analysis corpus
python scripts\preflight.py
```

Record any manual/browser check as NOT RUN unless actually performed. No packaged check is required unless dependency/module collection changed.

## Acceptance criteria

040.3 is review-clean only when:

- parser identity is source-specific in cache build/load/current checks;
- stitching supports different parser identities per ordered source without losing current provenance/cycle semantics;
- saved analysis provenance pins parser identity per source;
- legacy saved analyses/portable provenance remain readable through deterministic normalization;
- pinned historical caches remain renderable without relabeling;
- unrelated adapter revisions no longer invalidate other formats' caches/analyses;
- no N+1/file-I/O regression is introduced into list endpoints;
- no existing Neware numerical output changes;
- no relational migration or `CALC_VERSION` bump occurs;
- focused tests, golden regression and preflight pass and are recorded truthfully.

## Implementation record

**Merge base:** `562c2edf` (`main`, 0.22.0-beta.5). **Branch HEAD before this child:** `c0a8e8e`
(040.2 + its error-taxonomy follow-up). **Implementation commit:** `<pending — see below>`.

### Parser-identity grammar (locked in `parsing.py`)

```text
<prefix>:<adapter_revision>:r<canonical_raw_version>
```

`_FORMAT_IDENTITY_PREFIX = {FORMAT_NEWARE_BINARY: "nb", FORMAT_NEWARE_EXCEL: "nx"}`. Measured against
the real running values:

```text
nb:v2026.06.11:r1   17 characters
nx:6:r1              7 characters
```

`_MAX_PARSER_IDENTITY_LENGTH = 30` is asserted at construction time in `_identity_for_format`
(`ValueError` if exceeded), not trusted by eye — this is what fails loudly if a future upstream
version string gets long rather than silently overflowing `SourceFile.parser_version`.

Two entry points:

- `parsing.parser_identity(path)` — content-aware (calls `recognize_source`, which sniffs Excel
  workbook headers); used at actual build/parse time where a real path is available.
- `parsing.current_parser_identity_for_extension(ext)` — cheap, zero I/O, resolved from the static
  format registry alone; used everywhere a "is this source's cache current" question must be
  answered without opening the file (list endpoints, backfill gating, portable-import cache checks).

`parsing.PARSER_VERSION` (the Spec 039 transitional global bundle) is unchanged in value and is now
read only as a legacy fallback (a source with no stored `parser_version`, or a diagnostic-only
`/api/meta` field) — no cache build, cache lookup, or analysis provenance keys on it anymore.

### Every call site migrated off global `PARSER_VERSION`, and what was deliberately left

Migrated (all now resolve per-source identity from `SourceFile.ext`/actual path, never the global
bundle):

- `cache.py` — `schedule_build`, `build`, `build_write_behind` compute `parsing.parser_identity(source_path)`
  once and use it for the raw/cycle paths, the background-build dedup key, and the returned
  `parser_version`. `raw_path`/`cycles_path` keep `parsing.PARSER_VERSION` as an optional **default
  parameter only** (spec-sanctioned compatibility default); every production call now passes an
  explicit identity.
- `scanner.py` — `_has_current_scientific_cache` and `_prepare_capacity_source_worker` resolve the
  expected identity from the source's `ext` with no file I/O. `parse_file`,
  `update_source_from_path`, `update_source_from_path_if_stable` already stored whatever `cache.build`
  returned in `SourceFile.parser_version`, so they needed no change — they automatically pick up the
  real per-source identity now that `cache.build` returns it.
- `stitch.py` — `stitch_raw`/`stitch_cycles` now take `list[CachedSourceRef]` (`file_hash` +
  `parser_version` per ordered source) instead of one hash list plus one shared parser version.
- `analysis_engine.py` — new `resolve_source_parser_versions`/`cell_source_refs`/`current_source_refs`/
  `current_parser_identity`/`source_file_entries`/`display_parser_version` helpers; `compute`,
  `compute_steps`, `compute_dcir`, `compute_time_capacity` all resolve per-cell, per-source identity
  through these instead of one scalar `parser_version`. The reparse-on-demand gate now reparses a
  source only when its PINNED identity equals its CURRENT identity (never reparses a pinned-old
  source and relabels the result as current).
- `analysis_cache.py` — `result_key` fingerprints each unit's `source_parser_versions` (resolved with
  the exact same function `compute()` uses) instead of one global scalar.
- `chargeability.py`, `rate_capability.py` — same per-source resolution pattern as `analysis_engine`
  compute functions (both import `analysis_engine` already).
- `continuations.py` — `enrich_source_timing` resolves the expected identity from the candidate
  source's own filename extension.
- `portable_analysis.py` — the post-import "does a current cache already exist" check resolves the
  expected identity from the (possibly reused) source's extension; the provenance-remap step now
  remaps `sources[].files[].hash` alongside the existing `file_hashes` remap (see "gap found" below).
- `routers/library.py` — `source_file_needs_cache`, `_observed_steps_for_source`, `cell_cycles`.
- `routers/replicates.py` — `cell_cycle_frame`.
- `routers/files.py` — `_complete_prepared_import_source_file`'s cache-ready check,
  `raw_import_file_data` (loads at the identity `cache.build` actually returned instead of the
  global default), `preview_file`'s fallback.

Deliberately left on the global bundle:

- `main.py`'s `/api/meta` `parser_version` field — a diagnostic build-identity string with no
  per-source context, not used for any cache/provenance decision.
- A handful of `parser_version=parsing.PARSER_VERSION` test-fixture defaults in
  `tests/test_portable_analysis.py` (`inspect`/`possible_update` identity-matching tests that never
  touch the cache filesystem) — any string works there since only checksum-based identity matching is
  under test.

### Provenance shape

```json
{
  "computed_at": "...",
  "parser_version": "nb:v2026.06.11:r1 | mixed",
  "calc_version": "1.6.1",
  "sources": [
    {
      "cell_id": 1,
      "file_hashes": ["..."],
      "source_descriptors": [ /* existing per-position descriptors, cycles kind only */ ],
      "files": [
        {"hash": "...", "position": 1, "parser_version": "nb:v2026.06.11:r1"}
      ]
    }
  ]
}
```

`files` is the truthful per-source pin (added to every kind's `sources[]` entries: cycles, steps,
dcir, chargeability, rate_capability — only `time_capacity` has no persisted `sources[]` at all,
unchanged from before this child, since only the "cycles" compute's result is ever persisted into
`Analysis.provenance` via `build_provenance`). The top-level scalar `parser_version` /
`current_parser_version` are a **display-only** summary (`display_parser_version`): the shared
identity when every contributing source agrees, or the literal string `"mixed"` when they legitimately
differ — never a value that falsely implies one shared identity. `calc_version` stays one scalar by
design (it applies uniformly to the whole scientific computation).

### Legacy normalization rule

`analysis_engine.resolve_source_parser_versions(files, provenance, cell_id, use_current_versions)` is
the single function every compute/`result_key` call site uses:

1. If `use_current_versions` or no provenance: every source resolves to `current_parser_identity(f)`
   (from `f.ext`, no I/O).
2. Else, for each `provenance["sources"]` entry matching `cell_id`:
   - if it has a non-empty `files` array (new shape), pin exactly those hash→identity pairs;
   - else (legacy shape), apply the single legacy `provenance["parser_version"]` scalar to every hash
     in that entry's `file_hashes` — the only truthful historical fact available.
3. Any hash not covered by either (a brand-new source attached since the analysis was last computed)
   falls back to its own current identity — never another source's pinned identity.

This function is reused by `compute`, `compute_steps`, `compute_dcir`, `compute_time_capacity`,
`chargeability.compute`, `rate_capability.compute`, and `analysis_cache.result_key`, so the rendered
result and its cache key can never disagree about which identity was used.

### Proof: pinned legacy analysis renders from its old cache instead of recomputing

`tests/test_analysis_engine.py::AnalysisEngineTests`:

- `test_legacy_pinned_analysis_renders_from_pinned_cache_without_reparsing` — writes a raw/cycle cache
  directly at a fabricated old identity (`nb:vOLD.00.00:r1`, deliberately different from the real
  current `nb:v2026.06.11:r1`), passes legacy-shape provenance (`parser_version` scalar only, no
  `files[]`), and patches `scanner.parse_file` to raise `AssertionError` if called at all. The compute
  succeeds, returns the old cache's data (`x == [1, 2, 3]`), and a `newer_parser` badge is emitted —
  proving the pinned identity rendered without any reparse attempt.
- `test_missing_legacy_cache_is_not_relabeled_as_current` — same shape, but the pinned identity's
  cache was never written. `scanner.parse_file` is again patched to raise. The result comes back with
  an empty series and a `cache_missing` badge naming the pinned (missing) identity — never silently
  substitutes the current identity's cache and mislabels it.
- `test_recompute_under_current_versions_persists_new_provenance_shape` — `use_current_versions=True`
  against the same legacy-pinned provenance produces `build_provenance(result)["sources"][0]["files"]`
  at the CURRENT identity, proving recompute is the only path that advances the pin.

### Proof: cross-format cache invalidation is surgical

`tests/test_parser_identity.py::SurgicalCacheInvalidationTests`, using `patch.dict` on
`parsing._FORMAT_DESCRIPTORS` (not the released constants) so no real adapter revision was touched:

- bumping the Excel `SourceFormatDescriptor`'s `adapter_revision` changes
  `current_parser_identity_for_extension(".xlsx")` but leaves `.ndax`'s identity byte-identical, and
  vice versa for bumping the binary descriptor;
- patching `canonical_cycling.CANONICAL_RAW_VERSION` changes both `.ndax` and `.xlsx` identities;
- a `SourceFile` fixture pinned at the pre-bump identity is stale for its own format only after that
  format's descriptor is bumped, and unaffected by the other format's bump
  (`test_stale_source_file_needs_cache_is_format_scoped`).

`tests/test_analysis_cache.py`-equivalent coverage lives in `tests/test_analysis_engine.py`:
`test_result_key_changes_when_pinned_source_identity_changes` (case 13) and
`test_result_key_unaffected_by_an_unrelated_cells_source_identity` (case 14) prove the analysis cache
key follows the same surgical rule.

### `SPEC_VERSION` / `ANALYSIS_CACHE_VERSION` / `CALC_VERSION`

- `analysis_engine.SPEC_VERSION` — **unchanged (9)**. The persisted `Analysis.spec` JSON shape
  (selection/computation/aggregation/presentation) is untouched by this child; only the separate
  `Analysis.provenance` field's shape changed, and that is handled by the legacy-normalization
  function above rather than a `SPEC_VERSION` bump.
- `analysis_cache.ANALYSIS_CACHE_VERSION` — **bumped 4 → 5**. `result_key`'s fingerprint structure
  changed for every analysis kind uniformly (one global `parser_version` scalar replaced by each
  unit's `source_parser_versions` list), which is exactly the "changes that genuinely affect every
  analysis result family" case the module's own comment reserves this constant for. The digest would
  already differ without the bump (the JSON fed to the SHA-256 changed shape), so this is a
  documentation/hygiene bump, not a required-for-correctness one.
- `analysis_cache.RESULT_SCHEMA_VERSIONS` — bumped for every kind whose persisted `sources[]` payload
  gained the new `files` array: `cycles` 2→3, `steps` 2→3, `dcir` 1→2, `chargeability` 1→2,
  `rate_capability` 3→4. `time_capacity` is unchanged (2) — it has no `sources[]` field.
- `CALC_VERSION` (`backend/app/config.py`) — **unchanged**, confirmed by `git diff` showing the file
  untouched. `calc.py` was not edited.
- **No relational migration added.** `SourceFile.parser_version` remains `String(30)`; the grammar was
  measured to fit before implementation and reconfirmed with `_MAX_PARSER_IDENTITY_LENGTH`.

### Golden corpus — provenance-only drift, reviewed and copied in

`python scripts/build_golden_analysis_corpus.py refresh-expected --diff-report ...` against the
committed sources reported exactly one changed path per case (`changed_path_count: 1` in the
machine-readable diff report for all 8 cases) — `$.parser_version`, `'v2026.06.11-cxp6'` →
`'nb:v2026.06.11:r1'` — and nothing else. Independently re-verified with a raw `diff` of the
pretty-printed committed vs. candidate JSON for every case: one changed line each, always that exact
field. This is the intended provenance-representation change this child exists to make, not a
scientific value change; the scientific checkpoints in `approval.md` (independent CC+CV capacity,
CE/EE, Time/Capacity continuity, Steps duration, DCIR, Chargeability, Rate capability formulas) are
unaffected because none of them read `parser_version`. The candidate was copied into
`tests/fixtures/golden_analysis/expected/*.json`; `tests.test_golden_analysis` and
`tests.test_golden_approval_checkpoints` are green against the updated fixtures.
`scripts/verify_golden_approval_checkpoints.py`'s `_raw_for_key` also needed migrating off the bare
`cache.raw_path(hash)` default (it now reads the identity `scanner.parse_file` actually persisted on
the `SourceFile` row) — a pre-existing helper broken by the same cache-path change, not new fixture
drift.

### A gap found and fixed while implementing (not scientific)

Portable import (`portable_analysis.py`) already remapped `provenance["sources"][].file_hashes` to
the effective imported hash (when a reused library source has a different hash than the package's
original), but the new per-source `files[].hash` entries were not remapped alongside it — a
reused-but-different-hash import would have silently lost its pinned identity (falling back to
current-identity resolution) instead of carrying it forward consistently with `file_hashes`. Fixed by
remapping `files[].hash` the same way; covered by
`test_portable_provenance_preserves_per_source_parser_identity_and_remaps_hash`.

### Files changed

Production: `backend/app/services/{parsing,cache,stitch,analysis_engine,analysis_cache,scanner,
chargeability,rate_capability,continuations,portable_analysis}.py`,
`backend/app/routers/{library,replicates,files}.py`, `frontend/src/api.ts` (added
`Provenance.sources[].files` / `ProvenanceSourceFile` type only — no runtime/UI change),
`scripts/verify_golden_approval_checkpoints.py`.

Tests: new `tests/test_parser_identity.py` (17 tests: grammar, cache-build identity, surgical
cross-format/raw-version invalidation, list-endpoint no-I/O); updates to `tests/test_stitch.py`
(`CachedSourceRef` signature + 3 new mixed-identity tests), `tests/test_analysis_engine.py` (+7 new
tests: cases 12-14, 16-18, plus fixture fixes for the new per-source identity requiring an extension
on every `cache.build(...)` call), `tests/test_analysis_cache.py` (schema-version test fixed to bump
off its own new default rather than an already-current value), `tests/test_calc_and_cache.py`,
`tests/test_canonical_cycling.py`, `tests/test_neware_excel.py`, `tests/test_continuations.py`,
`tests/test_multi_source_regression.py`, `tests/test_portable_analysis.py` (+2 new tests, case 20;
fixture fixes for the same reason).

Docs: `docs/agent-knowledge/canonical-cycling-data.md` and `architecture.md` (both had stale
"Spec 040.3 will wire this up" language describing this child as future work), `docs/specs/README.md`
(040 child-status line), `AGENTS.md` (tree entry for the new test file).

Golden fixtures: `tests/fixtures/golden_analysis/expected/*.json` (`parser_version` field only, see
above).

### Commands run and results

```text
python -m unittest discover tests                              → Ran 946 tests — OK
python -m unittest tests.test_stitch tests.test_parser_identity
    tests.test_analysis_engine tests.test_analysis_cache
    tests.test_canonical_cycling tests.test_parser_dispatch
    tests.test_source_format_errors tests.test_scientific_preparation
    tests.test_continuations tests.test_portable_analysis
    tests.test_neware_excel tests.test_calc_and_cache
    tests.test_multi_source_regression tests.test_golden_analysis
    tests.test_golden_approval_checkpoints -v                   → all OK (individually re-verified)
python scripts/build_golden_analysis_corpus.py refresh-expected
    --output tmp/golden-0403-refresh
    --diff-report tmp/golden-0403-refresh/diff-report.json
    --replace                                                   → 1 changed path per case (parser_version only); copied into fixtures
npx tsc --noEmit  (frontend/)                                    → exit 0
npx vite build     (frontend/)                                   → succeeded (pre-existing chunk-size warning only)
python scripts\preflight.py                                      → PREFLIGHT PASSED, 5/5 stages
    (63/63 backend test modules incl. test_parser_identity; 495/495 frontend policy tests;
    frontend type check and production bundle both ran — frontend/src/api.ts changed)
```

Not run: manual/browser verification (no observable UI change — the only frontend edit is a TypeScript
interface addition with no runtime behavior change; per spec, "No new settings/UI surface is
required"). No packaged-backend smoke test (no PyInstaller collection-list change).

### Ambiguities / decisions

- The spec's illustrative provenance shape nests `files` inside each `sources[]` entry per cell; the
  implementation follows that exactly rather than inventing a flatter shape.
- `display_parser_version`'s `"mixed"` sentinel is a new implementation decision (not spelled out in
  the spec, which only says "do not keep a misleading single field"). Chosen over other options
  (joining all values, picking one arbitrarily) because it is honest without expanding the API
  surface, and the frontend only ever displays it as free text — no parsing/comparison logic depends
  on its exact spelling.
- `ANALYSIS_CACHE_VERSION` bump: the spec does not explicitly require it (digests already differ
  structurally). Bumped anyway per the module's own documented policy for changes affecting every
  result family; flagged explicitly here since the parent review process asked this question
  directly.

**Implementation SHA:** `<pending — filled in immediately after this commit>`.
**Review:** pending — see `reviews/040.3-per-source-parser-cache-stitching-and-provenance-review.md`
once created.
