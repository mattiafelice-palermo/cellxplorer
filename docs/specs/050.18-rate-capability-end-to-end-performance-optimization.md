# Spec 050.18: Rate Capability end-to-end performance optimization

Status: **Scheduled — implement first**  
Type: **backend runtime performance / Rate Capability**  
Branch: `feature/runtime-performance-optimization`  
Parent: [`050-runtime-performance-optimization.md`](050-runtime-performance-optimization.md)  
Profiling source: [`050.17-analysis-family-performance-profiling-and-prioritization.md`](050.17-analysis-family-performance-profiling-and-prioritization.md)  
Baseline implementation head when this child was authored: `71fc95f8f26f73b239ed587021164dc449ee71f4`  
Merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`  
Depends on: **050.17 review-clean**

## 1. Goal

Reduce the current Rate Capability warm result-cache-miss latency as far as is reasonably justified by measurement, while preserving every existing scientific, protocol-recognition, validation, provenance, ordering, cache and export contract.

This child is deliberately broader than a one-function micro-optimization. The 050.17 profile identifies `extract_pair_executions()` as the dominant boundary, but the implementation must inventory and address **every measured Rate Capability cost that can produce a meaningful end-to-end gain**, re-profile after each promoted tranche, and stop only when the remaining costs are too small, too risky, or insufficiently attributed to justify further production complexity.

Time/Capacity is out of scope.

## 2. Locked baseline and why this child is first

The review-clean 050.17 five-family matrix measured the current production route using one committed golden Cell and six source-distinct/content-identical relational clones, three warm repetitions, forced result-cache misses and exact persisted hits.

Authoritative Rate Capability baseline:

| Workload | p50 |
| --- | ---: |
| 1 Cell forced miss | **219.61 ms** |
| 6 Cells forced miss | **1267.82 ms** |
| 6 Cells exact persisted hit | **14.99 ms** |
| dominant parent | `execution_extraction` |
| dominant parent p50/share | **1091.84 ms / 86.12%** |

The corrected deep profile attributes the largest execution children to approximately:

- `execution_phase_row_filtering`: **~658 ms** at six Cells;
- `measurement_filtering_grouping`: **~225 ms**;
- remaining direct `execution_extraction` residual: **~90 ms**;
- cutoff validation and capacity/current/rate extraction are measured separately and are materially smaller;
- protocol reconstruction is much smaller than the execution path and must not be mistaken for the principal cause of the ~1.27 s route.

The first optimization hypothesis is therefore to eliminate repeated full-DataFrame scanning/filtering in execution extraction. That hypothesis is **not** permission to ignore secondary costs after the dominant scans are removed.

## 3. Current production anchors

Read the current branch before changing anything. Grep-able anchors include:

### Router/cache boundary

`backend/app/routers/analyses.py`

- `compute_rate_capability_analysis()`
- `engine.build_analysis_request_context()`
- `analysis_cache.result_key(...)`
- `analysis_cache.load_result_body("rate_capability", ...)`
- `analysis_cache.splice_result_body(...)`
- `analysis_cache.store_result(...)`
- `fast_json(...)`

050.16 established the direct immutable-body exact-hit path. A clean exact hit must continue to avoid Rate Capability scientific compute and raw Parquet reads.

### Scientific service

`backend/app/services/rate_capability.py`

- `compute(...)`
- `build_rate_pairs(...)`
- `extract_pair_executions(...)`
- `_execution_groups(...)`
- `_phase_rows(...)`
- `_reached_voltage(...)`
- `_numeric(...)`
- `_selected_rate(...)`
- `detect_sweep_blocks(...)`
- `build_common_rate_comparison(...)`

The current execution loop repeatedly performs operations equivalent to:

1. select measurement rows for one protocol-pair measurement step;
2. group those rows into occurrences;
3. derive each occurrence cycle;
4. rescan the full raw frame for all phase steps in that cycle;
5. rescan again for the paired reference phase;
6. evaluate cutoff completion;
7. extract capacity/current/rate values;
8. repeat for both charge and discharge directions and every pair.

The repeated phase-row selection is the measured first-order cost.

### Data/protocol boundaries

- `backend/app/services/cache.py:load_raw()`
- `backend/app/services/protocol.py:reconstruct_protocol()`
- request-local owner state in `backend/app/services/analysis_engine.py:AnalysisRequestContext`
- current profiling harness: `scripts/profile_analysis_families.py`

## 4. Scientific behavior that must remain byte-for-byte/equivalent

Do not change the meaning of:

- protocol reconstruction or rate-pair recognition;
- charge/discharge phase ownership;
- execution grouping boundaries and occurrence numbering;
- cycle association;
- upper/lower cutoff completion and tolerance semantics;
- capacity selection (`nanmax` behavior);
- current selection/median behavior;
- configured/observed C-rate semantics;
- mass/area normalization;
- valid/invalid execution logic;
- sweep-block detection and selected-rate tolerance;
- common-rate comparison;
- invalid-neighbour validation;
- result ordering, IDs, provenance and badges;
- parser-version pinning and multi-source fail-closed behavior.

If a faster representation is introduced, prove equivalence against the existing implementation on edge cases before replacing the old path.

## 5. Required opportunity inventory

Before production changes, extend/reuse the 050.17 profiler enough to establish a **Rate-only opportunity inventory** on the implementation head. Do not re-profile Time/Capacity.

At minimum retain separate measurements for:

1. request/context/key/cache work;
2. raw materialization;
3. protocol reconstruction;
4. rate-pair building;
5. execution extraction parent;
6. measurement-step filtering/grouping;
7. phase/reference row selection;
8. cutoff validation;
9. capacity extraction;
10. current extraction;
11. rate normalization;
12. execution-parent direct residual;
13. sweep detection;
14. candidate/rate selection;
15. common-rate comparison;
16. invalid-neighbour validation;
17. result/provenance assembly;
18. cache persistence;
19. JSON serialization;
20. complete route.

For each meaningful candidate record both 1-Cell and representative 6-Cell behavior, call/row/group counts, and the complete-route effect.

### Meaningful-work rule

A stage/candidate is worth production work when evidence shows a realistic route-level benefit, not merely because it can be made locally faster. As a default decision gate, investigate a stage when it is at least about **5% of the six-Cell miss or 10 ms p50**, or when several repeated operations from the same ownership boundary plausibly combine above that level.

Do not spend production complexity on 1–2 ms micro-optimizations.

## 6. Optimization tranche A — indexed execution ownership

The first implementation tranche should remove repeated whole-frame filtering while retaining current DataFrame/scientific semantics.

Preferred direction:

- normalize/inspect the raw frame once per source/Cell;
- build bounded request-local lookup structures for the columns already in memory, such as:
  - rows or positional ranges by `step_index`;
  - rows/ranges by `(cycle, step_index)` where useful;
  - execution-group boundaries for measurement steps;
- use those structures to obtain measurement, phase and reference rows without rescanning the entire raw frame for every occurrence/pair;
- preserve `_ordered()` semantics and exact row order;
- avoid copying the complete raw frame per lookup;
- keep the lookup request-local unless a later measured case justifies persistent cache state.

The implementer may choose a better current-code representation after inspection. The locked requirement is the outcome: repeated `raw[...]`/`.isin(...)` scans across the complete frame must not remain merely because they are convenient if a bounded indexed alternative produces a material complete-route win.

### Constraints

- Do not add a new SQLite schema or persistent scientific cache merely for this optimization without a separately demonstrated need.
- Do not reopen original source files.
- Do not create a global unbounded DataFrame cache.
- Do not pass ORM/session state to workers.
- Do not introduce concurrency before measuring the optimized serial/vectorized path.

## 7. Optimization tranche B — measurement grouping and numeric reuse

After tranche A, re-profile. If measurement grouping or numeric conversion remains material, address it.

Candidates include:

- compute measurement occurrence boundaries once for each relevant measurement-step subset instead of regrouping equivalent rows;
- avoid repeated `pd.to_numeric(...)` on the same cycle/current/voltage columns when a validated numeric view can be reused;
- reuse finite masks / extrema inputs where the same frame is tested repeatedly;
- keep capacity and current extraction vectorized;
- avoid building temporary Series/DataFrames solely to recover a scalar when an equivalent array/range operation is clearer and measurably faster.

Every change must retain NaN handling, ordering and cutoff semantics exactly.

## 8. Optimization tranche C — downstream Rate work

After execution extraction is improved, re-profile the whole route. Only then address newly material secondary costs.

Inspect and optimize where meaningful:

- `build_rate_pairs(...)`;
- `detect_sweep_blocks(...)`;
- candidate selection and `_selected_rate(...)`;
- `build_common_rate_comparison(...)`;
- invalid-neighbour/execution validation;
- result/provenance object construction;
- response serialization;
- owner/context/SQL work if it becomes material.

Prefer reducing repeated passes and allocations before concurrency.

## 9. Concurrency/native-code gate

Do **not** automatically copy the Time/Capacity process architecture or introduce Rust/Rayon.

Concurrency/native execution is eligible only if, after serial/indexed/vectorized cleanup:

- at least ~100 ms of independent CPU-bound work remains on the representative multi-Cell route;
- ownership boundaries can use immutable/non-ORM inputs;
- a controlled serial-vs-candidate benchmark shows a material **complete-route** win;
- one-Cell/small workloads do not regress unacceptably;
- deterministic order and scientific projection are exact;
- worker lifecycle/IPC/RSS cost is included.

If those conditions are not met, keep Rate Capability serial.

## 10. Cache and version consequences

Expected: **no migration, no `CALC_VERSION` bump, no result-schema bump**.

A version bump is required only if persisted scientific meaning or response shape changes, which this child should avoid.

Exact-hit behavior is a locked regression gate:

- exact persisted hit remains direct-body serving;
- no Rate `compute()` call;
- no raw read;
- no execution lookup/index construction;
- no worker/native dispatch.

## 11. Required implementation order

1. Capture the Rate-only baseline on the actual implementation head.
2. Preserve a scientific digest/order/provenance reference.
3. Implement tranche A only.
4. Run focused parity tests and re-profile 1/6 Cell complete route.
5. Keep tranche A only if it yields meaningful complete-route benefit without regressions.
6. Re-profile and implement tranche B only for remaining material costs.
7. Re-profile and inspect tranche C.
8. Apply the concurrency/native gate only if the cleaned serial route still justifies it.
9. Run final family benchmark, golden analysis and canonical preflight.
10. Record both rejected and retained candidates so future agents do not repeat failed experiments.

Do not batch all speculative changes and benchmark only at the end.

## 12. Performance acceptance

The child is complete when:

- every Rate stage that remains materially significant has either:
  - been optimized and retained with measured route benefit;
  - been experimentally rejected;
  - or has a documented scientific/complexity reason not to change;
- scientific digest/order/provenance are unchanged;
- exact-hit behavior remains clean.

Because the measured avoidable scan work is very large, the implementation should target a substantial improvement. As a minimum promotion bar, the final representative six-Cell forced-miss p50 should improve by **at least 25%** versus the 050.17 baseline of 1267.82 ms unless controlled experiments demonstrate that the measured scan time cannot safely be removed. That corresponds to a nominal ceiling of about **951 ms**. The goal is to beat this materially, not to stop at it if additional low-risk measured opportunities remain.

Also report:

- final 1-Cell miss p50;
- final 6-Cell miss p50/min/max;
- exact-hit p50;
- per-stage p50/share after optimization;
- response bytes;
- SQL counts/time;
- structural execution counts;
- profiler overhead;
- CPU/RSS/IPC if concurrency is tested.

## 13. Tests

Focused tests must cover, as applicable:

- indexed lookup returns the same rows/order as legacy full-frame filtering;
- repeated step/cycle occurrences and record-index gaps;
- NaN/missing cycle/current/voltage behavior;
- measurement/phase/reference cutoff validation;
- charge and discharge directions;
- multiple rate pairs and sweep blocks;
- invalid-neighbour validation;
- normalization by active mass / electrode area;
- deterministic IDs/order/provenance;
- exact-hit bypass;
- profiling instrumentation cleanup;
- scientific golden regression.

Use the repository's current canonical commands from `AGENTS.md`. Before handoff run:

```powershell
python scripts\preflight.py
```

Do not add redundant full-suite invocations immediately before preflight.

## 14. Out of scope

- Time/Capacity changes or benchmarks;
- frontend redesign;
- new scientific formulas or cutoff semantics;
- new persistent caches without a measured need;
- migrations;
- speculative whole-library prefetch;
- broad analysis-editor refactors.

## 15. Handoff evidence

The implementer handoff must state:

- baseline and final 1/6-Cell miss/hit timings;
- retained optimization tranches and measured route deltas;
- rejected experiments and why;
- final dominant stages/residual;
- scientific/golden parity;
- exact-hit proof;
- tests actually run;
- browser/manual checks actually run or `NOT RUN`.

The reviewer should reject a handoff that reports only a faster helper microbenchmark without a corresponding complete-route improvement.

## 16. Implementation and evidence record

The implementation checkpoint was committed as `ebfcaee` (parent
`6418482`). The final evidence below was captured from the same implementation
tree before the evidence-only review fix; the profiler's embedded commit field
therefore still identifies the clean parent `6418482`. The evidence is recorded
here so the disposable JSON under `tmp/` is not required to reproduce the
optimization decision.

### Workload and environment

The Rate-only command was run three times for each workload:

```powershell
python scripts\profile_analysis_families.py --families rate_capability --skip-time-specialist --output tmp\050.18-rate-final.json
```

The workload is the committed golden Rate Capability fixture: one Cell and six
source-distinct, content-identical relational Cell clones, with forced result
cache misses and separate exact persisted-hit controls. The final run used
Windows 11, Python 3.14.4, NumPy 2.5.0, pandas 3.0.3, pyarrow 24.0.0, and 16
logical CPUs. Rate Capability remained serial; no worker, native, RSS, or IPC
candidate was run. Browser/Plotly timing was `NOT RUN` by explicit user
instruction.

### Complete-route checkpoints

The locked 050.17 six-Cell miss baseline was 1267.82 ms. The implementation-head
Rate-only baseline measured 1240.68 ms p50 (1223.43–1247.63 ms). Each later row
is a complete-route measurement; the delta is versus the immediately preceding
snapshot and is indicative because each snapshot is a fresh three-repetition
run.

| checkpoint | 1-Cell miss p50 (ms) | 6-Cell miss p50 (min–max ms) | incremental 6-Cell delta |
| --- | ---: | ---: | ---: |
| implementation-head baseline | 212.73 | 1240.68 (1223.43–1247.63) | — |
| bounded index, initial | 145.62 | 860.59 (834.96–866.64) | −380.09 ms (−30.63%) |
| grouped position reuse | 134.09 | 794.34 (772.50–837.42) | −66.25 ms (−7.70%) |
| vectorized index construction | 102.71 | 652.11 (638.19–685.16) | −142.23 ms (−17.90%) |
| numeric-array reuse | 96.99 | 549.78 (538.80–589.72) | −102.33 ms (−15.69%) |
| request-local protocol reuse | 102.83 | 457.90 (446.32–482.05) | −91.88 ms (−16.71%) |
| indexed phase voltage values | 79.59 | 340.67 (340.58–357.99) | −117.23 ms (−25.60%) |
| indexed measurement values/positions | 50.58 | 157.75 (155.21–159.68) | −182.92 ms (−53.69%) |
| final ordered-position rerun | 48.34 | **158.77 (155.77–162.16)** | +1.02 ms (+0.65%, noise/ordering-contract rerun) |

The final exact persisted-hit p50 was 9.02 ms for one Cell and 12.25 ms for
six Cells (the implementation-head controls were 14.25 ms and 15.87 ms). Exact
hits made zero scientific compute calls and zero raw-load calls. Final forced
miss response bytes were 272,307 for six Cells; the one-Cell response was
44,831 bytes. The final miss-versus-locked-baseline reduction is approximately
87.5%.

### Final six-Cell opportunity inventory

The following p50 values are from the final three-repetition Rate-only run.
Shares use the 158.77 ms complete-route p50 as denominator. Parent and child
rows are intentionally not additive; the execution-parent residual is the
non-overlapping difference between its timer and its child sum.

| measured stage | p50 ms | route share | scope/calls |
| --- | ---: | ---: | --- |
| complete route | **158.77** | **100.00%** | 3 route samples |
| selection/context | 2.63 | 1.66% | top-level, 1 call |
| cache-key work | 0.08 | 0.05% | top-level, 1 call |
| raw materialization | 27.01 | 17.01% | 6 calls |
| protocol reconstruction | 18.17 | 11.45% | Rate-deep timer, 6 calls; 5 request-local cache hits |
| execution-index building | 42.31 | 26.65% | Rate-deep timer, 6 calls |
| execution extraction parent | 39.64 | 24.96% | Rate-deep timer, 198 calls |
| measurement filtering/grouping | 0.66 | 0.42% | 396 calls |
| phase/reference row selection | 9.89 | 6.23% | 444 calls |
| cutoff validation | 4.52 | 2.85% | 1332 calls |
| capacity extraction | 2.84 | 1.79% | 444 calls |
| current extraction | 8.55 | 5.39% | 444 calls |
| rate normalization | 0.12 | 0.08% | 444 calls |
| execution child sum | 26.59 | 16.75% | non-overlapping child timers |
| execution-parent direct residual | 13.14 | 8.28% | parent minus child sum; zero overlap |
| rate-pair building | 3.17 | 2.00% | 6 calls |
| sweep detection, charge | 0.90 | 0.57% | 6 calls |
| sweep detection, discharge | 0.60 | 0.38% | 6 calls |
| candidate/selected-rate filtering | 0.04 | 0.03% | 6 calls |
| common-rate comparison | 0.69 | 0.44% | 1 call |
| invalid-neighbour validation | 0.16 | 0.10% | 1 call |
| result/provenance assembly | 0.03 | 0.02% | 1 call |
| cache persistence | 5.88 | 3.71% | 1 call |
| JSON serialization | 0.24 | 0.15% | 1 call |

The observed scientific-compute root was 148.68 ms inclusive (93.65% of the
route); it contains the raw-materialization, protocol, index, execution, and
downstream scientific timers above. The final profiler overhead control was
−1.54 ms p50 (−0.96%), with scientific and order parity true for every sample.
The six-Cell structural counts were: 122,946 raw rows loaded; 1,098 step keys;
1,194 cycle/step keys; 444 measurement groups and execution rows; 82,242
measurement rows; 198 rate pairs; 1,290 reconstructed protocol steps; 12
chosen blocks; 48 comparison points; 6 invalid execution pairs; and 102 final
response points. SQL measured 17 statements and 0.490 ms cumulative p50
(six source-header lazy loads; statement-kind p50: analyses 1, cell metadata 1,
other 7, source files 7, source relationships 1).

### Retained and rejected decisions

- **Retained tranche A:** one request-local `_ExecutionIndex` per loaded raw
  source removes repeated full-frame step/cycle/phase scans while retaining
  source-local positions and `_ordered()` semantics. The complete six-Cell
  route moved from 1240.68 ms to 860.59 ms in the initial indexed snapshot.
- **Retained tranche B:** grouped measurement positions, reusable numeric
  arrays, indexed phase voltage values, and direct measurement values remove
  repeated temporary DataFrames and `to_numeric` conversions. The measured
  complete-route snapshots reached 794.34, 549.78, 340.67, and 157.75 ms as
  these bounded changes were promoted. The final ordered-position rerun was
  158.77 ms and retained the explicit shuffled-record ordering proof.
- **Retained bounded protocol reuse:** identical immutable header metadata plus
  nominal capacity is reused only within the request. Five of six clone
  reconstructions were hits; no persistent scientific cache or cache-key change
  was introduced.
- **No downstream production tranche:** rate-pair, sweep, selection,
  comparison, invalid-neighbour, provenance, serialization, and cache stages
  were re-profiled. All were below the route-level materiality gate except the
  already-measured raw/index/protocol/execution boundaries; their measured
  values and shares are recorded above.
- **Serial retained; concurrency/native deferred:** the remaining 148.68 ms
  scientific root is split across required raw loading, one-time index/protocol
  preparation, per-execution ordered result construction, and small downstream
  stages rather than one independent worker-safe kernel. No controlled
  serial-vs-worker/native complete-route candidate with lifecycle/RSS/IPC cost
  was available, so the concurrency gate was not satisfied and serial execution
  remains the lower-risk contract-preserving choice.
- **Remaining material boundaries documented rather than silently optimized:**
  the 42.31 ms index build is the one bounded per-source pass that replaces the
  removed repeated scans; further persistence/native work would add cache or
  lifecycle complexity. The 27.01 ms raw load is required for full-source
  protocol/phase ownership and remains on the existing canonical raw path. The
  18.17 ms protocol timer is already reduced to one distinct reconstruction per
  request; cross-request memoization would change ownership/cache lifecycle.
  The 13.14 ms execution residual is direct per-execution validation/result
  dictionary construction with no isolated safe vectorization boundary; changing
  it risks deterministic order, IDs, provenance, and validation semantics.

No migration, `CALC_VERSION`, result-schema, parser-identity, persistent-cache,
worker, or Time/Capacity change was made. Focused parity/corpus/profiler checks
passed (28 tests), canonical preflight passed 4/4 stages including 80 backend
modules and golden regression, and browser/manual checks were `NOT RUN` by
explicit user instruction.
