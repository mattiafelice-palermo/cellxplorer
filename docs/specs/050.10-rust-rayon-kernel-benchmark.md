# Spec 050.10: Rust/Rayon kernel benchmark

Status: **Implementation complete — awaiting review**
Parent: [`050-runtime-performance-optimization.md`](050-runtime-performance-optimization.md)  
Promoted from: **050.P5** (split from the original broad 050.P3)  
Branch: `feature/runtime-performance-optimization`  
Depends on: **050.9 review-clean**  
Correct merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`  
Review document: [`reviews/050.10-rust-rayon-kernel-benchmark-review.md`](reviews/050.10-rust-rayon-kernel-benchmark-review.md)

## Goal

Determine whether a **small compiled Rust kernel** materially accelerates the numerical hotspot identified by 050.9, then determine how much additional gain comes specifically from bounded Rayon parallelism across independent Cells.

This child isolates two effects:

1. current Python/NumPy/pandas implementation -> equivalent **sequential Rust**;
2. sequential Rust -> the **same Rust kernel with 2/4-way Rayon**.

The result is benchmark evidence only. Do not wire the Rust candidate into normal production requests in this child.

## Implementation record

050.9 identified the derivative rolling, gradient, ratio/filter, and postprocess stages as a
compact material hotspot, while also leaving a separate ordinary Time/Capacity transform
boundary to test. This child adds an isolated benchmark target under
`scripts/rust_derivative_kernel/` and the runner `scripts/profile_rust_derivative_kernel.py`.
The target is a persistent length-prefixed Rust process using Rayon; it is not a PyO3 binding,
is not part of the Tauri crate, and is not imported by normal backend requests. Python resolves
the existing cache-backed inputs and sends one coarse request containing all selected Cells.
Derivative candidates receive contiguous per-segment `float64` capacity/voltage arrays plus
compact phase/CV flags. Ordinary candidates receive contiguous per-Cell cycles, phases, raw
time, owner-prepared capacity, and resolved voltage arrays. C0/N0 are Python numeric adapters;
the corresponding current `analysis_engine` derivative/display helpers remain the scientific
parity oracles. C1/C2/C3 and N1/N2/N3 run the equivalent kernels with 1/2/4 Rayon workers.

The implementation preserves the current centered rolling `min_periods` rule, NumPy-gradient
finite-difference semantics, denominator threshold, percentile scale filter, explicit-CV mask,
discharge absolute-value rule, segment boundaries, cumulative Time reset handling, time-unit
conversion, capacity-axis selection, display reset/mirror semantics, voltage selection and
selection order. The standalone boundary copies input buffers into the subprocess frame and
reports copied bytes; output is decoded as zero-copy NumPy views over the response bytes. Request
filtering, phase/capacity preparation, contiguous buffer materialization and voltage selection
are timed once as owner preparation and excluded from all candidate kernel timers. No ORM/session,
pandas graph, Parquet read,
`CALC_VERSION`, migration, cache, frontend, or production executor changed.

## Entry condition from 050.9

Read 050.9 and its review first. It must identify at least one compact, stable, materially expensive array-oriented kernel worth testing.

If no such kernel exists, do not invent one. Record a no-go decision for native execution and hand that evidence to 050.11.

Candidate kernels may include only when measured as material:

- display-row selection/downsampling;
- contiguous run/segment classification;
- derivative rolling/smoothing;
- derivative gradient/postprocessing;
- finite-row/output-index generation;
- another compact numerical transform named by 050.9.

Ordinary consecutive Voltage-vs-Time may already have too little remaining transform work for Rust to matter. That is a valid outcome.

## Ownership boundary

Python remains owner of request/ORM/session lifecycle, source/protocol/provenance resolution, cache identity, normal Parquet orchestration, final result assembly and API serialization.

Rust owns only the measured numeric kernel:

```text
Python resolves compact numeric buffers
        -> one coarse Python/Rust call
             -> Cell A kernel
             -> Cell B kernel
             -> ...
        -> compact numeric/index outputs
        -> Python result/provenance assembly
```

Do not pass live SQLAlchemy state or a pandas object graph across the boundary. Prefer contiguous NumPy/Arrow-compatible numeric buffers and compact scalar descriptors.

## Locked decisions

1. **Sequential Rust before Rayon.** C1 vs C0 isolates native-code gain; C2/C3 vs C1 isolates Rayon gain.
2. **Same science.** Reproduce the current measured algorithm and operation semantics; no formula redesign.
3. **Measure the boundary.** Copies, dtype conversions, ownership transfer and output conversion count toward latency.
4. **Prefer borrowed/zero-copy numeric input when safe**, but report rather than assume it.
5. **No production integration.** A benchmark-only extension/build target is permitted.
6. **Bound Rayon to 1/2/4 workers** as applicable; never silently use all logical CPUs.
7. **Control nested native threading** and record PyArrow/NumPy/BLAS/Rayon settings.
8. **No whole-engine Rust port.** Reject a non-compact boundary rather than broadening scope.
9. **Keep ORM, SQLite, protocol planning and cache identity in Python.**
10. **Do not move Parquet reading into Rust by default.** 050.9 owns read-concurrency evidence; use identical resolved buffers unless that evidence explicitly identifies the read boundary itself as the experiment.
11. **No `CALC_VERSION`, migration or persistent-cache change** merely for equivalent native execution.
12. **No adaptive zoom, Plotly or cross-family work.**

## Build/binding choice

The existing Windows/Tauri crate was re-inspected. It remains unchanged: it is the desktop
shell, not the right home for a Python numerical extension. The benchmark uses the smallest
reproducible isolated arrangement instead: a Cargo target at
`scripts/rust_derivative_kernel/` with Rayon pinned by its own `Cargo.lock`, compiled with
`cargo build --release --locked`, and driven by a persistent length-prefixed subprocess.
This exposes explicit 1/2/4-worker controls and can be removed without changing packaging or
backend imports. The final host was `x86_64-pc-windows-msvc`, Rust `1.96.1`, release profile,
with Rayon `1.12.0`.

## Required ablation

### C0 — current Python reference

Measure the current implementation of the selected kernel on the same buffers used by Rust, both in isolation and in the representative backend context where practical.

### C1 — sequential Rust

Same kernel, forced to one native worker. Report separately:

- Python -> Rust preparation/conversion;
- borrowed vs copied input and copied bytes;
- Rust kernel time;
- Rust -> Python conversion;
- complete boundary wall time.

C1 vs C0 is the native-execution result.

### C2 — Rust/Rayon, 2 workers

Identical algorithm/boundary, two-way cross-Cell native execution.

### C3 — Rust/Rayon, 4 workers

Same with four workers.

C2/C3 vs C1 is the parallel-Rust result. Do not attribute C0 -> C3 entirely to Rayon.

## Cold and warm lifecycle

Measure separately:

- extension/module load if material;
- first Rust function call;
- first parallel call including lazy Rayon-pool initialization;
- steady-state warm calls;
- resident-memory change.

Do not hide one-time initialization in warm medians, but do not repeatedly charge it to every interaction either.

## Workloads

Use the 050.9 scenarios that actually exercise the chosen kernel rather than repeating the entire earlier matrix. Include at minimum:

- single-Cell control;
- representative 3/6-Cell case;
- 10-Cell case when relevant data permit;
- narrow/small-work case to expose crossing overhead;
- for a derivative kernel, the broad derivative cases identified by 050.9.

Use identical inputs/settings across C0-C3.

## Measurements

For every applicable C row record:

- full representative backend wall time where measurable;
- isolated kernel wall time;
- Python/native conversion/copy time;
- borrowed/copy status and copied bytes;
- Rust kernel time;
- Rayon parallel-region time and any separately valid queue/scheduling timing;
- worker count;
- CPU/effective cores;
- peak RSS/temporary allocation estimate where measurable;
- compiler profile/target and relevant build flags;
- native-thread settings;
- cold first-call and warm-call timings;
- scientific parity metrics;
- deterministic ordering.

Use at least five warm repetitions where practical and report median plus range/tail; keep cold timings separate.

The valid scheduling evidence is the Rust `parallel_region_ms`, aggregate kernel work, and
per-Cell elapsed kernel times returned in selection order. A separate Rayon queue/start-delay
metric is **NOT MEASURED**; no subtraction of concurrent per-Cell times from wall time is used.

## Scientific parity

For exact selection/index/downsampling operations, require exact canonical output and provenance equality.

For floating-point kernels:

- preserve current mathematical algorithm and operation semantics;
- use existing repository scientific/golden tolerances, not a new relaxed Rust tolerance;
- report maximum absolute and relative deviation;
- require identical finite/non-finite placement, segmentation, Cell ordering and provenance;
- preserve thresholds, sign rules and filter ordering.

Record bit-for-bit equality when achievable. If ordinary floating implementation details prevent bit identity while existing scientific tolerances are satisfied, state that explicitly.

A faster kernel with changed accepted science is rejected.

## Required decision

Record separately:

1. sequential native gain: C1 vs C0;
2. Rayon gain: C2/C3 vs C1;
3. Python/native boundary cost;
4. cold-start/build feasibility;
5. memory/oversubscription impact;
6. smallest useful Rayon worker count, if any.

Possible results: no Rust justified; sequential Rust only; Rust + 2-way Rayon; Rust + 4-way Rayon; or reject despite speed because boundary/build/memory/scientific cost is unacceptable.

050.11, not this child, selects the complete execution architecture.

## Results and decision evidence

The final command was:

```text
python scripts/profile_rust_derivative_kernel.py --repetitions 5 --skip-build --output tmp/050.10-r4-r5-full-final.json
```

It ran 32 workloads: 12 derivative workloads and 20 ordinary Time/Capacity workloads across
the committed golden source plus ten disposable source-identical Cell clones, and the real
cached Cells from the saved `Performance analysis` batch. The ordinary matrix includes
one-, three-, six-, and ten-Cell broad Time and capacity cases, an eleven-Cell Time and
capacity case where the application data permit, and one-Cell cycles 1–3 controls. Every
workload used five Python-reference warm repetitions, five C/N warm repetitions per worker
count, and a separate cold call per Rust worker count. The tables report isolated-kernel
medians in milliseconds; the boundary columns are complete Python↔Rust request medians,
including frame copies and output conversion.

| suite / derivative workload | C0 | C1 | C2 | C3 | C1 / C2 / C3 boundary |
|---|---:|---:|---:|---:|---|
| golden / dQ/dV 1 | 122.10 | 2.37 | 2.43 | 2.73 | 10.09 / 9.91 / 10.44 |
| golden / dQ/dV 3 | 387.01 | 7.92 | 5.55 | 4.20 | 33.08 / 30.79 / 29.00 |
| golden / dQ/dV 6 | 759.03 | 15.54 | 8.28 | 6.31 | 67.05 / 58.40 / 56.55 |
| golden / dQ/dV 10 | 1,301.18 | 25.73 | 14.52 | 9.63 | 112.42 / 100.14 / 96.39 |
| golden / dV/dQ 1 | 132.79 | 2.76 | 2.80 | 3.06 | 10.75 / 10.29 / 10.48 |
| golden / dQ/dV cycles 1–3 | 2.06 | 0.43 | 0.29 | 0.28 | 1.07 / 0.75 / 0.81 |
| application / dQ/dV 1 | 140.44 | 2.48 | 2.57 | 2.86 | 8.89 / 8.26 / 8.67 |
| application / dQ/dV 3 | 563.22 | 10.74 | 6.49 | 4.20 | 41.38 / 35.70 / 33.91 |
| application / dQ/dV 6 | 1,140.89 | 19.70 | 11.09 | 8.90 | 81.84 / 71.81 / 71.68 |
| application / dQ/dV 10 | 1,897.70 | 32.47 | 17.75 | 13.64 | 133.84 / 124.64 / 112.50 |
| application / dV/dQ 1 | 144.48 | 2.54 | 2.56 | 2.93 | 8.73 / 8.53 / 8.79 |
| application / dQ/dV cycles 1–3 | 2.17 | 0.34 | 0.31 | 0.34 | 0.79 / 0.81 / 0.82 |

C1 is consistently useful for the derivative kernel. On broad three-, six- and ten-Cell
requests, C2 reduces C1 isolated time by approximately 29–44% and C3 by approximately
47–63%; one-Cell and narrow work remain crossing-sensitive. The native boundary is retained
separately, so these are not full-backend latency claims.

The ordinary native projection results are:

| suite / ordinary workload | N0 | N1 | N2 | N3 | N1 / N2 / N3 boundary |
|---|---:|---:|---:|---:|---|
| golden / Time 1 Cell | 46.24 | 0.46 | 0.47 | 0.59 | 7.39 / 7.15 / 7.12 |
| golden / Time 3 Cells | 147.93 | 1.38 | 1.01 | 0.74 | 26.48 / 25.10 / 24.28 |
| golden / Time 6 Cells | 297.55 | 2.71 | 1.82 | 1.37 | 56.79 / 54.20 / 52.89 |
| golden / Time 10 Cells | 494.11 | 4.45 | 2.73 | 1.83 | 102.99 / 97.75 / 100.24 |
| golden / capacity 6 Cells | 1.10 | 2.04 | 1.39 | 0.98 | 55.04 / 54.69 / 55.50 |
| golden / capacity 10 Cells | 1.94 | 3.47 | 2.18 | 1.59 | 104.83 / 98.28 / 98.66 |
| golden / Time cycles 1–3 | 3.56 | 0.06 | 0.12 | 0.07 | 0.50 / 0.63 / 0.46 |
| application / Time 1 Cell | 42.74 | 0.55 | 0.38 | 0.35 | 5.38 / 5.34 / 5.36 |
| application / Time 3 Cells | 161.66 | 1.48 | 0.91 | 0.88 | 27.54 / 25.91 / 25.72 |
| application / Time 6 Cells | 342.70 | 3.11 | 1.86 | 1.61 | 63.16 / 60.13 / 61.45 |
| application / Time 10 Cells | 531.12 | 4.85 | 2.67 | 1.87 | 113.07 / 106.26 / 105.66 |
| application / Time 11 Cells | 596.40 | 5.25 | 3.15 | 2.03 | 124.44 / 122.25 / 120.37 |
| application / capacity 6 Cells | 1.63 | 2.30 | 1.81 | 1.24 | 62.91 / 61.42 / 59.95 |
| application / capacity 11 Cells | 2.63 | 4.02 | 2.48 | 1.73 | 122.72 / 121.54 / 119.63 |
| application / Time cycles 1–3 | 3.35 | 0.06 | 0.06 | 0.07 | 0.55 / 0.49 / 0.76 |

N1/N2/N3 use one coarse request for all selected Cells. Broad Time projection shows a clear
sequential-native result (N1 is about 99% below N0 in isolated kernel time) and a separate
cross-Cell Rayon result (N3 is about 40–61% below N1 for the broad 3/6/10/11-Cell rows). The
small and one-Cell controls do not justify applying Rayon indiscriminately. Capacity-axis N0
is already sub-millisecond to low-single-digit isolated work; although N3 scales below N1 on
broad batches, the 5–121 ms boundary dominates it, so this evidence does not justify a native
capacity projection by itself. The exact N0–N3 values, copied bytes, CPU time/effective cores,
per-Cell timings, ordering and parity are retained in the disposable JSON report.

This answers the Rust-specific question left by 050.9 rather than substituting for it. The
accepted 050.9 Python-thread result marked ordinary multi-Cell work useful: B3/B4 reduced the
complete backend context from 676.35 ms to 622.98/612.29 ms for golden normal-6 capacity and
from 1,014.46 ms to 920.39/903.70 ms for application normal-11 Time. The new N2/N3 figures
measure only the isolated native numeric projection plus its Python/native boundary; Python
request setup, cache reads, provenance, downsampling and response assembly remain outside it.

Owner-side preparation remained separate from all candidate timers. For broad Time, golden
1/3/6/10-Cell preparation medians were approximately 18/59/126/214 ms and application
1/3/6/10/11-Cell medians were approximately 18/66/140/216/260 ms. Broad capacity preparation
was approximately 31/107/212/368 ms for golden 1/3/6/10 and 29/117/249/375/423 ms for
application 1/3/6/10/11. Rust was not injected into the production request path, so no Rust
full-backend latency claim is made.

### Persistent warm lifecycle (R5)

The explicit `P4` model is one long-lived Rust process with one Rayon pool configured for four
workers. For each suite, the pool was created by an unmeasured warmup request, then the same
worker handled this measured sequence across five repetitions each:

```text
derivative-small-1-3-dqdv -> derivative-1-all-dqdv -> derivative-6-all-dqdv
-> normal-small-1-3-time -> normal-1-all-time -> normal-6-all-time
-> normal-6-all-capacity
```

All 70 P4 warm rows passed scientific parity. Every measured row recorded
`spawn_api_ms=0` and `rayon_pool_init_ms=0`; the one-time cold lifecycle and the first pool
warmup were recorded separately. The four-worker pool was therefore exercised on one-Cell and
small requests without recreating a smaller pool per interaction. Idle/after-pool-warmup working
sets were 4.63/5.08 MB for the fixture session and 4.62/5.03 MB for the application session;
the largest observed peak working sets over the mixed sequences were 53.4 MB and 56.7 MB. The
measured small-request medians were 0.74 ms ordinary Time and 0.92 ms derivative for the
fixture, and 1.07 ms ordinary Time and 1.08 ms derivative for the application, with pool
initialization remaining zero.

| persistent session | spawn API | spawn to ready | pool init | warm spawn/pool |
|---|---:|---:|---:|---|
| golden fixture | 8.97 ms | 13.84 ms | 0.169 ms | 0 / 0 for all 35 rows |
| application batch | 4.68 ms | 9.92 ms | 0.132 ms | 0 / 0 for all 35 rows |

The steady-state recommendation for later composition is therefore a resident bounded worker
and pool, with startup/restart cost treated as service lifecycle rather than charged to every
plot interaction. This child still adds no production startup, supervision, fallback or
packaging integration.

Scientific parity passed for all 480 C1/C2/C3 and N1/N2/N3 warm calls, all 96 separate cold
Rust calls, all 160 C0/N0 warm calls, and all 70 P4 persistent-warm calls. Finite/non-finite
placement, output shape, projection semantics and Cell ordering matched with maximum absolute
and relative deviation both zero. The gate used the existing repository tolerances
(`relative_tolerance=1e-7`, `absolute_tolerance=1e-9`), not a Rust-specific relaxation.

### 050.11 handoff

050.10 independently validates sequential Rust for the measured derivative kernel and the
ordinary Time projection. It validates bounded Rayon for broad multi-Cell derivative and Time
projection batches, but not for one-Cell/small work. Capacity-axis native execution is rejected
as a standalone recommendation because its Python/native boundary dominates the already-small
numeric transform. R5 establishes a resident four-worker process/pool as the benchmarked
steady-state model, while startup/restart/fallback and packaging remain out of scope. 050.11
must compose only the accepted mechanisms, preserve the workload gate and boundary ownership,
and decide the production lifecycle around this evidence.

## Acceptance criteria

- Kernel choice comes from 050.9 evidence.
- C0/C1/C2/C3 and N0/N1/N2/N3 are separated whenever Rust proceeds.
- Boundary copy/conversion and cold/warm lifecycle are measured.
- Scientific parity passes existing repository criteria.
- No normal request is switched to Rust/Rayon.
- No migration, `CALC_VERSION`, persistent cache, saved/export or frontend behavior changes.
- 050.11 receives a clear statement of which native mechanism, if any, survived independently.

## Verification

Completed verification for this benchmark-only child:

- `cargo test --manifest-path scripts\rust_derivative_kernel\Cargo.toml --locked`: PASS (3/3 isolated rolling/gradient tests).
- `cargo build --release --manifest-path scripts\rust_derivative_kernel\Cargo.toml --locked`: PASS (Windows MSVC release target).
- `python -m py_compile scripts/profile_rust_derivative_kernel.py`: PASS.
- `python scripts/profile_rust_derivative_kernel.py --repetitions 5 --skip-build --output tmp/050.10-r4-r5-full-final.json`: PASS (32 workloads, 480 C/N warm rows, 96 separate cold Rust calls, 160 C0/N0 warm rows, and 70 P4 persistent-warm rows; 0 parity failures; exact cache/source inputs from fixture and real batch).
- Focused normal and persistent smoke matrices: PASS; Python adapter, Rust protocol, N0–N3/C0–C3 parity and mixed persistent lifecycle checks all passed.
- Existing production scientific code was not touched; no golden-corpus refresh/export was run. The preflight `tests.test_golden_analysis` module nevertheless passed.

Before handoff:

```text
git diff --check
python scripts\preflight.py
```

If a Rust candidate survives the local performance gate, run the smallest current Windows build/import smoke check needed to support a packaging-feasibility claim. Otherwise record it as NOT RUN.

The isolated Windows release executable and persistent worker smoke both passed. No Tauri or
packaged-sidecar build/import was run because this child deliberately does not integrate the
candidate into production packaging.

## Out of scope

- production Rust wiring;
- production Python executors or multiprocessing;
- scientific algorithm changes;
- broad Rust rewrite;
- adaptive zoom / 050.P4;
- Plotly trace consolidation;
- progressive streaming / 050.P2;
- cross-family indexed detail / 050.P7;
- migrations or scientific-version changes.
