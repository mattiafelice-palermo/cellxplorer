# Review 039.4 — Neware Excel analysis regression and feature closure

Specification: [`../039.4-neware-excel-analysis-regression-and-closure.md`](../039.4-neware-excel-analysis-regression-and-closure.md)  
Parent: [`../039-neware-excel-export-support.md`](../039-neware-excel-export-support.md)  
Branch: `feature/neware-excel-support`  
Merge base: `main` at `0df1fb3e48dfc8a37ee2e9c2a07667ed09942a5b`  
Implementation commit: `805f318b60dc3815459792503073c1b1953d2ed1`  
Implementation handoff: `ae8ac6b8190eaf4f70ef7daf0a80ff40850b9672`  
Status: **Review clean — focused 039.4 and fresh cumulative Parent 039 review both clean**

## Focused 039.4 review

### Confirmed

- The 039.4 implementation delta is closure-focused: generated/registered Excel integration regressions, portable-original provenance coverage, release/version documentation, and a scanner docstring. It does not change `analysis_engine.py`, `step_blocks.py`, `dcir.py`, `chargeability.py`, `rate_capability.py`, `calc.py`, or the analysis router.
- Registered structured `.xlsx` sources are exercised through the existing SQLAlchemy/TestFile/cache and analysis-service paths for Cycles and Time/Capacity rather than through an Excel-only result path.
- The Steps regression preserves the parent distinction between programmed `step_index` and executed `step`: one selected programmed step is executed repeatedly, and the existing Steps compute path emits separate occurrences.
- The DCIR fixture is shaped to the existing rest/pulse contract and is processed by the existing `dcir.detect_candidates` plus `analysis_engine.compute_dcir`; the detector/formula implementation was not changed for Excel.
- The Rate Capability fixture declares three charge rates with a fixed complementary discharge and is processed by the existing protocol reconstruction, pairing and rate-capability compute path; the production detector/extractor was not changed for Excel.
- Excel without semantic condition expressions follows the existing Chargeability no-candidate path. No condition or SoC formula is fabricated from executed data.
- Portable-report coverage verifies that an original `.xlsx` filename, extension and hash are preserved and that the original file is embedded through the existing checksummed original-source payload mechanism. The portable implementation itself required no source-format scientific fork.
- The app version is synchronized at `0.22.0`; `CALC_VERSION` remains `1.6.1`.
- User-facing wording consistently says structured Neware `.xlsx` exports and does not advertise arbitrary/generic Excel support.

## Fresh cumulative Parent 039 review

### Branch scope and architecture

- The cumulative feature branch is based on the locked merge base `0df1fb3e48dfc8a37ee2e9c2a07667ed09942a5b`. The branch diff is confined to Parent 039 parser/import/source-lifecycle support, its tests/spec/reviews, small source-format UI wording, documentation and the final minor version bump.
- Structured Excel ownership is isolated in `backend/app/services/neware_excel.py`; `backend/app/services/parsing.py` owns source-format dispatch and the single global parser-bundle identity. Routers/scanner do not import `openpyxl` directly.
- `.nda`/`.ndax` stay on the existing NewareNDA boundary. `.xlsx` is normalized into the same canonical raw columns, protocol structures and raw/cycle Parquet cache tiers.
- `record` remains the point-level scientific source of truth. Excel `step` and `cycle` summaries are validation layers; they do not replace raw-derived scientific values.
- `step_index` remains programmed protocol identity and `step` remains executed occurrence identity.
- The cache key architecture remains `(source hash, parser version, calc version)`. The global bundle is `NewareNDA version + cxp3`; there is no per-format analysis provenance model and no new cache tier.
- Import inspection remains bounded and DB-free during file inspection: stat/hash/bounded metadata/restat, cached by immutable source fingerprint, with the existing adaptive serial/bounded-process strategy.
- Scanner discovery remains suffix-only. Exact-hash relinking is parser-family guarded, and same-path invalid Excel replacement preserves the previous registered identity/cache while marking it changed.
- Stable source adoption fully validates and builds the replacement before mutating the registered source, then invalidates dependents and removes the old cache only after successful adoption.
- The user-facing Cell model remains exactly one internal `Test` with ordered `TestFile.position` links. No new Test model or multi-Test product state was introduced.
- Mixed binary/Excel continuation behavior remains governed by the existing continuation rules; protocol-derived analyses continue to fail closed for multi-source Cells under the pre-existing shared protocol-analysis guard.

### Scientific and regression integrity

- No production scientific service in the cumulative branch selects a scientific formula because the source extension is `.xlsx`; the scientific analysis modules are unchanged by Parent 039.
- `CALC_VERSION` is unchanged at `1.6.1`; no scientific formula change was bundled into parser support.
- No database migration was added or edited.
- The committed binary golden expected files changed only in parser-version/provenance identity, from the pre-feature parser string to the global `...-cxp3` bundle. The scientific payload remains guarded by the existing golden suite; the harness fallback was updated to the live global parser version rather than a hard-coded historical version.
- No private supplied workbook or other `.xlsx` binary fixture was committed. Excel tests generate privacy-safe workbooks at runtime.

### Release, packaging and documentation closure

- README, AGENTS, architecture/parser documentation, spec index and changelog describe structured Neware `.xlsx` support and its missing-condition limitation without claiming generic Excel support.
- The synchronized user-facing minor version is `0.22.0`; `check_versions` is reported clean.
- The implementer records a successful generated-workbook packaged sidecar smoke, normal registration, Cycles/Steps compute, exact-process restart/cache-hit checks, and successful stable NSIS build. Installer installation itself was not run and is not claimed.
- The final 039.4 analysis/browser UI matrix is truthfully recorded as **NOT RUN**. This is not an actionable implementation finding: 039.4 does not change the scientific-family UI implementations, and the child explicitly permits an unrun browser matrix to be recorded for reviewer/user merge judgment. It remains a sensible optional pre-release/manual check.
- Repository project-context documentation was updated where a repository copy exists. The external ChatGPT Project mirror is outside GitHub and was not silently claimed as updated.

## Verification record

### Implementer-reported

- Parent backend matrix including Excel integration and golden modules: **375 passed**.
- `node --test frontend\\tests\\*.test.ts`: **461 passed**.
- `python scripts\\check_versions.py --expected-version 0.22.0`: **PASS**.
- `python scripts\\preflight.py --no-cache`: **PREFLIGHT PASSED, 5/5 stages**, all 59 backend test modules included.
- Existing golden analysis suite: 30 tests plus 3 approval-checkpoint tests; candidate comparison reported zero changed scientific paths for all eight cases.
- Real supplied workbook, disposable/read-only registered/cache-backed acceptance: 13,982 raw rows, 40 cycles, 201 executed steps, 26 programmed rows, 19.21 mg active mass, 3.3 mAh nominal capacity, start `2026-07-17 11:21:32`; locked capacity/time/energy deviations within tolerance; Cycles/Time-Capacity/Steps exercised; DCIR/Rate Capability/Chargeability recorded not applicable for that protocol.
- Generated packaged-runtime smoke and restart: **RUN/PASS**; stable NSIS build produced `CellXplorer_0.22.0_x64-setup.exe`.
- Final 039.4 analysis/browser UI matrix: **NOT RUN**.

### Reviewer-independent

I independently inspected through the GitHub connector:

- the live coordination handoff and exact implementation/handoff commits;
- the correct merge base and full cumulative changed-file scope;
- Parent 039 and child 039.4 locked decisions/acceptance;
- current AGENTS and relevant parser/scientific/packaging knowledge documents;
- parser dispatch/versioning and canonical cache behavior;
- bounded import inspection and scanner/relink/stable-update behavior;
- the complete 039.4 Excel analysis integration tests and portable `.xlsx` provenance test;
- the cumulative branch for migration, scientific-module, `CALC_VERSION`, golden-provenance and private-workbook scope.

I did **not** independently execute Python, frontend, preflight, packaging, private-workbook, installer or browser/manual commands. The command results above remain implementer-reported evidence.

## Findings

No actionable findings.

## Decision

**REVIEW CLEAN.**

The focused 039.4 review and the fresh cumulative Parent 039 review are both clean. Parent 039 is ready for the user’s merge/release decision. The final manual analysis/UI matrix remains explicitly **NOT RUN** and may be performed as an optional pre-release confidence check; it is not being represented as completed.
