# Review 039.4 — Neware Excel analysis regression and feature closure

Specification: [`../039.4-neware-excel-analysis-regression-and-closure.md`](../039.4-neware-excel-analysis-regression-and-closure.md)  
Parent: [`../039-neware-excel-export-support.md`](../039-neware-excel-export-support.md)  
Branch: `feature/neware-excel-support`  
Merge base: `main` at `0df1fb3e48dfc8a37ee2e9c2a07667ed09942a5b`  
Initial 039.4 implementation: `805f318b60dc3815459792503073c1b1953d2ed1`  
R1 fix: `b2db0c495787a7b547d8b256fbc7595ed774f2a5`  
R1 implementation-record checkpoint: `2ecedf9a22b7ede89ca4ceb8bb60175e6c7aa14d`  
R1 handoff: `bfbac9defa5ab22fc52cd705642336531e4b1e55`  
Reviewer lock: `6e55d93faf5d6d657108b44176a3de10f077bbf2`  
Status: **Review clean — 039.4 R1 resolved and fresh cumulative Parent 039 review clean**

## Focused 039.4 follow-up review

### R1 — Resolved

The registered/cache-backed Excel Rate Capability regression now inspects the actual output of
`rate_capability.compute()` and verifies the three returned charge points by rate and capacity:

- `0.2C -> 10.2 mAh`
- `0.5C -> 10.5 mAh`
- `1.0C -> 11.0 mAh`

The test uses `assertAlmostEqual(..., places=6)` on the computed `capacity_mah` values and retains the
existing recognition, point-count, discharge-family and source-provenance assertions. It does not
re-read the fixture directly and introduces no Excel-specific scientific calculation.

The 039.4 acceptance checklist and implementation record were updated to name the completed numeric
verification and the R1 fix commit.

### Scope of the R1 correction

Comparison from initial 039.4 implementation `805f318...` to R1 fix `b2db0c4...` shows no production
scientific or source-lifecycle code changes. The only code change is the 16-line regression assertion
in `tests/test_neware_excel.py`; remaining changes in that interval are spec/review/coordination
documentation. Therefore the fix does not alter detector thresholds, parser behavior, cache behavior,
scientific formulas, API shapes, schema, or frontend behavior.

## Fresh cumulative Parent 039 review

I reran the cumulative branch review against the locked merge base
`0df1fb3e48dfc8a37ee2e9c2a07667ed09942a5b` after the R1 correction.

### Branch scope and architecture

- The cumulative feature scope remains confined to structured Neware `.xlsx` parser/import/source
  lifecycle support, its tests/spec/reviews, small source-format UI wording, documentation and the
  synchronized minor version bump.
- Structured Excel ownership remains isolated in `backend/app/services/neware_excel.py`; shared
  dispatch and the single parser-bundle identity remain in `backend/app/services/parsing.py`.
- `.nda/.ndax` stay on the existing NewareNDA path; `.xlsx` is normalized into the same canonical raw,
  protocol and raw/cycle cache model.
- `record` remains the point-level scientific source of truth; optional `step` and `cycle` sheets are
  validation summaries only.
- `step_index` remains programmed protocol identity and `step` remains executed occurrence identity.
- Cache ownership remains `(source hash, parser version, calc version)` with global parser bundle
  `...-cxp3`; `CALC_VERSION` remains `1.6.1`.
- Import inspection remains bounded and DB-free during filesystem/parser work. Scanner discovery is
  suffix-only, exact-hash relinking is parser-family guarded, invalid same-path Excel replacement
  preserves the old registered identity/cache, and stable replacement validates/builds before
  relational adoption and old-cache removal.
- A Cell still owns exactly one internal `Test` with ordered `TestFile.position` links. No new Test
  model or Excel-specific downstream scientific state was introduced.
- No production scientific service chooses a formula based on `.xlsx`.
- No database migration was added or modified.
- No private supplied workbook or committed `.xlsx` binary fixture was added; tests generate
  privacy-safe workbooks at runtime.

### Regression and release closure

- The Excel integration suite covers Cycles, Time/Capacity, repeated Steps, DCIR, Rate Capability and
  truthful Chargeability degradation through registered/cache-backed paths.
- The R1 follow-up now closes the only missing locked Rate Capability numeric assertion.
- Existing binary golden scientific outputs remain represented as unchanged apart from the expected
  parser-bundle provenance identity.
- README, AGENTS, architecture/parser documentation, spec index and changelog describe structured
  Neware `.xlsx` support without promising generic Excel import.
- App version remains synchronized at `0.22.0`; `CALC_VERSION` remains unchanged.
- Implementer-reported packaged generated-workbook sidecar/import/Cycles/Steps/restart smoke and
  stable NSIS build remain recorded as PASS; installer installation itself remains not run and is not
  claimed.
- The final 039.4 analysis/browser UI matrix remains truthfully **NOT RUN**. Under the child review
  policy this is an optional pre-release confidence check, not a blocking implementation defect.
- External ChatGPT Project context mirrors were not silently claimed as updated; repository docs are
  the source for any Project-file replacement after merge.

## Verification record

### Implementer-reported for R1 follow-up

- Focused Excel regression: PASS.
- All 8 `tests.test_rate_capability` tests: PASS.
- Elevated `python scripts\\preflight.py --no-cache`: **PREFLIGHT PASSED, 5/5 stages**, including all
  59 backend modules, 461 frontend tests, TypeScript and Vite production bundle.

### Previously implementer-reported Parent 039 closure evidence

- Parent backend matrix including Excel integration and golden modules: **375 passed**.
- `node --test frontend\\tests\\*.test.ts`: **461 passed**.
- `python scripts\\check_versions.py --expected-version 0.22.0`: **PASS**.
- Existing golden analysis suite: 30 tests plus 3 approval-checkpoint tests; candidate comparison
  reported zero changed scientific paths for all eight cases.
- Real supplied workbook acceptance: 13,982 raw rows, 40 cycles, 201 executed steps, 26 programmed
  rows, 19.21 mg active mass, 3.3 mAh nominal capacity and expected start time; locked
  capacity/time/energy deviations within tolerance; Cycles/Time-Capacity/Steps exercised; advanced
  analyses correctly recorded not applicable for that protocol.
- Generated packaged-runtime smoke and restart: **RUN/PASS**; stable NSIS build produced
  `CellXplorer_0.22.0_x64-setup.exe`.
- Final 039.4 analysis/browser UI matrix: **NOT RUN**.

### Reviewer-independent

I independently inspected through the GitHub connector:

- the live R1 handoff and exact R1 implementation/documentation commits;
- the actual `tests/test_neware_excel.py` R1 patch and its assertions on the registered/cache-backed
  `rate_capability.compute()` output;
- the cumulative diff from the locked merge base to the corrected implementation;
- the delta from initial 039.4 implementation to the R1 fix, confirming no production code changed;
- the previously reviewed parser/cache/import/scanner architecture, scientific-module scope,
  `CALC_VERSION`, migration scope, golden-provenance scope, documentation/version closure and
  private-workbook scope.

I did **not** independently execute Python, frontend, preflight, packaging, private-workbook,
installer or browser/manual commands. Those command results remain implementer-reported evidence.

## Findings

No actionable findings.

## Decision

**REVIEW CLEAN.**

R1 is resolved. The focused 039.4 review and fresh cumulative Parent 039 review are both clean.
Parent 039 is ready for the user’s merge/release decision. The final manual analysis/UI matrix remains
explicitly **NOT RUN** and can be performed as an optional pre-release confidence check; it is not
represented as completed.
