# Spec 054.1 — Channel-isolated updater shutdown — Review

## Review status

**Review clean.** R1 and R2 are resolved. Spec 054.1 is accepted and may advance to child 054.2.

The original substantive implementation is commit `45954da9284f10a6955cb37e4e03408a363346e4`, directly after the separately authorized workflow-test repair `b9c84a64de883fbdc635178e6dbd378f71b0d4a1`. Reviewer checkpoints were `b6369742b1b977791b067a3e51e414e149296af3` and `5265fcf4cec5d13bcd33527cbfa8dc0f0123a097`; review-fix checkpoints were `66e9ce4c9764bc1e62112437e94f14743857069e` and the evidence-only `3b2007459ad352b4f2df5e0c8f5e2372ce997384`.

The feature remains intentionally stacked on the Spec 053 Alpha release-channel lineage (`codex/alpha-release-channel` at `1a687ad0aac6cbdf3a6fee6a1c1b72ee644e867d`).

## Implementer-reported verification

The reviewer did not independently rerun these commands or packaged tests. The following is committed implementer evidence.

### Automated / focused checks

- R1 basename fix: NSIS extracts `installation_process_scope.ps1` under the same basename imported by `kill_installation_processes.ps1`.
- Focused process-scope/config suite: **PASS** (32 tests).
- Canonical `python scripts\preflight.py`: **PREFLIGHT PASSED**, 4/4 stages, all 85 backend modules passed.
- Packaged artifacts at `0.27.0-beta.12` were built and installed in disposable Stable/Beta/Alpha roots with installer exit 0.

### Packaged Stable/Beta/Alpha matrix

- Side-by-side install: **PASS** — all three frontends responsive and backend children observed under the matching install roots.
- Stable target update: **PASS** — Stable target processes were reaped; Beta frontend PID `21336` remained responding and backend PID `15872` under the Beta install root returned `/api/health` HTTP PASS with `status=ok` and `database.status=ready`; Alpha frontend PID `2340` remained responding and backend PID `36656` under the Alpha install root returned the same healthy response.
- Beta target update: **PASS** — Beta target processes were reaped; Stable frontend PID `39704` remained responding and backend PID `47604` under the Stable install root returned `/api/health` HTTP PASS with `status=ok` and `database.status=ready`; Alpha frontend PID `2340` / backend PID `36656` remained healthy under the Alpha root.
- Alpha target update: **PASS** — Alpha target processes were reaped; Stable frontend PID `39704` / backend PID `47604` remained healthy under the Stable root; Beta frontend PID `38456` and backend PID `36252` remained healthy under the Beta root, with `/api/health` HTTP PASS and ready database status.
- Orphan target backend: **PASS** — orphan Stable target-path processes were removed by Stable update without taking the sibling Beta channel down.
- Pre-uninstall path: **PASS** — disposable Alpha2 target processes were removed and target root deleted while Stable and Beta sibling processes survived.
- Visual installer/uninstaller inspection: **NOT RUN** — no safe desktop screenshot/control surface was available; automated silent installer/uninstaller smoke used disposable roots/data. This limitation was explicitly accepted in review round 2 because the non-waivable matrix requirement was target-channel frontend/backend isolation rather than visual confirmation.

No GitHub status checks are attached to the evidence checkpoint; the preflight result above is implementer-reported.

## Reviewer code inspection

The implementation remains correct by code reading:

- target process ownership is exact install-root path membership;
- Windows path comparison is case-insensitive and directory-boundary-safe;
- sibling install-prefix collisions do not match;
- protected installer/uninstaller ancestry is excluded;
- cleanup retains the bounded quiet-period loop and aborts on failure;
- installer cleanup uses `taskkill /PID`, not shared executable image names;
- the Tauri running-app path still stops only its tracked `BackendChild` PID tree;
- Stable/Beta/Alpha identities, update endpoints, and data roots remain distinct and unknown identifiers fail closed;
- the corrected packaged predicate filename contract is protected by focused tests that compare the NSIS-extracted basename with the PowerShell-imported basename directly.

## Findings

### R1 — P0 — RESOLVED — Packaged cleanup helper could not import its predicate script

**Affected files**

- `src-tauri/nsis-hooks.nsh`
- `src-tauri/kill_installation_processes.ps1`
- `tests/test_app_channels.py`
- `tests/test_frontend_channel.py`

**Current**

Resolved at `66e9ce4`. NSIS extracts the predicate as `installation_process_scope.ps1`, matching the helper import exactly. Regression tests assert the extracted/imported basenames are equal, and the corrected helper was exercised by packaged update and uninstall paths.

**Target**

Satisfied.

**Acceptance criteria**

- [x] Extracted/imported basename matches.
- [x] Focused regression assertion catches basename divergence.
- [x] Existing ownership/ancestor/no-image-name-kill checks reported green.
- [x] Canonical preflight reported green after repair.
- [x] Corrected packaged helper exercised without import failure.

### R2 — P1 — RESOLVED — Packaged matrix lacked post-update sibling backend survival/responsiveness evidence

**Affected area**

- Packaged Windows acceptance evidence for Spec 054.1
- `docs/specs/054-agent-coordination.md`

**Current**

Resolved at `3b2007459ad352b4f2df5e0c8f5e2372ce997384`. Each non-waivable Stable/Beta/Alpha target-update row now records both sibling backend PID/install-root identity and a live `/api/health` result after the target update. `/api/health` returns `status=ok` with `database.status=ready`, establishing backend liveness and database readiness rather than frontend survival alone.

**Target**

Satisfied.

**Acceptance criteria**

- [x] Stable target: Beta and Alpha sibling backends remain under their own install roots and responsive.
- [x] Beta target: Stable and Alpha sibling backends remain under their own install roots and responsive.
- [x] Alpha target: Stable and Beta sibling backends remain under their own install roots and responsive.
- [x] Backend PIDs/process paths are recorded.
- [x] Application-level backend health results are recorded.
- [x] Row 7 remains explicitly documented as `NOT RUN` with the accepted environmental reason.

## Child readiness

- R1: resolved.
- R2: resolved.
- 054.1: **review-clean / accepted**.
- 054.2: may begin implementation.
- Feature branch: **not ready to merge yet** because parent Spec 054 still has child 054.2 pending and requires cumulative final review after that child.
