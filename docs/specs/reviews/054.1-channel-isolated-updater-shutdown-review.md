# Spec 054.1 — Channel-isolated updater shutdown — Review

## Review status

**Changes required.** Review round 2 resolves R1. R2 remains open for one missing packaged acceptance check. Active child 054.1 is not yet ready to advance to 054.2.

The original substantive implementation is commit `45954da9284f10a6955cb37e4e03408a363346e4`, directly after the separately authorized workflow-test repair `b9c84a64de883fbdc635178e6dbd378f71b0d4a1`. The first reviewer checkpoint is `b6369742b1b977791b067a3e51e414e149296af3`. Review-fix checkpoint `66e9ce4c9764bc1e62112437e94f14743857069e` is one commit directly on top of that review checkpoint and changes only the R1 filename contract/tests plus workflow handoff records.

The feature remains intentionally stacked on the Spec 053 Alpha release-channel lineage (`codex/alpha-release-channel` at `1a687ad0aac6cbdf3a6fee6a1c1b72ee644e867d`).

## Implementer-reported verification at 66e9ce4

The reviewer did not independently rerun these commands or packaged tests; this section records the implementer's committed evidence exactly.

- R1 basename fix: NSIS extracts `installation_process_scope.ps1` under the same basename imported by `kill_installation_processes.ps1`.
- Focused process-scope/config suite: **PASS** (32 tests).
- Canonical `python scripts\preflight.py`: **PREFLIGHT PASSED**, 4/4 stages, all 85 backend modules passed.
- Packaged artifacts built at version `0.27.0-beta.12`:
  - `CellXplorer_0.27.0-beta.12_x64-setup.exe`
  - `CellXplorer.Beta_0.27.0-beta.12_x64-setup.exe`
  - `CellXplorer.Alpha_0.27.0-beta.12_x64-setup.exe`
- All three disposable installs reported exit 0.
- Matrix row 1: **PASS** — Stable/Beta/Alpha installed side by side; frontends responsive at PIDs `39524/42228/33348`; backend children observed under each matching install root.
- Matrix row 2: **PASS as recorded for frontend isolation** — Stable `/UPDATE` exit 0; Stable frontend stopped; Beta PID `42228` and Alpha PID `33348` remained alive; updated Stable binary remained present.
- Matrix row 3: **PASS as recorded for frontend isolation** — Beta `/UPDATE` exit 0; Beta frontend stopped; Stable PID `30908` and Alpha PID `33348` remained alive; updated Beta binary remained present.
- Matrix row 4: **PASS as recorded for frontend isolation** — Alpha `/UPDATE` exit 0; Alpha frontend stopped; Stable PID `30908` and Beta PID `16968` remained alive; updated Alpha binary remained present.
- Matrix row 5: **PASS** — orphan Stable backend launch produced 15 Stable-path processes; Stable `/UPDATE` exit 0; zero Stable-path processes remained while Beta remained alive.
- Matrix row 6: **PASS** — disposable Alpha2 target had 8 target-path processes; pre-uninstall exit 0; target root removed; zero target processes remained; Stable PID `30908` and Beta PID `16968` remained alive.
- Matrix row 7: **NOT RUN** — no safe desktop screenshot/control surface was available for visual installer/uninstaller inspection; automated silent installer/uninstaller smoke used disposable roots/data only.

No GitHub status checks are attached to `66e9ce4`; canonical preflight above is implementer-reported evidence from the handoff.

## Reviewer code inspection, round 2

### R1 implementation

The packaged filename contract is now coherent:

```text
NSIS /oname:  $PLUGINSDIR\installation_process_scope.ps1
PowerShell:    Join-Path $PSScriptRoot "installation_process_scope.ps1"
```

The two focused test owners now capture the `/oname` basename and imported basename with regexes and assert equality. This is materially stronger than the previous substring-only tests and would fail on the exact regression found in round 1.

The underlying cleanup model remains unchanged and correct by code inspection:

- target ownership is exact install-root path membership;
- comparison is case-insensitive and directory-boundary-safe;
- protected installer/uninstaller ancestry is excluded;
- cleanup is bounded by the existing quiet-period loop;
- the helper uses `taskkill /PID`, not shared image-name kills;
- the Tauri running-app path still stops only its tracked `BackendChild` PID tree;
- Stable/Beta/Alpha identities and data roots remain distinct and unknown identifiers fail closed.

### Row 7 treatment

The visual installer/uninstaller inspection is explicitly recorded **NOT RUN** with an exact environmental reason. Under the round-1 R2 acceptance wording, non-core rows could be recorded as NOT RUN with the exact reason; only the three target-channel isolation rows were explicitly non-waivable. Therefore row 7 is accepted as a documented limitation for this child review and is **not** the reason R2 remains open.

## Findings

### R1 — P0 — RESOLVED — Packaged cleanup helper cannot import its predicate script

**Affected files**

- `src-tauri/nsis-hooks.nsh`
- `src-tauri/kill_installation_processes.ps1`
- `tests/test_app_channels.py`
- `tests/test_frontend_channel.py`

**Current**

Resolved at `66e9ce4`. NSIS now extracts the predicate under the exact basename imported by the cleanup script. The regression tests compare those runtime basenames directly. Focused tests and canonical preflight are reported passing, and the corrected helper was exercised by packaged Stable/Beta/Alpha update paths plus the changed uninstall path.

**Target**

Satisfied.

**Acceptance criteria**

- [x] Extracted/imported basename matches.
- [x] Focused regression assertion catches basename divergence.
- [x] Existing ownership/ancestor/no-image-name-kill checks reported green.
- [x] Canonical preflight reported green after repair.
- [x] Corrected packaged helper exercised without missing-script/import failure.

### R2 — P1 — OPEN — Packaged matrix does not record post-update sibling backend survival/responsiveness for rows 2–4

**Affected area**

- Packaged Windows acceptance evidence for Spec 054.1
- `docs/specs/054-agent-coordination.md` / this review file

**Current**

Rows 2–4 record the target update exiting successfully and the two non-target **frontend PIDs** remaining alive. Row 1 establishes that each channel initially had backend children under the matching install root, but the handoff does not record a post-update check that those sibling backend processes were still alive and responsive after each target update.

Spec 054.1 explicitly requires the packaged verification to:

- verify every non-target frontend remains open; and
- verify every non-target backend remains alive and responsive.

That second condition cannot be claimed from frontend PID survival alone. Code inspection makes cross-channel backend termination unlikely, but repository workflow prohibits treating an unperformed/unrecorded manual acceptance check as passed by inference.

Rows 1, 5, and 6 otherwise provide strong packaged evidence for install-root ownership, target orphan cleanup, and protected-uninstaller behavior. Row 7 is accepted as an explicitly recorded NOT RUN limitation under the prior review contract.

**Target**

Complete the existing packaged Stable/Beta/Alpha matrix by recording post-update sibling backend survival/responsiveness for each non-waivable target-channel row.

No code change is required unless this verification exposes a defect.

**Acceptance criteria**

Using the existing disposable installed builds or equivalent corrected packages, record for each row:

1. **Stable target:** after Stable `/UPDATE`, confirm Beta and Alpha backend processes are still present under their respective install roots and remain responsive to a backend/API health request or equivalent application-level responsiveness check.
2. **Beta target:** after Beta `/UPDATE`, confirm Stable and Alpha backends remain present under their respective install roots and responsive.
3. **Alpha target:** after Alpha `/UPDATE`, confirm Stable and Beta backends remain present under their respective install roots and responsive.
4. Record the relevant backend PIDs/process paths (or an equally unambiguous process-root identity) and the responsiveness check/result for each sibling channel.
5. Do not rerun canonical preflight solely for this evidence-only follow-up unless implementation files change; if code/tests do change, rerun the required focused checks and canonical preflight before handoff.
6. Keep matrix row 7 recorded as `NOT RUN` with the existing exact reason; no additional visual claim is required for resolution of R2 in this review round.

## Current merge/readiness assessment

- R1: resolved.
- R2: one verification-only gap remains.
- 054.1: **not review-clean yet**.
- 054.2: **must not start yet**.
- Feature branch: **not ready to merge**; parent Spec 054 still has child 054.2 pending regardless.
