# Review: Spec 050.7 — derivative backend postprocessing optimization

Status: **Review clean — round 1**  
Branch: `feature/runtime-performance-optimization`  
Merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`  
Review baseline before 050.7: `5b0d74e7d63995087ce4399b07b718b4f4b24e9c`  
Parent: [`../050-runtime-performance-optimization.md`](../050-runtime-performance-optimization.md)  
Spec: [`../050.7-derivative-backend-postprocessing-optimization.md`](../050.7-derivative-backend-postprocessing-optimization.md)

## Review scope

Reviewed the complete 050.7 implementation delta after the review-clean 050.6 baseline. The child is backend-only: it removes repeated control/string-processing work from `analysis_engine._derivative_curve()` while preserving the existing derivative numerical kernels, response contract, prepared-capacity reuse, cache/version identities, provenance, downsampling and frontend trace grouping.

The branch also contains the separately authored planning-only `050.P2` proto-child. That document is not implementation scope, is not a scheduled workflow child, and is not part of the 050.7 technical acceptance decision.

## Verification evidence

### Implementer-reported

- Focused derivative + Time/Capacity tests — PASS, 99 tests.
- Supporting cache/path tests — PASS, 61 tests.
- Golden analysis — PASS, 30 tests; all eight committed expected digests remained `SAME`.
- Paired derivative profiler — PASS, five repetitions per scenario.
- `git diff --check` and changed-module `py_compile` — PASS.
- Canonical `python scripts\\preflight.py` — PASS, 4/4 stages; all 77 backend test modules passed; frontend checks skipped as unchanged.
- Browser/manual checks — NOT REQUIRED by this backend-only child.

Reported paired same-machine derivative-stage medians:

| Scenario | Before | After | Reduction |
| --- | ---: | ---: | ---: |
| dQ/dV 1–3 | 10.8 ms | 4.2 ms | 61.4% |
| dQ/dV 1–20 | 50.5 ms | 19.6 ms | 61.1% |
| dQ/dV All | 434.4 ms | 155.9 ms | 64.1% |
| dV/dQ All | 420.9 ms | 157.7 ms | 62.5% |
| dQ/dV All, specific | 437.6 ms | 157.9 ms | 63.9% |

The reported structural counters are unchanged across the paired runs. The All-cycle case still processes 71,190 selected rows, 908 contiguous runs, 454 eligible runs and 405 output/frontend traces on the committed golden fixture.

### Reviewer-independent inspection

Reviewer commands were **NOT RUN** in the Chat + GitHub-connector environment. The following was verified by reading the branch implementation, tests and profiler artifacts:

- Compared the 050.7 delta against review-clean 050.6 baseline `5b0d74e7d63995087ce4399b07b718b4f4b24e9c`; application changes are limited to derivative engine/profiler code and focused backend tests/documentation. No frontend application implementation is included.
- `_derivative_curve()` now classifies the frame-wide explicit-CV-only mask once with existing `calc.status_matches()` semantics. The per-run pandas `astype(str).str.lower().str.contains(...)` work is gone.
- `calc.status_matches()` lowercases distinct status values and performs literal substring matching. For the locked `"cv"` / `"cccv"` needles this preserves the prior case-insensitive substring behavior while treating missing values as non-matches.
- Contiguous run boundaries are computed once from row-aligned changes in `(cycle, segment, phase)`. Run order and keys remain identical to the previous sequential boundary scan.
- The centered rolling mean, `np.gradient`, dQ/dV or dV/dQ division, `1e-10` denominator threshold, non-finite filter, 5th/95th percentile local-scale rejection, discharge absolute-value rule and x/y assignments remain in the same operation order.
- The new status-classification timing is profiler-only. Ordinary scientific responses receive no new profiling fields.
- Focused `DerivativeCurveTests` cover dQ/dV, dV/dQ, specific capacity, even→odd smoothing normalization, explicit CV masking, CCCV preservation, mixed row-local status masking, near-zero denominator, local outlier rejection, NaN input, exact contiguous run/phase counters, phase selection, missing status, empty input and no-eligible-run behavior.
- Profiling aggregation includes the new whole-frame `status_classification` stage and retains the existing bounded derivative counters without exposing source identities/private raw content.
- Durable performance documentation describes the implemented invariant/classification and run-boundary optimization without claiming a new scientific/cache version.
- No SQLite migration, `CALC_VERSION`, parser/canonical version, prepared-derived generation, analysis result schema, analysis-cache identity, frontend trace construction, portable-report or saved-artifact behavior is changed by 050.7.

## Findings

No concrete defect, spec deviation, regression risk requiring a change, or missing child-level verification was identified.

## Merge/readiness status

Spec 050.7 is **review-clean**. The measured broad derivative-stage target is exceeded while the small-range case improves rather than regresses, and the scientific/golden contract is reported unchanged.

This closes the scheduled 050.7 child. Parent 050 may return to cumulative final review. Proto-children `050.P1` and `050.P2` remain planning-only and do not themselves block Parent 050 merge readiness or enter workflow state unless explicitly promoted into numeric children.