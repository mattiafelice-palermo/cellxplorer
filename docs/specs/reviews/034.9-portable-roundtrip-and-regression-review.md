# Review 034.9: Portable round-trip and multi-source regression closure

Specification: [`../034.9-portable-roundtrip-and-regression.md`](../034.9-portable-roundtrip-and-regression.md)  
Parent: [`../034-multi-source-cell-continuations.md`](../034-multi-source-cell-continuations.md)  
Branch: `feature/spec-034-multi-source-continuations`  
Reviewed implementation head: `871d27d28a19a9ae1afd437c02f43f338e7d13b3`  
Current merge base: `4bc4feee35444615714e424e963d8531fdcebc21`  
Status: **implemented and verified by code review**

## Final assessment

No remaining 034.9-specific defect was found.

Portable reports now use the Cell-level ordered `sources` shape, preserve source order across Cell
documents, global source documents, embedded originals and ZIP downloads, and recreate exactly one
internal compatibility row for a newly imported Cell. New exports do not serialize Test names or
Test envelopes as scientific structure.

The strict portable-chain decoder resolves the previous untrusted-input finding:

- a source catalog is required and duplicate catalog IDs are rejected;
- current-format entries require unique known source IDs and integer positions exactly `1..N`;
- `tracked_tail`, when present, must be present consistently and true only on position `N`;
- legacy compatibility accepts exactly one non-empty Test envelope with unique known source IDs;
- malformed structure is validated while loading the package, before inspection resolution or any
  import database write;
- inspection and import therefore return the same deterministic structure error.

The regression closure now contains an explicit 17-case matrix. Each case points to named executable
test methods, and full test discovery executes those methods. The matrix covers stitching, missing
local labels/caches, boundary uncertainty, time gaps/overlaps, compatibility findings, duplicate and
already-linked rejection, reorder/staleness, scheduled-tail scope, portable order, one-row creation,
and second-row rejection.

Current `main` (`4bc4feee35444615714e424e963d8531fdcebc21`) is integrated into the branch through
merge commit `3abfb2fda72e45560fc0ce0614c1643d1f95b731`.

## Prior findings

### R1 — Declarative rather than executable 17-case closure corpus

**Disposition:** Resolved. The fixture maps every case to concrete test methods and the integrated
full suite reported all mapped tests passing.

### R2 — Portable import silently normalized malformed or multiple-chain input

**Disposition:** Resolved. One strict decoder is used before inspection/import work and malformed
current or legacy structures fail deterministically.

### R3 — Closure declared before current-main integration

**Disposition:** Resolved. Current `main` is the feature branch merge base and the integrated matrix
was rerun at the implementation head.

## Verification record

### Implementer reported at integrated head

- `python -m unittest discover tests` — 697 passed.
- `node --test frontend\\tests\\*.test.ts` — 271 passed.
- `npx.cmd tsc --noEmit` — passed.
- `npx.cmd vite build` — passed.
- `python scripts\\check_versions.py` — passed.
- `python scripts\\preflight.py --no-cache` — 5/5 stages passed.
- Browser/disposable-data matrix — not run.

### Reviewer independently performed

- Compared current `main` with the feature head and confirmed the branch is 52 commits ahead, 0
  behind, with `4bc4feee35444615714e424e963d8531fdcebc21` as merge base.
- Inspected Cell/source serialization, source ordering, strict current/legacy chain validation,
  package-load validation order, valid round-trip construction, malformed-package tests, and the
  17-case test mapping at `871d27d28a19a9ae1afd437c02f43f338e7d13b3`.
- Did not execute commands because no executable checkout was available.
- Did not run browser or disposable-data interaction.
- No GitHub Actions status or workflow run exists for the feature head.

## Child readiness

**034.9 is ready.** The parent branch remains blocked only by Review 034.7 R3. After that bounded
invariant fix, rerun the focused 034.7 tests and the no-cache preflight; 034.9 does not require
additional implementation unless those changes touch portable source-chain resolution.
