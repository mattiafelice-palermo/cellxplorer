# Review 034.8: Protocol-derived analysis safety for continued Cells

Specification: [`../034.8-protocol-derived-analysis-safety.md`](../034.8-protocol-derived-analysis-safety.md)  
Parent: [`../034-multi-source-cell-continuations.md`](../034-multi-source-cell-continuations.md)  
Branch: `feature/spec-034-multi-source-continuations`  
Reviewed implementation head: `871d27d28a19a9ae1afd437c02f43f338e7d13b3`  
Current merge base: `4bc4feee35444615714e424e963d8531fdcebc21`  
Status: **implemented and verified by code review**

## Final assessment

No remaining 034.8 defect was found.

The backend centralizes the scientific decision in `analysis_engine.protocol_analysis_guard` and
applies it before protocol-derived cache reads, recognition, computation, background-job creation,
saved artifact/thumbnail access, portable report generation, and idle warmup. Mixed selections fail
as a whole; single-source selections remain supported.

The frontend follow-ups resolve the two previous findings:

- the current local selection is resolved from persisted analysis detail plus live Cell and
  replicate-group data;
- missing source counts remain `pending` and fail closed rather than being interpreted as zero;
- newly added direct Cells and group members therefore cannot issue a protocol-derived compute while
  compatibility is unresolved;
- portable export keeps guarded saved plots visible as disabled rows, names the affected or
  unresolved Cells, selects only supported plots, and revalidates selected IDs before sending;
- the backend remains authoritative and rejects any guarded view that bypasses the frontend.

## Prior findings

### R1 — Portable export silently hid guarded saved plots

**Disposition:** Resolved. Guarded plots remain visible in the dialog with the scientific reason for
omission, cannot be selected, and are excluded only through an explicit supported-plot selection.

### R2 — Frontend mirror could fail open for newly edited selection

**Disposition:** Resolved. Unresolved direct Cells and replicate groups create a pending fail-closed
state, while resolved live data supplies authoritative `source_count` values.

## Verification record

### Implementer reported at integrated head

- Focused backend suite after the follow-up — 73 passed.
- Focused frontend suite after the follow-up — 22 passed.
- Integrated full backend discovery — 697 passed.
- Integrated frontend suite — 271 passed.
- TypeScript, Vite build, version consistency, and `python scripts\\preflight.py --no-cache` —
  reported passed.
- Browser interaction — not run.

### Reviewer independently performed

- Inspected the backend guard and its route/job/artifact/warmup/portable call sites.
- Inspected `multiSourceAnalysisPolicy`, current-selection source-count resolution, pending behavior,
  portable option construction, disabled guarded rows, supported-only selection, and request-time
  revalidation at `871d27d28a19a9ae1afd437c02f43f338e7d13b3`.
- Read the focused frontend policy tests and protocol safety tests.
- Did not execute commands because no executable checkout was available.
- Did not run browser interaction.
- No GitHub Actions status or workflow run exists for the feature head.

## Child readiness

**034.8 is ready.** The parent branch remains blocked only by Review 034.7 R3; no additional 034.8
implementation is required.
