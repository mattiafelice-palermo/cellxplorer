# Review 039.2 — Neware Excel metadata, protocol and cache integration

Specification: [`../039.2-neware-excel-metadata-protocol-and-cache.md`](../039.2-neware-excel-metadata-protocol-and-cache.md)  
Parent: [`../039-neware-excel-export-support.md`](../039-neware-excel-export-support.md)  
Branch: `feature/neware-excel-support`  
Merge base: `main` at `0df1fb3e48dfc8a37ee2e9c2a07667ed09942a5b`  
Original implementation commit: `024525e01b184583d443d672d4b93b91bedfbfc6`  
R1–R3 follow-up implementation: `696e4f967488a1fafd0360a4c8571c53db7ba0cc`  
R4–R5 follow-up implementation: `06b33641446e8592748ff56884d50303d02fdee7`  
R6–R8 follow-up implementation: `0abff4102cb534f0652ffb6bc52648eec69c6c9b`  
R6–R8 handoff checkpoint: `d260f9e54bbfba1fc8b58a8b66ea2eb4a107eda5`  
Status: **REVIEW CLEAN**

## Confirmed

- The branch remains based on merge base `0df1fb3e48dfc8a37ee2e9c2a07667ed09942a5b` and the R6–R8 implementation delta is limited to parser-version/provenance handling, bounded metadata grouping, focused regressions, golden parser-version provenance updates, and the required implementation/coordination records. No 039.3 implementation scope is present in the reviewed follow-up.
- R1–R5 remain resolved; this follow-up did not reopen their behavior.
- R6 is resolved: `EXCEL_PARSER_REVISION` is now `2`, so the shared bundle becomes `v2026.06.11-cxp2`. `parsing.PARSER_VERSION` still derives directly from NewareNDA version plus the Excel revision, the bundle remains well within the persisted 30-character limit, and `CALC_VERSION` remains `1.6.1`. Pre-fix `cxp1` caches/provenance therefore cannot be treated as current `cxp2` output.
- The golden expected-file changes in the R6 follow-up are provenance-only `parser_version` replacements from `v2026.06.11-cxp1` to `v2026.06.11-cxp2`; the inspected implementation patch does not alter numerical golden values.
- R7 is resolved: `_find_labeled_value()` now identifies recognized metadata-label boundaries and restricts value lookup to the cells before the next recognized label. A missing optional value therefore remains missing instead of consuming the neighboring label/value group.
- R7 regression coverage exercises both the `test` information block and optional `unit` Start/End time extraction, including a blank value followed by a populated neighboring group.
- R8 is resolved: the implementation record now names the R6–R8 implementation checkpoint and records the supplied-workbook timing comparison: metadata read `0.262049 s` versus full `parse_timeseries` `2.859019 s` for 13,982 raw rows, with metadata at approximately 9.2% of full parse time.
- No migration, no `CALC_VERSION` bump, no private workbook content, and no packaged-runtime claim were introduced by this follow-up.

## Verification record

### Implementer-reported

For R6–R8 implementation `0abff4102cb534f0652ffb6bc52648eec69c6c9b`:

- combined focused run — **112 passed** (`tests.test_neware_excel` 43, `tests.test_protocol` 23, `tests.test_calc_and_cache` 36, `tests.test_parsing` 2, `tests.test_rate_capability` 8);
- `python -m py_compile ...` — passed;
- `git diff --check` — passed;
- `python scripts\preflight.py` — **PREFLIGHT PASSED**, 5/5 stages;
- real supplied-workbook metadata/protocol/parse/validation/cache probe — passed;
- metadata timing `0.262049 s`; full parse timing `2.859019 s` for 13,982 rows;
- browser/manual UI — not applicable to this backend child;
- packaged runtime — NOT RUN, correctly deferred to 039.3.

### Reviewer-independent

I independently inspected through the GitHub connector:

- exact implementation commit `0abff4102cb534f0652ffb6bc52648eec69c6c9b` and handoff checkpoint `d260f9e54bbfba1fc8b58a8b66ea2eb4a107eda5`;
- the R6–R8 delta against `06b33641446e8592748ff56884d50303d02fdee7`;
- `neware_excel.py` revision and bounded label-group logic;
- `parsing.py` bundle formula;
- the focused R7 and parser-version regressions;
- golden expected-file changes for provenance-only parser-version movement;
- the 039.2 implementation record and required timing evidence.

I did **not** independently execute Python, preflight, private-workbook or packaged-runtime commands. Those results remain implementer-reported.

## Findings status

### R1 — Resolved
Missing `test`/declared-protocol metadata degrades capability without blocking canonical raw parsing.

### R2 — Resolved
Step-summary timing uses declared record interval or the exact 2-second fallback.

### R3 — Resolved
Bare numeric timestamps are rejected for canonical record/metadata/summary timestamp paths.

### R4 — Resolved
The metadata seam establishes the required `record` contract before labelling a workbook as Neware Excel.

### R5 — Resolved
A three-rate Excel-derived protocol reaches the actual Rate Capability pairing seam with the required normalized fields.

### R6 — Resolved
Excel parser revision/provenance advanced to revision `2` / `v2026.06.11-cxp2`; stale pre-fix `cxp1` caches are no longer current under the final reviewed semantics.

### R7 — Resolved
Optional metadata values are bounded to their recognized label groups and cannot bleed into neighboring groups.

### R8 — Resolved
The child-required metadata-vs-full-parse timing evidence and current follow-up checkpoint are recorded.

## Decision

**REVIEW CLEAN — 039.2 may advance to 039.3.**

No actionable R findings remain in 039.2. The reviewer did not independently execute the implementer-reported test/preflight/private-workbook checks, but the code and committed verification evidence satisfy the R6–R8 acceptance criteria and no new defect was identified within the requested review scope.
