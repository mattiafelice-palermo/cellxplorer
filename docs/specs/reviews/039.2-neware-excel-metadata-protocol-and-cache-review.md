# Review 039.2 — Neware Excel metadata, protocol and cache integration

Specification: [`../039.2-neware-excel-metadata-protocol-and-cache.md`](../039.2-neware-excel-metadata-protocol-and-cache.md)  
Parent: [`../039-neware-excel-export-support.md`](../039-neware-excel-export-support.md)  
Branch: `feature/neware-excel-support`  
Merge base: `main` at `0df1fb3e48dfc8a37ee2e9c2a07667ed09942a5b`  
Original implementation commit: `024525e01b184583d443d672d4b93b91bedfbfc6`  
R1–R3 follow-up implementation: `696e4f967488a1fafd0360a4c8571c53db7ba0cc`  
R4–R5 follow-up implementation: `06b33641446e8592748ff56884d50303d02fdee7`  
First R6–R8 follow-up implementation: `0abff4102cb534f0652ffb6bc52648eec69c6c9b`  
Final R6–R8 correction implementation: `871834c06703592a0d1774383ca37498581bf2ac`  
Reviewer lock commit: `4f38bb1ad77c215770862962558d4d983b3945f3`  
Status: **Review clean**

## Confirmed

- The branch remains based on merge base `0df1fb3e48dfc8a37ee2e9c2a07667ed09942a5b`.
- R1–R5 remain resolved and were not reopened during this final R6–R8 pass.
- No 039.3 implementation code landed before this review. From final implementation `871834c0...` to the reviewer lock, only the 039 coordination/spec audit files changed.
- `EXCEL_PARSER_REVISION` is now `3`. The existing global bundle formula therefore produces `v2026.06.11-cxp3`, while `CALC_VERSION` remains `1.6.1`.
- Repository search found no remaining `v2026.06.11-cxp2` current-version reference.
- The final golden expected-file delta is provenance-only: each affected golden JSON changes only `parser_version` from `cxp2` to `cxp3`; no numerical expected values are altered.
- `_find_labeled_value()` now binds every verified metadata label to the fixed value slot two columns to its right, matching the locked A/C, D/F, G/I grammar and the corresponding optional `unit` Start/End layout. It no longer scans later cells or depends on whether a neighboring label is recognized.
- The focused regression leaves `Voltage range` blank while preserving neighboring `Curr. lower` and asserts that `VoltageRange` remains absent. Existing recognized-neighbor and unit Start/End no-bleed regressions remain present.
- The 039.2 implementation record explicitly names both `0abff4102cb534f0652ffb6bc52648eec69c6c9b` and final correction `871834c06703592a0d1774383ca37498581bf2ac`.
- The implementation record retains truthful real-workbook timing evidence and reports the final real-workbook probe after the correction.
- No migration, `CALC_VERSION` bump, private workbook commit or packaged-runtime claim was introduced by the final R6–R8 correction.

## Verification record

### Implementer-reported

For final correction `871834c06703592a0d1774383ca37498581bf2ac`:

- combined focused run — **113 passed** (`tests.test_neware_excel` 44, `tests.test_protocol` 23, `tests.test_calc_and_cache` 36, `tests.test_parsing` 2, `tests.test_rate_capability` 8);
- `python -m py_compile ...` — passed;
- `git diff --check` — passed;
- `python scripts\preflight.py` — **PREFLIGHT PASSED**, 5/5 stages;
- real supplied-workbook metadata/protocol/parse/validation/cache probe — PASS with 13,982 raw rows, 40 cycles and 201 executed steps;
- final timing probe — metadata `0.216633 s`, full parse `2.393578 s`; earlier accepted timing evidence was `0.262049 s` versus `2.859019 s`;
- browser/manual UI — not applicable to this backend child;
- packaged runtime — NOT RUN, correctly deferred to 039.3.

### Reviewer-independent

I independently inspected through the GitHub connector:

- the exact final implementation commit `871834c06703592a0d1774383ca37498581bf2ac`;
- the exact delta from `0abff4102cb534f0652ffb6bc52648eec69c6c9b` through the final implementation;
- the live branch scope after `871834c0...`, confirming only documentation/coordination changes before the reviewer lock;
- `backend/app/services/neware_excel.py` revision and fixed positional metadata lookup;
- `backend/app/services/parsing.py` global parser-bundle formula;
- `backend/app/config.py` confirming `CALC_VERSION = "1.6.1"`;
- the committed verified-layout fixture and final blank-`Voltage range` / `Curr. lower` regression;
- preservation of the existing recognized-neighbor and unit Start/End no-bleed tests;
- the final 039.2 implementation record and timing/checkpoint evidence;
- the final golden-file delta, which is provenance-only;
- repository search for stale `v2026.06.11-cxp2`, which returned no results;
- GitHub combined status for `871834c0...`, which exposes no CI statuses on this feature commit.

I did **not** independently execute Python, preflight, private-workbook or packaged-runtime commands. Those results remain implementer-reported.

## Findings status

### R1 — Resolved
Missing `test`/declared-protocol metadata degrades capability without blocking canonical raw parsing.

### R2 — Resolved
Step-summary timing uses the declared record interval or the exact 2-second fallback.

### R3 — Resolved
Bare numeric timestamps are rejected for canonical record, metadata and step-summary timestamp paths.

### R4 — Resolved
The metadata seam establishes the required bounded `record` contract before labelling a workbook as Neware Excel.

### R5 — Resolved
The required three-rate Excel-derived protocol reaches the existing Rate Capability pairing seam.

### R6 — High: Final parser revision/provenance after R7 semantic correction — **RESOLVED**

Affected files:
- `backend/app/services/neware_excel.py`
- `tests/test_neware_excel.py`
- golden expected provenance files
- `docs/specs/039.2-neware-excel-metadata-protocol-and-cache.md`

**Resolution**

Final implementation `871834c06703592a0d1774383ca37498581bf2ac` advances `EXCEL_PARSER_REVISION` from `2` to `3`. The unchanged bundle formula now yields `v2026.06.11-cxp3`, so metadata produced by the earlier incomplete `cxp2` grouping semantics is not treated as current under the final positional semantics. `CALC_VERSION` remains `1.6.1`.

The inspected golden changes are only `parser_version: v2026.06.11-cxp2` → `v2026.06.11-cxp3`. No numerical golden value changed.

All R6 acceptance criteria are satisfied.

### R7 — Medium: Bind metadata values to verified positional groups — **RESOLVED**

Affected files:
- `backend/app/services/neware_excel.py`
- `tests/test_neware_excel.py`

**Resolution**

`_find_labeled_value()` now reads exactly `label_index + 2`, matching the verified A/C, D/F and G/I export grammar and the equivalent optional `unit` Start/End groups. A blank value therefore remains blank and cannot consume any later neighboring label, recognized or otherwise.

The final regression blanks `C5` (`Voltage range` value) while leaving `D5 = "Curr. lower"` intact and asserts `VoltageRange` is absent. The earlier recognized-neighbor and optional-unit no-bleed tests remain present.

All R7 acceptance criteria are satisfied.

### R8 — Low: Complete child audit record — **RESOLVED**

Affected files:
- `docs/specs/039.2-neware-excel-metadata-protocol-and-cache.md`

**Resolution**

The implementation record now explicitly names:

- first R6–R8 follow-up `0abff4102cb534f0652ffb6bc52648eec69c6c9b`;
- final R6–R7 correction `871834c06703592a0d1774383ca37498581bf2ac`.

It preserves the accepted timing evidence and records the final probe after the correction. No additional performance claim is inferred by the reviewer.

All R8 acceptance criteria are satisfied.

## Decision

**REVIEW CLEAN — 039.3 may begin.**

R1–R8 are resolved. The reviewer may advance `ACTIVE_CHILD` to 039.3 and return control to the implementer according to the coordination protocol.
