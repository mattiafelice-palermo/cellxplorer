# Review 038.8 — Analysis editor integration and thin route

Specification: [`../038.8-analysis-editor-integration.md`](../038.8-analysis-editor-integration.md)  
Parent: [`../038-analyses-feature-modularization.md`](../038-analyses-feature-modularization.md)  
Branch: `feature/analyses-feature-modularization`  
Merge base: `main` at `b6452a4d6691ef1f9b6acf3e353a6f05a5873ed7`  
Implementation commit: `19cad17cc3c7d4b182747a8eaa18fb424d2e3a1a`  
Handoff checkpoint reviewed: `d8e2f614adc84e8f93e4557c550aa6e326407435`  
Status: **Changes required — 038.8 and parent 038 are not review-clean**

## Confirmed

The focused 038.8 implementation is one implementation commit from the accepted pre-038.8 checkpoint `d7720497b633d82abb81da702dcfdb6496954b7b` to `19cad17cc3c7d4b182747a8eaa18fb424d2e3a1a`.

The main ownership move is otherwise consistent with the child:

- `frontend/src/features/analyses/editor/AnalysisEditor.tsx` owns the former single-analysis controller and composition;
- `frontend/src/pages/AnalysisPage.tsx` is reduced to route-id/override resolution plus rendering `AnalysisEditor`;
- the route wrapper preserves the previous `analysisIdOverride ?? Number(useParams().analysisId)` precedence and `workspaceVisible = true` default;
- `AnalysisWorkspaceContent.tsx` imports `AnalysisEditor` and `AnalysesIndexView` directly instead of importing route pages;
- the workspace preserves its keep-mounted/unmount scheduling and visibility behavior while passing the same analysis identifier and workspace visibility into the editor;
- no backend, migration, persistent schema, scientific calculation, API payload, query-key, cache-version, `CALC_VERSION`, artifact-renderer version, portable-format version, dependency, or build-configuration change is present in the focused implementation commit;
- durable ownership/path documentation and the channel-path assertion were updated to the feature-owned locations.

The cumulative branch remains based directly on merge base `b6452a4d6691ef1f9b6acf3e353a6f05a5873ed7`. The implementation scope is the Spec 038 parent package, its eight sequential frontend refactor children, review/coordination records, and path/documentation updates required by those moves. I found no separate backend/scientific feature mixed into the branch.

## Verification record

### Implementer-reported

The 038.8 implementation/handoff reports:

- `python -m unittest tests.test_app_channels -v`: **17 tests, OK**;
- `node --test frontend\\tests\\*.test.ts`: **459 passed, 0 failed**;
- `npx.cmd tsc --noEmit`: passed;
- Vite production build: passed, **7517 modules transformed**;
- required structural searches: clean under the documented interpretation;
- `git diff --check`: passed;
- `python scripts\\preflight.py --no-cache`: **PREFLIGHT PASSED, 5/5 stages**, including **459 frontend tests** and **58 backend test modules**;
- the first sandboxed no-cache preflight hit the known Windows path-access failure; the implementer reports the authoritative elevated rerun passed;
- manual/browser parent integration matrix: **NOT RUN**.

### Reviewer-independent

This review used ChatGPT + the GitHub connector only. I independently inspected:

- the live 038.8 coordination handoff;
- the parent and child specifications;
- the focused `d7720497...` → `19cad17c...` implementation boundary;
- the thin current `AnalysisPage.tsx` against the pre-extraction route/id handling;
- the beginning/end and routing-sensitive portions of `AnalysisEditor.tsx` against the pre-extraction page implementation;
- current `AnalysisWorkspaceContent.tsx` against its pre-038.8 page-based embedding path;
- the final parent target tree and dependency rules;
- cumulative branch scope against the parent merge base;
- durable architecture/path documentation changes;
- the current app-channel path assertion;
- historical path-only documentation rewrites against the merge-base versions of Specs 005 and 024.

I did **not** independently execute Python, Node, TypeScript, Vite, preflight, browser, Plotly/GPU, native-share, or packaged-app checks. Implementer-reported command results are therefore not reviewer-executed verification.

The manual parent matrix remains unrun. This is accurately recorded and is not a separate finding because the parent explicitly permits it to be recorded as manual/not run unless browser automation is separately authorized.

## Findings

### R1 — Medium: `AnalysisEditor` still constructs application routes

**Affected files**

- `frontend/src/features/analyses/editor/AnalysisEditor.tsx`
- route/navigation owner used by the chosen fix, most likely existing workspace/adapter code
- `frontend/src/pages/AnalysisPage.tsx` and `frontend/src/features/analyses/workspace/AnalysisWorkspaceContent.tsx` only if their narrow adapter callbacks need to change

**Current**

The child locks `AnalysisEditor` as a feature-owned editor and explicitly states that it must not know route URL construction. The extraction removed `useParams`, but the new editor still imports `useNavigate` from `react-router-dom` and directly constructs application destinations:

- duplicate success: `navigate(`/analyses/${a.id}`)`;
- delete success: `navigate("/analyses")`;
- not-found recovery: `navigate("/analyses")`.

The editor also still owns `useSearchParams` for the existing `?tab=` / `?plot=` deep-link consumption. That query-parameter behavior is not itself treated as a defect in this finding; the concrete violation is that the feature editor constructs route URLs directly. The implementation record currently states that the editor does not construct URLs, which is therefore not true at this checkpoint.

**Target**

Keep the editor product behavior unchanged while removing direct route construction from `AnalysisEditor`.

Use the smallest semantic navigation boundary that fits the already-locked 49-entry tree. Do **not** add a new production module solely for this fix. An acceptable implementation can reuse an existing workspace/adapter owner for route construction, or extend the existing minimal editor integration with narrow semantic navigation callbacks/intents. Do not pass router objects, the full editor state object, or introduce a general routing abstraction/global store.

Preserve duplicate -> copied analysis, delete/not-found -> Analysis Database, deep-link consumption, workspace keep-mounted/unmount behavior, invalidations, and browser-history behavior.

**Acceptance criteria**

1. `AnalysisEditor.tsx` contains no direct construction of `/analyses` or `/analyses/<id>` navigation destinations and no `navigate(...)` calls that encode those routes.
2. Duplicate success still opens the duplicated analysis through both direct-route and workspace usage.
3. Delete success and the not-found Back action still return to the Analysis Database through both direct-route and workspace usage.
4. Existing `?tab=` / `?plot=` deep-link restore-and-consume behavior remains unchanged; do not drop or double-consume it while changing the navigation boundary.
5. `AnalysisPage.tsx` remains a thin adapter and `AnalysisWorkspaceContent.tsx` remains page-independent; no module under `features/analyses/` imports a route page.
6. No extra production file is added outside the locked Parent 038 target tree.
7. Update the 038.8 implementation record so its statement about route/URL ownership matches the actual implementation.
8. Rerun the focused frontend/app-channel checks, TypeScript, Vite, structural searches, `git diff --check`, and `python scripts\\preflight.py --no-cache`; record exact results and continue to record the manual matrix accurately as run or NOT RUN.

### R2 — Medium: historical specs were rewritten to pretend they used the new 038 paths

**Affected files**

At minimum:

- `docs/specs/005-warmup-queue-deadlock-fix.md`
- `docs/specs/024-same-folder-drop-deletes-items.md`
- `docs/specs/004-app-shell-quick-settings.md`

Also inspect any other pre-038 historical spec changed only to replace an original analysis/component path with its new Spec 038 owner.

**Current**

038.8 explicitly says: **“do not rewrite historical specs to pretend they originally used the new paths.”** The cumulative branch nevertheless rewrites historical documents in exactly that way.

The clearest example is Spec 005. At the merge base, its 2026-07-25 defect and implementation record truthfully refer to the then-current owners such as:

- `frontend/src/pages/AnalysisPage.tsx`;
- `frontend/src/components/CacheWarmupCoordinator.tsx`;
- `frontend/src/warmupCompletion.ts`.

The current branch changes those historical passages to `features/analyses/editor/artifacts/...`, including the old implementation record itself, which makes it read as though the 2026-07-25 implementation originally landed in the future Spec 038 structure. Spec 024 similarly changes its historical “pattern established by `frontend/src/warmupCompletion.ts`” statement to the new artifact path.

These are not durable current-architecture documents; they are historical specifications/implementation records. Current ownership is already documented in `AGENTS.md`, `docs/agent-knowledge/`, and the active 038 specs.

**Target**

Restore historical specs so statements about their original implementation/anchors remain historically truthful. Do not make a historical implementation record track later file moves.

Where a historical spec genuinely needs to help a current reader find moved code, retain the original historical path and add a clearly labelled present-day note such as “Current owner after Spec 038: ...” rather than replacing the historical fact.

Do not revert the final-path updates in current durable ownership documentation (`AGENTS.md`, `docs/agent-knowledge/`) or in the active 038 specs/reviews.

**Acceptance criteria**

1. Spec 005 again records its original 2026-07-25 implementation paths accurately, or explicitly distinguishes original path from current post-038 owner without falsifying the historical record.
2. Spec 024's historical reference to the pattern that existed when it was written is restored or equivalently qualified as historical/current.
3. Audit the other pre-038 spec files touched by this branch for the same path-only rewrite; restore/qualify any equivalent cases, including Spec 004 where applicable.
4. Current ownership remains correctly documented in `AGENTS.md`, `docs/agent-knowledge/architecture.md`, and the Spec 038 package.
5. No application/scientific behavior changes as part of this documentation correction.
6. `git diff --check` and the final required 038.8 verification remain clean after the documentation corrections.

## Cumulative parent review

Apart from R1 and R2, the cumulative Spec 038 ownership model is consistent with the locked parent target:

- the Analysis Database, workspace, editor, family, plotting, protocol, recognition, policy, artifact, and portable responsibilities now live under `features/analyses/`;
- `AnalysisPage.tsx` and `AnalysesIndexPage.tsx` are thin adapters rather than helper libraries;
- the previously reviewed Cycles, Time/Capacity, saved-artifact/warmup, portable-flow, visibility, protocol, recognition, and database/workspace moves remain present at their accepted owners;
- no backend/scientific change was required to complete the frontend organization;
- the final no-cache verification is recorded as passing by the implementer.

R1 prevents declaring the final route/feature dependency boundary complete. R2 prevents accepting the required final documentation reconciliation. Therefore the cumulative parent cannot yet be marked complete or ready to merge.

The PR title/body are also stale and still describe a specification-only PR. Refresh that metadata before final merge so the PR accurately describes the implemented cumulative feature; this is merge hygiene rather than a separate 038.8 code finding.

## Decision

**Changes required.** Resolve R1 and R2 only, rerun the required final verification, and request a second 038.8/final-parent review.

038.8 is **not yet review-clean**. Parent 038 is **not yet complete**, and the branch is **not ready to merge to `main`**.
