# Review 038.3 — Existing editor module organization

Specification: [`../038.3-existing-editor-module-organization.md`](../038.3-existing-editor-module-organization.md)  
Parent: [`../038-analyses-feature-modularization.md`](../038-analyses-feature-modularization.md)  
Branch: `feature/analyses-feature-modularization`  
Merge base: `main` at `b6452a4d6691ef1f9b6acf3e353a6f05a5873ed7`  
Implementation commit: `a57851fa9e19a2f323da6bc3fc0190a5c046fe7e`  
Coordination handoff checkpoint: `2cdabaa21b4d6f60513bff5b04f6e90cf9c1285c`  
Status: **Review clean — ready to proceed to 038.4**

## Confirmed

The implementation is one focused commit ahead of the prior coordination checkpoint and stays within the 038.3 ownership boundary.

The 15 named modules are at their locked target paths:

- family modules:
  - `families/cycles/diagnosticCycles.ts`;
  - `families/steps/StepsPlotCard.tsx`;
  - `families/dcir/DcirPlotCard.tsx`;
  - `families/chargeability/ChargeabilityPlotCard.tsx`;
  - `families/rate-capability/RateCapabilityPlotCard.tsx`;
- protocol modules:
  - `ProtocolSegmentsPanel.tsx`;
  - `ProtocolStructureViewer.tsx`;
  - `protocolGroupNormalization.ts`;
  - `protocolStepFilters.ts`;
  - `protocolStepNeighbours.ts`;
- recognition modules:
  - `RecognitionProgress.tsx`;
  - `recognitionProgress.ts`;
- policies:
  - `analysisDraftPolicy.ts`;
  - `analysisPlotPolicy.ts`;
  - `multiSourceAnalysisPolicy.ts`.

The existing `analysisVisibility.ts` remains the separate canonical visibility owner established by 038.1.

## Family-card behavior

I compared the current Steps, DCIR, Chargeability, and Rate Capability modules against their pre-038.3 versions. Their changes are ownership/import-path changes only:

- API/result/view types are unchanged;
- query hooks and endpoints are unchanged;
- settings, trace/layout builders, visibility helpers, export helpers, and public exports remain in the same family cards;
- generic `Plot` remains owned by shared components;
- plotting/style/visibility dependencies point directly at their existing 038.1 owners;
- protocol and recognition dependencies point directly at their new 038.3 owners;
- none of the family modules imports `pages/AnalysisPage`.

`AnalysisPage.tsx` imports the moved family modules from the new feature paths, preserving the intended one-way page/editor dependency for this checkpoint.

The explicit `.tsx` extension on imports of `RecognitionProgress.tsx` disambiguates it from sibling `recognitionProgress.ts` on case-insensitive Windows filesystems. The implementer-reported TypeScript and production build both pass with that resolution.

## Protocol, recognition, and policy ownership

`ProtocolSegmentsPanel.tsx` and `ProtocolStructureViewer.tsx` import `normalizeGroup` directly from sibling `protocolGroupNormalization.ts`. The old pure-helper re-export through the UI component is removed.

The pure protocol helpers remain direct deterministic frontend structure/display helpers; no backend protocol reconstruction responsibility moved into the frontend.

`recognitionProgress.ts` retains the existing shared-token map, background-job polling interval/termination rule, delayed visibility behavior, and error/loading semantics. `RecognitionProgress.tsx` is a pure move.

The policy moves preserve their logic. In particular:

- `analysisDraftPolicy.ts` remains the owner of draft/save/discard transitions;
- `analysisPlotPolicy.ts` retains `SAVED_PLOT_THUMBNAIL_RENDER_VERSION = 6`, matching the pre-move value;
- `multiSourceAnalysisPolicy.ts` is a pure move;
- `workspace/analysisWorkspace.ts` now imports the draft-policy type from its final owner.

No old root/component compatibility copies or re-export shims were introduced.

## Repository/document path updates

The implementation updates maintained repository and specialist documentation whose source-path references became stale as a direct consequence of these moves. Those changes describe the new owners rather than changing scientific definitions.

The path-sensitive app-channel test was updated to the new family/protocol/recognition paths while retaining its semantic-teal assertions.

No backend, API, migration, scientific calculation, query-key, result-schema, persistent-spec, cache identity, or artifact-version change is present.

## Verification record

### Implementer-reported

The implementation/handoff records:

- focused 038.3 frontend policy/protocol/diagnostic suite: `71/71` passed;
- `python -m unittest tests.test_app_channels -v`: `17/17` passed;
- `npx.cmd tsc --noEmit`: passed;
- `npx.cmd vite build`: passed (`7512` modules transformed; existing non-blocking bundle warnings only);
- all three required stale-path searches: no matches, including no family import from `pages/AnalysisPage`;
- `python scripts\preflight.py`: `PREFLIGHT PASSED`, `5/5` stages, including `459` frontend tests and `58` backend test modules plus type check and production bundle;
- browser/manual matrix: **not run**.

### Reviewer-independent

This review used ChatGPT + the GitHub connector only, not Work. I independently inspected the live branch, isolated implementation diff, active parent/child/coordination state, representative pre/post family-card source, protocol direct-helper ownership, recognition-progress implementation, page imports, saved-plot renderer version, and current test/path updates.

I did **not** independently execute Node, Python, TypeScript, Vite, preflight, grep, or browser checks. The command results above are implementer-reported.

## Findings

No actionable 038.3 defect, specification deviation, regression risk, or missing required verification was found.

The manual family/recognition/policy/multi-source/theme/keyboard matrix remains unrun. This is not a finding because 038.3 explicitly requires those checks to be recorded as run or not run, and the implementation record accurately says they were not run.

## Decision

**038.3 is review-clean. 038.4 may begin.**

The shared feature branch is **not ready to merge to `main`** because 038.4–038.8 remain part of the same parent feature and require their sequential implementation/review gates.