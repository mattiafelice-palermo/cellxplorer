# Spec 054.2 — Non-interactive automatic updates — Review

## Review status

**Review clean at the implementation-child level.** R1 is resolved at `b8ac6d3259eb1f9884a11aec6a4bdc1b4724ced7`. R2 is closed as an implementer finding because all repository-controlled prerequisites are now verified and the only remaining acceptance input is the unavailable live disposable-Windows desktop matrix. **The live matrix is not waived, not inferred, and not marked PASS; it transfers unchanged to the parent FINAL_REVIEW external/manual acceptance gate.**

The substantive 054.2 implementation remains `38a636e7a1db2e116bde813652c41669dfb635c2`, directly after accepted 054.1 reviewer checkpoint `e8ef3f62a9ffe5b7b6bc06b95af70c9159777766`. Reviewer checkpoint `1b0796aca7348ef690cb5e36d666bb3207e9807a` raised R1/R2; `b8ac6d3` repaired R1; reviewer checkpoint `168aefd03ff03def0a7f758a5098fbff61c8aa12` returned only R2; `8506722f68a4916c2ff52ae05cedd647d01ae290` repaired only the pre-existing hosted Alpha-bootstrap path assertion; `f3047f7684b2e5629f38055e443815eb3000dfee` accepted that portability checkpoint and returned only R2; `161b434607e392bd084c0f006399507c60ccc586` records the signed hosted build evidence reviewed below.

## Verification evidence

### Implementer-reported local evidence

The reviewer did not independently run the local commands. The handoffs record:

- post-R1 focused updater/config/channel suite: **PASS** (49 tests);
- canonical `python scripts\preflight.py --no-cache` after R1: **PASS** (4/4 stages; 158 backend/frontend test files/modules);
- fresh Stable/Beta/Alpha disposable package builds after R1: **PASS** at `0.27.0-beta.12`;
- Alpha bootstrap portability regression test after `8506722`: **PASS**;
- canonical `python scripts\preflight.py --no-cache` after `8506722`: **PASS** (4/4 stages; 158 backend/frontend test files/modules).

### Reviewer-verified hosted signed-build evidence

The reviewer independently inspected GitHub Actions runs and artifact metadata for the repository's manual build-only `release.yml` path. All three runs use exact head SHA `f3047f7684b2e5629f38055e443815eb3000dfee`, which is the reviewer checkpoint immediately before the evidence-only `161b434` handoff; no product code changed in `161b434`.

- **Stable — workflow run 45 / run id `33330210124`: PASS.** Exact branch/SHA confirmed. Release-local no-cache preflight passed. The Tauri build environment had the repository signing secret available, generated the Stable NSIS updater executable and matching `.exe.sig`, and uploaded workflow artifact `windows-x64-nsis` id `9737543578`, size `85,156,028` bytes, digest `sha256:a79cc97d4b848d9f39dbe3961d1db9b997ab49334ff079c8362a83340513afeb`.
- **Beta — workflow run 46 / run id `33330717773`: PASS.** Exact branch/SHA confirmed. Release-local no-cache preflight passed; Beta channel verification passed; Tauri used `src-tauri/tauri.beta.conf.json` with the signing secret available, generated the Beta NSIS updater executable and `.exe.sig`, and uploaded artifact id `9737654461`, size `85,147,409` bytes, digest `sha256:1556de928f9c745b0378096dc5aa3185bea0eba15b687a71521f1513874cff7f`.
- **Alpha — workflow run 47 / run id `33331143970`: PASS.** Exact branch/SHA confirmed. Release-local no-cache preflight passed all 4/4 stages and 158 backend/frontend modules/files, including `tests.test_alpha_bootstrap`; Alpha channel verification passed; Tauri used `src-tauri/tauri.alpha.conf.json` with the signing secret available, generated the Alpha NSIS updater executable and `.exe.sig`, and uploaded artifact id `9737778011`, size `85,145,397` bytes, digest `sha256:09a6fa7c8523671f9f9d61afee190d1c90d15df0253cb5d0e131dfc3f7e62315`.

For all three manual build-only runs, the release/publish path was intentionally not entered: there was no release/tag publication and the publication/channel-pointer steps were skipped. This establishes that signed updater inputs can be produced safely without cutting a release.

### Still NOT RUN

The required **live in-app passive-update desktop matrix remains NOT RUN**. There is no safe disposable Windows desktop/VM control surface available, and the real per-machine Stable/Beta/Alpha installation identities and user data must not be used as destructive acceptance fixtures.

The hosted build-only jobs cannot substitute for observation of the actual desktop takeover. They prove signed package production and hosted preflight, not the following runtime behaviors:

- CellXplorer download progress and `Applying update…` immediately before exit;
- absence of the ordinary interactive NSIS wizard;
- visible passive NSIS progress after the application closes;
- no material blank/no-feedback interval;
- unattended completion and relaunch/manual-launch outcome;
- requested version actually running afterward;
- representative analysis/saved-plot/settings preservation;
- live sibling-channel frontend/backend survival during the in-app update;
- controlled failure behavior where practical.

Those rows remain mandatory parent acceptance evidence and must be performed when a safe disposable Windows desktop/VM becomes available.

## Reviewer inspection

### Passive updater configuration and channel resolution

The implementation remains code-review clean. Stable uses updater `installMode: "passive"`; Beta and Alpha inherit the same updater mode while replacing their channel endpoint/identity overlays; standalone NSIS installation remains `perMachine`. Updater artifacts remain enabled and signature/version/channel validation is preserved.

### Visible `Applying update…` handoff

`runVerifiedInstall()` dispatches the protected `launching` state before `/api/session/finish` and before invoking `install_app_update`. The modal renders the explicit `Applying update…` state with active progress feedback, and duplicate/dismiss actions remain protected.

### Retry install without re-downloading

Install retry calls the install path directly rather than re-entering download. Rust restores the same verified update bytes after a pre-exit `update.install(&bytes)` error, and version/channel validation remains in force.

### R1 repair: passive NSIS progress-page initialization and action hiding

`CxBeforeInstFiles` initializes `CxPrepareWindow` for passive `/P` before the styled install-files page uses the custom fonts and 760×500 frame. `CxStyleInstFilesBody` re-hides Back/Next/Cancel after MUI page creation when passive. Interactive installs retain the existing action-visible path. Focused regression coverage encodes this contract.

### Hosted Alpha-bootstrap assertion portability repair

`8506722` changes only `tests/test_alpha_bootstrap.py` plus generated workflow files. The old raw string-prefix assertion was vulnerable to Windows path spelling/canonicalization. The replacement compares `Path(paths[0])` to the exact resolved Alpha managed-import destination, matching the established Beta-test pattern. Hosted Alpha run 47 now independently confirms that test passes in the release workflow.

## Findings

### R1 — P1 — RESOLVED — Passive progress page skipped initialization required by custom NSIS styling

**Affected files**

- `src-tauri/cellxplorer-installer.nsi`
- `tests/test_updater_configuration.py`

**Current**

Resolved at `b8ac6d3`. Passive install-files now initializes its required custom geometry/fonts before styling and does not expose interactive navigation actions.

**Target**

Satisfied.

**Acceptance criteria**

1. [x] `/P /UPDATE` reaches install-files with custom frame/font prerequisites initialized by code path.
2. [x] Passive mode does not restore welcome/location/reinstall decision pages or action buttons.
3. [x] Custom progress geometry has its outer-frame prerequisite before inner layout.
4. [x] Focused regression coverage encodes passive initialization/action hiding.
5. [x] Standalone interactive installer path remains distinct and `perMachine`.
6. [x] Focused updater/config tests and canonical preflight reported PASS after repair.
7. [ ] Runtime visual confirmation belongs to the still-required external desktop acceptance gate; it is not inferred here.

### R2 — P1 — RESOLVED AS IMPLEMENTER FINDING / TRANSFERRED TO FINAL EXTERNAL GATE — Signed packaged passive-update acceptance

**Affected area**

- Windows signed updater acceptance for Stable/Beta/Alpha
- parent Spec 054 final packaged/manual acceptance

**Current**

The repository-controlled portion of the finding is now satisfied: signed Stable/Beta/Alpha updater artifacts were independently verified from successful build-only GitHub Actions runs on exact product checkpoint `f3047f7`, and all three hosted runs passed release-local no-cache preflight without publishing a release.

The remaining unsatisfied portion is exclusively a manual external acceptance dependency: there is no safe disposable Windows desktop/VM on which to perform the real in-app updater takeover. No further implementer code change or repository-side evidence generation is available or requested unless that future matrix exposes a defect.

**Target**

At FINAL_REVIEW, block on the unavailable disposable Windows desktop/VM rather than keeping an implementer in `FIX_REVIEW` with no actionable change. When the environment becomes available, resume final review and perform the live matrix below before completion.

**Acceptance criteria retained for final external acceptance**

For Stable, Beta, and Alpha, using a real signed updater artifact and safe disposable/test state:

1. [ ] Start from an older installed build and initiate the update from CellXplorer.
2. [ ] Observe CellXplorer download progress, determinate when length is known and visibly indeterminate otherwise.
3. [ ] Observe the `Applying update…` handoff before old-app exit.
4. [ ] Confirm the ordinary interactive NSIS wizard does **not** appear.
5. [ ] After the main app exits, observe the compact passive updater/progress window while installation is actually running.
6. [ ] Confirm no material blank/no-feedback interval between CellXplorer handoff and installer progress.
7. [ ] Confirm installer progress requires no user interaction and ends with an understandable completion/failure outcome.
8. [ ] Confirm automatic relaunch when supported, or record the exact visible completion/manual-launch fallback.
9. [ ] Confirm the requested new version is installed/running.
10. [ ] Confirm representative analyses, saved plots, and settings remain present.
11. [ ] With another channel running, confirm its frontend remains responsive and its backend remains alive/responsive.
12. [ ] Exercise a controlled download/install failure if practical; otherwise record the row as NOT RUN with the exact reason.
13. [x] Signed Stable/Beta/Alpha build inputs and exact run/artifact identities are recorded; private signing material was neither exposed nor committed.

## Child readiness

- Passive configuration: code-review clean.
- Stable/Beta/Alpha resolution: code-review clean.
- `Applying update…` handoff: code-review clean.
- Retry install without re-download: code-review clean.
- Signature/version validation: preserved.
- Hosted Alpha-bootstrap assertion portability repair: accepted and hosted-confirmed.
- Signed build-only artifacts: reviewer-verified for Stable/Beta/Alpha.
- R1: **resolved**.
- R2: **resolved as an implementer finding; mandatory live rows transferred unchanged to final external/manual acceptance**.
- 054.2: **review-clean at the implementation-child level**.
- Parent 054: must enter cumulative FINAL_REVIEW and cannot be COMPLETE until the live disposable-Windows matrix is performed.
