# Review: Spec 050.11 execution-strategy composition and decision

Status: **Review clean**

Branch: `feature/runtime-performance-optimization`  
Correct merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`  
Active child reviewed: `050.11`

## Scope reviewed

Reviewed the returned R1-R3 corrections against the accepted 050.9/050.10 evidence and the live benchmark implementation. The child remains benchmark/decision-only; it does not wire native execution or concurrency into production requests.

Implementer-reported verification for the final round:

- corrected full composition benchmark: PASS (22 workloads, 550 candidate runs, five warm repetitions, golden fixture plus saved real `Performance analysis` data, zero reported parity failures);
- exact result-cache controls: PASS;
- `py_compile`, `tests.test_analysis_engine` 95/95, Rust tests 3/3, Rust release build and `git diff --check`: PASS;
- canonical `python scripts\preflight.py`: PASS (4/4; all 77 backend modules);
- browser/manual: NOT RUN, correctly not required for this benchmark-only child.

Reviewer independently inspected the corrected composition profiler and implementation record. The reviewer did **not** independently execute the local benchmark/test commands in this connector-only environment.

## Resolved findings

### R1 — resolved

A0 and R1/D0/D1/D2 now stop the backend wall timer before JSON serialization. Serialization is recorded separately for all candidates, so the headline complete-backend comparisons are now like-for-like.

### R2 — resolved

The composed candidates now use a single pre-native production preparation pass. `PreparedCell` retains the filtered/sorted rows, phase/capacity state, provenance inputs and downstream metadata; the benchmark then materializes native buffers once, performs one coarse Rust call, and continues downstream masking/downsampling/response assembly from that retained state without re-entering `compute_time_capacity()` and repeating upstream transforms.

This is still a benchmark-owned mirror rather than production integration, which is appropriate for 050.11. Scientific parity/order is reported exact across the final matrix.

### R3 — resolved

`derivative-10-all-dqdv` is now added locally whenever at least ten Cells are available. The final fixture and application matrices both include the real 10-Cell derivative row, and the architecture decision includes it rather than extrapolating from six Cells.

## Corrected evidence

Representative real-application complete-backend medians are:

| workload | A0 Python | R1 Rust1 | D0 Rust4 | D1 read2+Rust1 | D2 read2+Rust4 |
|---|---:|---:|---:|---:|---:|
| Time 6 Cells | 586.0 ms | 601.5 | 617.6 | 606.9 | 591.8 |
| Time 10 Cells | 1,051.9 | 998.0 | 1,035.8 | 993.6 | 996.9 |
| Time 11 Cells | 1,096.7 | 1,143.0 | 1,117.4 | 1,098.7 | 1,107.8 |
| capacity 6 Cells | 746.2 | 751.6 | 739.4 | 721.2 | 726.3 |
| dQ/dV 6 Cells | 2,028.1 | 823.0 | 814.9 | 828.6 | 802.3 |
| dQ/dV 10 Cells | 3,291.9 | 1,325.3 | 1,313.6 | 1,318.9 | 1,288.1 |

For derivative workloads, resident sequential Rust is materially useful, but four-way Rayon and read concurrency add less than the 5% complete-boundary promotion threshold. For ordinary Time/Capacity, the native candidates are neutral-to-slower overall; the current Python/NumPy path remains the correct execution basis.

The corrected real 6-Cell derivative decomposition also shows why the marginal Rayon/read gains disappear at the request boundary: about 310-316 ms remains in ordinary pre-native transforms, about 100 ms in native-buffer materialization, ~73-86 ms in the Rust boundary, and ~142-147 ms in downstream Python assembly, in addition to the read stage.

## User-directed priority for the next child

The user has explicitly clarified that **derivatives are not a priority**. The primary product objective is ordinary Time/Capacity responsiveness, with an aspirational target of about **200 ms user-visible interaction latency** for representative warm multi-Cell work.

Therefore the 050.11 outcome **C — sequential Rust kernel** is accepted only as the answer to the execution-strategy composition experiment: it describes the best measured derivative-native option. It does **not** determine the next production priority.

The next child must focus on ordinary Time/Capacity and treat the corrected A0 medians above as the baseline. It should profile and reduce the dominant ordinary-path costs — data reuse/access, transform preparation, per-Cell work, downsampling/response projection, serialization and frontend/Plotly completion — rather than implementing the derivative Rust path merely because Rust won the derivative experiment.

## Merge/readiness

050.11 is **review-clean**. The branch is **not yet ready to merge** because Spec 050 continues with a new user-directed 050.12 child focused on ordinary Time/Capacity latency. No open 050.11 findings remain.
