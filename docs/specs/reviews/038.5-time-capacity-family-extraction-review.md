# Review 038.5 — Time/Capacity family extraction

Specification: [`../038.5-time-capacity-family-extraction.md`](../038.5-time-capacity-family-extraction.md)  
Parent: [`../038-analyses-feature-modularization.md`](../038-analyses-feature-modularization.md)  
Branch: `feature/analyses-feature-modularization`  
Merge base: `main` at `b6452a4d6691ef1f9b6acf3e353a6f05a5873ed7`  
Implementation commit: `dbeb78fb0360e9eca6ebb64b9d5a80abc5138689`  
Status: **Review clean — ready to proceed to 038.6**

## Confirmed

The reviewed implementation is the active 038.5 Time/Capacity extraction and changes only the frontend ownership boundary plus status/coordination documentation. No backend, migration, persistent schema, cache-version, build-configuration, or test-configuration file changed in the implementation diff.

`frontend/src/features/analyses/editor/families/time-capacity/TimeCapacityPlotCard.tsx` is now the single Time/Capacity family owner for:

- Time/Capacity configuration/defaults and normalization;
- coordinate construction and segmentation helpers;
- current-axis quantity/availability handling;
- canonical Time/Capacity trace and layout builders;
- Time/Capacity settings;
- live query, compute-token/job polling, progress/readiness, and plot card;
- style-modal preview and ordinary image/data export.

The implementation keeps this child cohesive in one production family file rather than introducing query/traces/layout submodules.

## Query and scientific-result contract

The live family preserves the locked request/cache contract:

- query key remains `["time-capacity", analysisId, dataSignature]`;
- endpoint remains `POST /api/analyses/{analysisId}/time-capacity`;
- payload remains the full `spec`, `job_token`, `viewport_width: 1200`, `precision: "standard"`, and `compact: true`;
- viewport width remains fixed at 1200 in the scientific cache identity;
- the data signature continues to include selection, protocol segments/filter, hidden protocol-segment IDs, cycle selection/range, point limit, X axis, time unit, display mode, electrode area, and derivative settings only for ICA/DVA;
- purely client-side stacked/current-axis presentation remains outside the scientific result identity;
- `staleTime` and `gcTime` remain 30 minutes;
- previous data remains the placeholder during refresh;
- live compute is enabled only with at least one selected entry;
- background-job polling remains keyed by `background-job-token`, polling running/null state at 300 ms with delayed token clearing;
- `onReadyChange` continues to track loading/fetching readiness.

The extracted builders continue to consume backend `TimeCapacityResult` / `TimeCapacityTrace` as authoritative. I found no new frontend scientific recomputation path and no backend/API/result-shape change.

## Builder and consumer reuse

The implementation exports `timeCapacityTracesForResult` and `timeCapacityLayout` as the canonical family builders. The move is ownership-oriented: the pre-existing page implementation is removed and the same logic is now exported from the family module.

`AnalysisPage.tsx` imports the family surface rather than retaining duplicate Time/Capacity settings/query/trace/layout/card code. Existing page-owned orchestration that intentionally remains for later children reuses the canonical builders:

- saved Time/Capacity preview uses `timeCapacityTracesForResult` plus `timeCapacityLayout`;
- portable Time/Capacity snapshot generation uses the same builders after its existing compact backend request;
- editor normalization imports `DEFAULT_TIME_CAPACITY` / `timeCapacityConfig`;
- live editor settings/card import `TimeCapacitySettings` / `TimeCapacityPlotCard`.

The implementation diff shows the old `tracesForTimeCapacity` page call sites changed to the exported canonical builder rather than introducing a second renderer.

## Parent/guidance boundary

The result matches the parent ownership model and one-way dependency direction: Time/Capacity now lives under `features/analyses/editor/families/time-capacity/`, while saved-artifact, portable, and editor-controller orchestration remain in the page for their later sequential children.

The reviewed change is a behavior-preserving frontend refactor. I found no user-facing redesign, route change, persistent analysis-spec version change, `CALC_VERSION` change, saved-artifact renderer/signature change, new dependency, or unrelated production-module split.

## Verification record

### Implementer-reported

The 038.5 implementation/handoff reports:

- focused frontend suite: `81/81` passed;
- `npx.cmd tsc --noEmit`: passed;
- `npx.cmd vite build`: passed (`7514` modules transformed; existing non-blocking warnings only);
- structural page-owner / canonical-builder / live-query searches: passed;
- `git diff --check`: passed;
- `python scripts\preflight.py`: `PREFLIGHT PASSED`, `5/5` stages, including `58` backend test modules, frontend policy tests, type check, and production bundle;
- browser/manual matrix: **not run**.

### Reviewer-independent

This review used ChatGPT + the GitHub connector only, not Work. I independently inspected the live branch coordination state, parent and active child specification, `AGENTS.md`/architecture guidance, the implementation commit diff and changed-file boundary, the extracted family query contract, canonical builder ownership, and current `AnalysisPage.tsx` imports/consumer reuse.

I did **not** independently execute Node, Python, TypeScript, Vite, preflight, grep, export, portable-report, or browser checks. The command results above are implementer-reported rather than reviewer-executed verification.

## Findings

No actionable 038.5 defect, specification deviation, regression risk, or missing required recorded verification was found.

The extensive Time/Capacity manual matrix remains unrun. This is not a finding because the child explicitly requires those checks to be recorded as run or not run, and the implementation record accurately records them as not run.

## Decision

**038.5 is review-clean. 038.6 may begin.**

The shared feature branch is **not ready to merge to `main`** because 038.6–038.8 and the parent integration review remain sequentially gated.
