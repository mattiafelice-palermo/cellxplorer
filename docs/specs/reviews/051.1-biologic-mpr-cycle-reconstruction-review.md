# Review — Spec 051.1: BioLogic MPR logical-cycle reconstruction

Status: **Reopened — browser acceptance required**  
Ready to merge: **No**  
Spec: [`../051.1-biologic-mpr-cycle-reconstruction.md`](../051.1-biologic-mpr-cycle-reconstruction.md)  
Parent: [`../051-biologic-mpr-extensible-column-registry.md`](../051-biologic-mpr-extensible-column-registry.md)  
Branch: `feature/biologic-mpr-extensible-columns-051`  
Main / merge base: `706dc0f14880202a8c5e22b35020502bcf3b4dc9`  
051.1 reviewer handoff: `e588a6ed6413c4a4d8dd8663661ab7737a281342`  
Initial implementation handoff: `43a726c5c620324d50fc4918969e4a06af244b62`  
Returned-fix handoff: `eff841a4ee9aa34c29e8cb6ccf09f62af42e4087`

## Review status

R1-R4 remain resolved at the code/source-trace level. The user subsequently performed another real browser acceptance pass and the voltage-vs-capacity view still renders as a visibly scrambled trace when Capacity is selected on the X axis with Consecutive display mode. The previous review therefore closed too early without browser verification of the actual user-facing plot.

The user has now explicitly approved browser verification for this issue. The prior constraint recorded in earlier handoffs as `browser checks: NOT RUN per user instruction` no longer applies to R5. The implementer must use the browser on the real application and user Cell before returning this review.

The source-level BioLogic evidence from `eff841a4` remains useful: Cell 135 contains a 456-row one-cycle source followed by a 49,308-row, 1,629-cycle repeating source; raw ID-211/ID-7 values agree with canonical capacities and `calc.per_cycle(...)` at early/middle/late points. R5 is therefore specifically a browser/rendering acceptance gate and must distinguish a user-facing display-coordinate defect from a BioLogic scientific-data defect.

## Verification already recorded

### Implementer-reported at `eff841a4`

- `tests.test_biologic_gcpl`: **PASS (58)**;
- `tests.test_biologic_closure`: **PASS (22)**;
- canonical preflight: **PASS**;
- real user EGG adapter/cycle/capacity source trace: **PASS**;
- browser checks: **NOT RUN**.

### Reviewer-independent

The reviewer inspected the returned fixes, cumulative branch scope, cycle reconstruction, capacity ownership, cache provenance, and generic Time/Capacity display-coordinate code. The reviewer did not execute the private source or browser. The existing implementation of `_time_capacity_display_x(...)` was already identified as suspicious for `display_mode=consecutive` with a capacity X axis because phase/cycle capacity resets are not concatenated.

## Resolved findings

- **R1 — High: execution-evidenced loop reconstruction contract** — RESOLVED in `eff841a4`; the bounded rule is now explicit in the spec and regression-covered.
- **R2 — Medium: cache-hit cycle provenance** — RESOLVED in `eff841a4`; exact adapter provenance is persisted/recovered instead of inferred from cycle count.
- **R3 — Low: stale repository guidance** — RESOLVED in `eff841a4`.
- **R4 — High: real EGG cycle/capacity scientific credibility** — RESOLVED at source/canonical/calc level by the read-only real-file trace in `eff841a4`.

### R5 — High: Real browser voltage-vs-capacity acceptance still fails

Affected files / evidence path:

- `backend/app/services/analysis_engine.py` and/or the current Time/Capacity display-coordinate owner after inspection;
- relevant Time/Capacity worker/cache modules if they contribute the display X coordinate;
- frontend Time/Capacity plotting code only if the browser/API trace proves the defect is frontend-owned;
- focused Time/Capacity tests;
- the user's local Cell 135 and one known Neware control cell for browser verification.

**Current**

The user retested the current branch in the real application after the `eff841a4` source-level validation. With Time/Capacity set to Voltage vs Capacity and display mode `Consecutive`, the trace is still visibly scrambled rather than forming a coherent capacity progression. The browser result therefore contradicts the previous practical acceptance conclusion even though the BioLogic source/canonical/cycle capacities themselves were traced successfully.

Previous static inspection already identified a likely generic cause: `_time_capacity_display_x(...)` subtracts the first finite capacity value for `consecutive`, while the phase-capacity coordinate resets at cycle/phase boundaries. The implementer also reported reproducing backward display-X transitions on the EGG data and stated that this behavior predates Spec 050 and is reproducible outside BioLogic. That diagnosis now needs to be confirmed in the actual UI with browser evidence before the branch can be considered accepted.

**Target**

Use browser verification to trace the real user-facing failure end-to-end. Determine whether the malformed plot is caused by generic Time/Capacity consecutive-capacity display coordinates, BioLogic/stitching data, frontend rendering, or another layer. Do not rely only on dataframe/API inspection; verify the actual rendered plot.

The user explicitly authorizes browser testing for this finding. If the browser confirms that the defect is the previously diagnosed generic Time/Capacity consecutive-capacity behavior, report that clearly with browser/API evidence. Do not silently introduce unrelated generic feature work into 051.1 unless the reviewer/user explicitly authorizes that scope after the diagnosis. If the defect is BioLogic-specific or caused by the 051/051.1 changes, fix it in this branch and re-run browser acceptance.

**Acceptance criteria**

- Browser verification is **RUN**, not `NOT RUN`, and the handoff explicitly states that it was performed with user approval.
- Open the actual Cell 135 Analysis page and reproduce the user's Voltage vs Capacity view using the same `Consecutive` mode shown in the retest.
- In the browser, compare at minimum:
  - Voltage vs Time + Consecutive;
  - Voltage vs Capacity + Consecutive;
  - Voltage vs Capacity in the other available display mode(s) that reset/overlap cycles.
- Inspect the request/response or plotted series data sufficiently to identify whether X coordinates are already wrong before rendering or become wrong in the frontend.
- Repeat the Consecutive + Capacity check on a known Neware cell. This is required to distinguish a generic Time/Capacity defect from a BioLogic-specific regression.
- Record screenshots or concise browser observations in the handoff; do not commit private database/source bytes.
- If BioLogic-specific: fix the owning layer, add focused regression coverage, run canonical preflight, and demonstrate the corrected real EGG plot in the browser.
- If generic/pre-existing: identify the exact owning function/component and the smallest correct follow-up scope; do not claim the visible problem is resolved merely because source/canonical capacities are correct.
- Do not mark 051 merge-ready until this browser acceptance result has been reviewed.

## Current conclusion

**Changes required / acceptance incomplete.** R1-R4 remain resolved. R5 is the active blocker. The user has explicitly approved browser verification for R5.