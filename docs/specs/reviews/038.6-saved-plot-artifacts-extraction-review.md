# Review 038.6 — Saved-plot artifacts extraction

Specification: [`../038.6-saved-plot-artifacts-extraction.md`](../038.6-saved-plot-artifacts-extraction.md)  
Parent: [`../038-analyses-feature-modularization.md`](../038-analyses-feature-modularization.md)  
Branch: `feature/analyses-feature-modularization`  
Merge base: `main` at `b6452a4d6691ef1f9b6acf3e353a6f05a5873ed7`  
Implementation commit: `c17be850c7cd8a647f05026b0b0f226f96a28b74`  
Handoff checkpoint reviewed: `fd4d34d549a8a2476604bbff63f0f62f2e0a9b32`  
Status: **Review clean — ready to proceed to 038.7**

## Confirmed

The reviewed checkpoint is isolated to the 038.6 saved-plot artifact ownership move plus implementation-owned documentation/path updates. The implementation adds or moves exactly the six locked artifact modules under `frontend/src/features/analyses/editor/artifacts/`:

- `DraftPlotCard.tsx`;
- `SavedPlotsPanel.tsx`;
- `SavedPlotPreviews.tsx`;
- `AnalysisCacheWarmupRenderer.tsx`;
- `CacheWarmupCoordinator.tsx`;
- `warmupCompletion.ts`.

`AnalysisPage.tsx` now imports the saved-panel, cached-preview, artifact type, and portable snapshot builder from those owners instead of retaining the extracted implementations. The moved `CacheWarmupCoordinator` imports `AnalysisCacheWarmupRenderer` from the artifact feature rather than importing a route page.

## Artifact and draft contract

The extracted preview pipeline retains the session-only draft guard at every server-artifact boundary. Draft preview IDs use React Query memory only for lookup, and `storePlotArtifactWithRetry` returns the locally generated artifact without posting a draft ID to the saved-artifact endpoint.

The saved-preview dispatch remains family-specific and reuses the canonical builders established by earlier 038 children:

- Cycles: `cycleTracesForResult` / `cyclePlotLayout`;
- Time/Capacity: `timeCapacityTracesForResult` / `timeCapacityLayout`;
- Steps: `stepsTracesForResult` / `stepsLayoutForSpec`;
- DCIR: `dcirTracesForResult` / `dcirLayoutForSpec`;
- Chargeability: `chargeabilityTracesForResult` / `chargeabilityLayoutForSpec`;
- Rate Capability: `rateCapabilityTracesForResult` / `rateCapabilityLayoutForSpec`.

I found no duplicate scientific trace/layout implementation introduced by the extraction.

## Thumbnail, artifact, and warmup equivalence

The reviewed move preserves the relevant pre-extraction contracts:

- saved-row thumbnail generation remains `480 × 288` with the same raster treatment;
- hover preview remains a true 4:3 relayout (`640 × 480` SVG to `480 × 360` raster);
- Plotly image generation remains serialized through the same module-level promise queue;
- saved-artifact persistence retains the same retry sequence `[0, 800, 1600, 2600]` and 404-only retry behavior;
- warmup thumbnail/artifact lookups retain their existing query keys, stale times, enabled gates, and one retry;
- visible and warmup preview computation remains gated behind thumbnail/artifact misses;
- warmup-generated thumbnails are not published to React Query before the authorized backend artifact write succeeds;
- `resolveWarmup` remains the terminal-state resolver, and `CacheWarmupCoordinator` retains the five-minute watchdog plus per-task idempotent completion guard;
- terminal completion still invalidates background jobs, cache inventory, and Analysis Database thumbnails;
- the manual `cellxplorer:warmup-now` event, idle/hidden admission policy, one-task-at-a-time behavior, and pause/resume flow are unchanged.

This matches the current `state-and-performance.md` requirement that prepared markers are hints, that missing thumbnails/artifacts trigger regeneration, and that warmup must not publish a generated thumbnail or complete the task before the authorized store succeeds.

## Page/editor reconnection

The extracted `SavedPlotsPanel` keeps the prior draft-card plus saved-list composition and receives editor lifecycle state through props/callbacks. The page still owns active saved-plot state, dirty/session state, save/delete/open callbacks, autosave/persistence, tab switching, and preview-readiness gating.

The `draft={null}` wiring in the current page is not a regression: the pre-extraction page passed the same `null` draft to `TabDraftPlotCard` for this live-unsaved workspace path. The extraction preserves that behavior rather than inventing a new draft store.

Portable snapshot construction was moved with the artifact helpers as required by 038.6. The page still supplies the same in-memory artifact lookup callback from React Query, and the builder keeps family-specific backend requests and canonical trace/layout reuse. Portable estimate/source-preflight/share orchestration remains page-owned for 038.7.

## Scope and versioning

The focused implementation diff contains no backend, migration, persistent schema, scientific calculation, result-schema, cache-version, `CALC_VERSION`, artifact renderer-version, endpoint-payload, build-configuration, or dependency change. The maintained path documentation was updated to the new artifact owners.

## Verification record

### Implementer-reported

The implementation/handoff reports:

- `frontend/tests/warmupCompletion.test.ts`: **10/10 passed**;
- `analysisPlotPolicy.test.ts` + `analysisVisibility.test.ts`: **19/19 passed**;
- `python -m unittest tests.test_app_channels -v`: **17/17 passed**;
- TypeScript `npx.cmd tsc --noEmit`: passed;
- Vite production build: passed, **7517 modules transformed**;
- `git diff --check`: passed;
- `python scripts\preflight.py`: **PREFLIGHT PASSED, 5/5 stages**, including **459 frontend tests** and **58 backend test modules**;
- manual/browser checklist: **NOT RUN**.

### Reviewer-independent

This review used ChatGPT + the GitHub connector only. I independently inspected:

- the live 038.6 coordination handoff;
- the active child specification and current repository guidance;
- the focused `1d573a1e...` → `c17be850...` implementation diff;
- all six target artifact owners and current page imports/reconnection;
- draft artifact guards;
- family builder dispatch;
- thumbnail geometry and serialized generation;
- artifact retry semantics;
- warmup completion/watchdog behavior;
- current state/performance invariants;
- pre- versus post-extraction wiring for the live draft-panel path and selected artifact helpers.

I did **not** independently execute Node, Python, TypeScript, Vite, preflight, browser, GPU/Plotly export, or packaged-app checks. Implementer-reported command results are therefore not reviewer-executed verification.

## Findings

No actionable 038.6 defect, specification deviation, regression risk, branch contamination, or missing required recorded verification was found.

The extensive manual matrix remains unrun. This is not a finding because 038.6 requires the implementation record to state whether those checks were run, and it accurately records **NOT RUN**.

## Decision

**038.6 is review-clean. 038.7 may begin.**

The shared Spec 038 branch is **not ready to merge to `main`** because 038.7, 038.8, and the final parent integration review remain sequentially gated.
