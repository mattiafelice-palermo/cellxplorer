# Review 038.4 — Cycles family extraction

Specification: [`../038.4-cycles-family-extraction.md`](../038.4-cycles-family-extraction.md)  
Parent: [`../038-analyses-feature-modularization.md`](../038-analyses-feature-modularization.md)  
Branch: `feature/analyses-feature-modularization`  
Merge base: `main` at `b6452a4d6691ef1f9b6acf3e353a6f05a5873ed7`  
Implementation commit: `de1b31e1987bd532e2bd30c070f6e6a14f03fd21`  
Coordination handoff checkpoint: `555c0fe7b2d914acb811f29f88dffe96f2e4dddb`  
Status: **Review clean — ready to proceed to 038.5**

## Confirmed

The implementation is one focused commit ahead of the 038.4 reviewer handoff and changes only the 038.4 extraction boundary plus status documentation.

`frontend/src/features/analyses/editor/families/cycles/CyclePlotCard.tsx` is now the single Cycles family owner for:

- quantity and legacy-key resolution;
- mass-normalization eligibility;
- Cycles compute signature;
- `useCyclesResult` query/job polling;
- diagnostic-cycle filtering/reindexing adapter;
- canonical cycle traces;
- canonical cycle layout;
- Cycles settings;
- live Cycles plot/card;
- style preview and ordinary image/data export.

`diagnosticCycles.ts` remains the focused sibling policy module established by 038.3.

## Query ownership

`useCyclesResult` preserves the pre-extraction live-query contract:

- query key: `["compute", analysisId, computeSignature(spec)]`;
- endpoint: `POST /api/analyses/{analysisId}/compute`;
- payload still includes the full `spec` plus `job_token`;
- compute token generation and delayed token clearing remain intact;
- `staleTime` remains five minutes;
- background-job polling remains keyed by `background-job-token`, uses the same endpoint, and polls running/null state at 300 ms;
- the page passes the same initial-compute readiness plus exclusion of Time/Capacity, Steps, DCIR, Chargeability, and Rate Capability tabs;
- the page continues to retain the last valid result in `rendered` while refresh is pending.

No duplicate generic live compute query remains in `AnalysisPage.tsx`.

## Builder/presentation preservation

I compared the extracted `cycleTracesForResult` and `cyclePlotLayout` boundaries with the pre-038.4 page-owned `tracesForResult` and `cyclePlotLayout` blocks. The ownership-oriented rename/export preserves the existing builder contracts and presentation inputs.

The family continues to own the existing behavior for:

- individual Cell lines and replicate aggregation/bands;
- CE overlay and series-style resolution;
- low-N behavior;
- diagnostic hide/reindex policy;
- source-boundary and provenance columns;
- Plotly layout/style/legend/zoom/runtime behavior;
- delayed progress, empty/error states, and export mechanics.

The implementation does not introduce a generic figure compiler or split the Cycles family into extra production modules.

## Consumer reuse

`AnalysisPage.tsx` imports the canonical Cycles family surface rather than retaining local copies.

Current page-owned orchestration that intentionally remains for later children uses those same builders:

- saved Cycles preview calls `cycleTracesForResult` and `cyclePlotLayout`;
- portable Cycles snapshot generation calls the same builders after its existing compute request;
- editor normalization uses `normalizeLegacyCycleQuantityKey` from the family;
- live settings/card use the extracted `CycleSettings` / `CyclePlotCard`;
- Recap/naming consumes the shared query/result and family quantity label.

The page contains no remaining `function CycleSettings`, page-owned Cycles trace/layout/card implementation, or duplicate generic live query owner.

## API/cache/scientific boundary

No backend file, API shape, query-key meaning, result schema, database migration, persistent analysis-spec format, `CALC_VERSION`, saved-artifact renderer version, or invalidation policy changed.

The extraction continues to treat backend `ComputeResult` values as authoritative; no per-cycle scientific formula or aggregation logic moved into a new frontend source of truth.

## Verification record

### Implementer-reported

The 038.4 implementation/handoff reports:

- focused frontend suite: `90/90` passed;
- `npx.cmd tsc --noEmit`: passed;
- `npx.cmd vite build`: passed (`7513` modules transformed; existing non-blocking warnings only);
- structural Cycles-owner / builder / generic-query searches: passed;
- `git diff --check`: passed;
- `python scripts\preflight.py`: `PREFLIGHT PASSED`, `5/5` stages, including `459` frontend tests and `58` backend test modules plus type check and production bundle;
- initial sandboxed Vite/preflight path hit the documented Windows access-denied environment issue; the elevated canonical rerun passed;
- browser/manual matrix: **not run**.

### Reviewer-independent

This review used ChatGPT + the GitHub connector only, not Work. I independently inspected the active child and coordination state, isolated implementation diff, extracted Cycles family query and core builders, pre-extraction page source, current page query enablement/last-valid-result behavior, and saved/portable builder call sites.

I did **not** independently execute Node, Python, TypeScript, Vite, preflight, grep, export, or browser checks. The command results above are implementer-reported.

## Findings

No actionable 038.4 defect, specification deviation, regression risk, or missing required verification was found.

The extensive Cycles manual matrix remains unrun. This is not a finding because 038.4 requires those checks to be recorded as run or not run, and the implementation record accurately records them as not run.

## Decision

**038.4 is review-clean. 038.5 may begin.**

The shared feature branch is **not ready to merge to `main`** because 038.5–038.8 remain part of the parent feature and still require sequential implementation/review gates.