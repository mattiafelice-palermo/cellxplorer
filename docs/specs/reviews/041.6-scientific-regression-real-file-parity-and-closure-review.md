# Review 041.6 — Scientific regression, real-file parity and feature closure

**Branch:** `feature/biologic-mpr-gcpl-support`  
**Merge base:** `aca39740039b4d7146afc9104f5c471bff7c7c46`  
**Returned-fix handoff head reviewed:** `cafd383233a7d33a39e1434079f366acdc7c1e94`  
**Final R11 implementation commit:** `f5ee8f4d1ebba84ed4ffa964f969d0e7120a8384`  
**Status:** **CHILD REVIEW CLEAN — entering cumulative Parent 041 review**

This round is reviewer-only static inspection through the GitHub connector. The reviewer did **not** run tests, preflight, builds, packaged-app checks, browser checks, or private-file/MPR-MPT parity, and did not edit implementation code.

The child implementation findings are now resolved. Parent 041 still has the locked external scientific closure gate: no privacy-approved same-experiment `.mpr`/`.mpt` pair is available, so real semantic parity remains **NOT RUN**. The child may therefore enter the required cumulative parent review, but this child result does **not** declare Parent 041 `FEATURE_COMPLETE` or ready to merge.

## Finding status

### R1 — RESOLVED

The unsupported implementation-authored parity waiver is gone and the original paired-file closure requirement is restored.

### R2 — RESOLVED

The invented charge/discharge execution-pair cycle heuristic was removed. The verified production layout is metadata-only unless an independently decoded full-cycle field is available, and unvalidated half-cycle progression fails closed.

### R3 — RESOLVED

A successful no-cache preflight was subsequently reported after the scientific rollback; exact-SHA provenance from that round was reconciled by R5.

### R4 — RESOLVED

Persisted `bm:gcpl3:r1` canonical registrations are retired through the bounded relational capability path, including online/offline sources and pinned analyses. Old gcpl3 caches cannot remain live scientific truth.

### R5 — RESOLVED

The earlier contradictory preflight records were reconciled. `01c7a73` is retained as historical exact-head evidence for the earlier R1-R4 no-cache run.

### R6 — RESOLVED

Metadata-only continuation acknowledgement is bound to immutable inspected content identity. A hash change produces a new confirmation ID; harmless reorder of the same hash can preserve acknowledgement. The final continued-cell payload derives `allow_metadata_only` from the currently acknowledged server finding, and the real initial multi-source `/api/imports/cells` path is covered.

### R7 — RESOLVED

Raw data is capability-gated rather than extension-gated for continuation sources, so metadata-only/non-canonical sources do not expose a guaranteed-failing action.

### R8 — RESOLVED

The production reader accepts only the independently observed 16-ID/53-byte MPR layout. The synthetic-only 15-ID/49-byte Ece-omitted variant is rejected as unsupported.

### R9 — RESOLVED

The post-R8 BioLogic adapter identity is `gcpl5` (`bm:gcpl5:r1`). Persisted pre-R8 gcpl4 rows are reconciled through bounded persisted-header evidence: the observed layout is promoted to current metadata-only identity, while withdrawn/insufficient layouts remain fail-closed and require reinspection. No unrelated parser identity or `CALC_VERSION` change is introduced.

### R10 — RESOLVED

The 041.6 implementation record distinguishes exact verification checkpoints correctly:

- `7f39d3f2edf74fbe4a4a7ec6b4536d88c15e0a0a` is the R6/R9 code checkpoint with the reported 1,145-test backend run and normal cached preflight behavior;
- `01c7a73` remains explicitly historical no-cache evidence rather than being presented as current;
- later R11 checkpoints and their verification are separately identified.

### R11 — RESOLVED

The acceptance-time import message is now capability-safe without pretending capability proves a cache job exists:

- pure metadata-only submissions state that no cycling-data preparation is queued;
- canonical-only submissions state that cycling data **will be prepared in the background if needed**;
- mixed submissions apply the same conditional wording specifically to canonical sources.

This preserves truthful messaging for already-cache-ready canonical sources while later actual-running cache UI remains free to use definitive job-state wording. The focused policy tests were updated to cover metadata-only, canonical-only and mixed conditional semantics.

## External scientific closure gate

The 041.6 implementation review is clean, but Parent 041 cannot be marked complete under the current locked spec while paired `.mpr`/`.mpt` ground truth is absent. The verified real MPR layout remains metadata-only, so the required registered-source real-format scientific matrix and semantic parity remain blocked rather than synthetically satisfied.

Do not weaken this requirement or reinterpret array-level `raw_cycle_index` fixtures as equivalent external ground truth without an explicit user decision.

## Verification record

### Implementer reported

Final R11 checkpoint: `f5ee8f4d1ebba84ed4ffa964f969d0e7120a8384`.

- Focused frontend import-progress suite: PASS — 20 tests.
- `python scripts\preflight.py` on `f5ee8f4`: reported PASS — 5/5; all 68 backend modules, 541 frontend policy tests, TypeScript, and Vite production bundle passed.
- Paired MPR/MPT semantic parity: **NOT RUN**; no `.mpt` available.
- Packaged smoke and live browser/manual matrix: NOT RUN.

Earlier exact verification remains attributable in the implementation record:

- `7f39d3f2edf74fbe4a4a7ec6b4536d88c15e0a0a`: reported 1,145 backend tests and normal cached preflight for R6/R9.
- `01c7a73`: historical earlier exact no-cache checkpoint.

### Reviewer independently inspected

- Remote handoff head `cafd383233a7d33a39e1434079f366acdc7c1e94`.
- Exact R11 implementation patch `f5ee8f4d1ebba84ed4ffa964f969d0e7120a8384`.
- Conditional canonical-only and mixed import success wording.
- Focused policy-test expectations for metadata-only, canonical-only and mixed cases.
- Existing backend registration behavior showing canonical capability does not necessarily imply a new cache job.
- Current workflow requirement that a clean final child enters `FINAL_REVIEW` rather than `COMPLETE`.

### Reviewer did NOT execute

- Any Python/frontend test command.
- Preflight, TypeScript, Vite, or build command.
- Packaged Windows smoke.
- Browser/manual matrix.
- Private MPR parse in this review round.
- MPR/MPT semantic parity.

## Decision

**CHILD REVIEW CLEAN — enter the fresh cumulative Parent 041 review.** R1-R11 are resolved at the child implementation-review level.

This is not a merge-readiness decision. Parent 041 remains subject to its full cumulative requirements, including the still-missing paired MPR/MPT scientific ground truth.