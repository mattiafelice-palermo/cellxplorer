# Review 041.6 — Scientific regression, real-file parity and feature closure

**Branch:** `feature/biologic-mpr-gcpl-support`  
**Merge base:** `aca39740039b4d7146afc9104f5c471bff7c7c46`  
**Returned-fix handoff head reviewed:** `08634901a863f26cd3b8170f35e6908cb0231736`  
**R6/R9 implementation commit:** `7f39d3f2edf74fbe4a4a7ec6b4536d88c15e0a0a`  
**Status:** **CHANGES REQUIRED / SCIENTIFIC CLOSURE BLOCKED — not ready to merge**

This round is reviewer-only static inspection through the GitHub connector. The reviewer did **not** run tests, preflight, builds, packaged-app checks, browser checks, or private-file/MPR-MPT parity, and did not edit implementation code.

Parent 041 still has the locked external scientific closure gate: no privacy-approved same-experiment `.mpr`/`.mpt` pair is available, so real semantic parity remains **NOT RUN** and Parent 041 cannot enter final scientific closure or `FEATURE_COMPLETE`. Synthetic fixtures remain regression evidence only.

## Returned finding status

### R1 — RESOLVED

The unsupported implementation-authored parity waiver is gone and the original paired-file closure requirement is restored.

### R2 — RESOLVED

`gcpl4` removed the invented charge/discharge execution-pair cycle heuristic. The verified production layout is metadata-only unless an independently decoded full-cycle field is available, and unvalidated half-cycle progression fails closed.

### R3 — RESOLVED

A successful no-cache preflight was subsequently reported after the scientific rollback; exact-SHA provenance from that round was reconciled by R5.

### R4 — RESOLVED

Persisted `bm:gcpl3:r1` canonical registrations are retired through the bounded relational capability path, including online/offline sources and pinned analyses. Old gcpl3 caches cannot remain live scientific truth.

### R5 — RESOLVED

The earlier contradictory preflight records were reconciled. `01c7a73` remains valid historical evidence for the R1-R4 exact-head no-cache preflight; later code changes now require their own checkpoint record, tracked separately as R10 rather than reopening R5.

### R6 — RESOLVED

Metadata-only continuation acknowledgement is now bound to immutable inspected content identity. The backend uses the source hash for the `metadata_only_source` finding identity while retaining the staged key separately for request-local payload binding. A hash change therefore produces a new confirmation ID, while harmless reorder of the same hash can preserve acknowledgement.

The final continued-cell payload derives `allow_metadata_only` from the currently acknowledged server finding, not the unrelated separate-cell checkbox. Coverage now includes the hash-change/reinspection case and the real initial multi-source `/api/imports/cells` path: an unacknowledged mixed-source Cell is rejected, while an acknowledged metadata-only MPR registers successfully without an MPR cycling-cache job.

### R7 — RESOLVED

`ContinuationSourceList` gates the Raw data action through capability state (`metadata_only` / `canonical_cycling`) rather than extension. Canonical-capable sources retain the action; metadata-only/non-canonical sources do not.

### R8 — RESOLVED

The production MPR reader accepts only the independently observed 16-ID/53-byte layout. The synthetic-only 15-ID/49-byte Ece-omitted variant is rejected as unsupported, and durable documentation distinguishes future two-electrode semantics from currently verified binary evidence.

### R9 — RESOLVED

The post-R8 BioLogic adapter identity is now `gcpl5`, giving current MPR sources effective identity `bm:gcpl5:r1`. Persisted pre-R8 `bm:gcpl4:r1` rows are handled by a bounded, no-source-I/O startup reconciliation path using persisted data-header evidence only:

- exact observed 16-ID/53-byte evidence is promoted to the current metadata-only identity, including while the original file is offline;
- withdrawn 15-ID/49-byte or insufficient evidence remains fail-closed as metadata-only with `requires_reinspection`, without gaining canonical scientific availability;
- unrelated Neware/Excel identities are not part of the reconciliation query;
- the normal publication path also refuses to publish stale pre-R8 MPR capability as current;
- no `CALC_VERSION` change was introduced.

Focused regressions cover the verified and withdrawn persisted gcpl4 states and the updated parser identity.

## New findings

### R10 — Medium: the 041.6 implementation/verification record still presents an obsolete code checkpoint as current

Affected files:
- `docs/specs/041.6-scientific-regression-real-file-parity-and-closure.md`
- `docs/specs/041-agent-coordination.md` and this review only as needed to keep verification provenance aligned

**Current**

The 041.6 implementation record already describes the R6 and R9 behavior, but its verification bullets still say the full backend suite passed **1,141 tests** and that `01c7a73` is the exact committed implementation checkpoint, followed by the statement that later implementation-record/coordination commits are documentation/workflow-only.

That is no longer true after the R6/R9 implementation at `7f39d3f`: the latest handoff reports **1,145 backend tests** and a later canonical preflight for that code tranche. `01c7a73` remains useful historical evidence for the earlier R1-R4 no-cache preflight, but it cannot be presented as the current implementation checkpoint after subsequent production code and test changes.

**Target**

Keep the historical `01c7a73` evidence explicitly historical, then add the exact current R6/R9 implementation checkpoint and its actual reported verification. Preserve the distinction between stages that the latest preflight reran and stages it legitimately skipped through the preflight cache.

**Acceptance criteria**

- `7f39d3f2edf74fbe4a4a7ec6b4536d88c15e0a0a` is identified as the current R6/R9 implementation checkpoint.
- The latest reported full-backend count is recorded as 1,145, without overwriting the historical 1,141-test record where that history is useful.
- The latest `python scripts\preflight.py` result is recorded truthfully, including any TypeScript/Vite stages skipped as unchanged rather than implying `--no-cache` coverage at `7f39d3f`.
- The statement that all commits after `01c7a73` are documentation/workflow-only is removed or scoped only to commits for which it is actually true.
- The implementation record, coordination handoff and canonical review allow a reader to identify which exact SHA each verification result covers.
- Do not rerun checks merely to rewrite history if the actual tested checkpoint/results are already known.

### R11 — Low: pure metadata-only import success copy falsely says cycling-data preparation is running

Affected files:
- `frontend/src/pages/InboxPage.tsx`
- the existing import-progress/policy test owner, or a focused frontend regression test

**Current**

The import mutation's `onSuccess` notification is unconditional:

```text
<cells> accepted. Registration is being committed; cycling data preparation continues in the background.
```

Current BioLogic MPR registration is deliberately metadata-only and 041.4 requires it to queue **no cycling-cache job**. Therefore an all-metadata-only import can receive a success message claiming background scientific preparation that does not exist. The later footer is capability/job-state driven, but the initial success notification is not.

**Target**

Make the accepted/import-progress copy truthful from generic source capability/job state. A pure metadata-only submission must not claim cycling preparation. A submission that actually has canonical-capable source work may retain the existing background-preparation wording. Do not add an MPR-specific scientific branch merely to change copy.

**Acceptance criteria**

- Pure metadata-only import success does not say cycling/scientific data preparation is continuing.
- Canonical-capable import with actual cache preparation retains truthful background-preparation feedback.
- Mixed submissions use truthful generic wording/state without assuming every accepted source has a cycling job.
- Add focused frontend coverage for the metadata-only and cycle-capable message/policy cases.

## External scientific closure gate

Even after R10/R11 are fixed, 041.6 cannot be marked review-clean under the current locked spec while paired `.mpr`/`.mpt` ground truth is absent. The verified real MPR layout remains metadata-only, so the required registered-source real-format scientific matrix and semantic parity remain blocked rather than synthetically satisfied.

Do not weaken this requirement or reinterpret array-level `raw_cycle_index` fixtures as equivalent external ground truth without an explicit user decision.

## Verification record

### Implementer reported on the R6/R9 handoff

Implementation checkpoint: `7f39d3f2edf74fbe4a4a7ec6b4536d88c15e0a0a`.

- Focused backend R6/R9/import/MPR checks: PASS — 158 tests.
- Focused parser/format/MPR checks: PASS — 107 tests.
- Frontend continuation checks: PASS — 14 tests.
- Full backend suite: PASS — 1,145 tests; golden digests reported unchanged.
- `python -m compileall -q backend tests`: PASS.
- `git diff --check`: PASS.
- `python scripts\preflight.py`: reported PASS — 5/5; frontend type-check and production-bundle stages were reported as correctly skipped as unchanged by the preflight cache rather than rerun.
- Paired MPR/MPT parity: **NOT RUN**; no `.mpt` available.
- Packaged smoke and live browser/manual matrix: NOT RUN.

Historical verification retained for provenance: `01c7a73` was the exact earlier R1-R4 implementation checkpoint reported to pass `python scripts\preflight.py --no-cache` 5/5 with 1,141 backend tests. That historical run is not verification of later R6/R9 code.

### Reviewer independently inspected

- Remote handoff head `08634901a863f26cd3b8170f35e6908cb0231736`, implementation checkpoint `7f39d3f2edf74fbe4a4a7ec6b4536d88c15e0a0a`, and unchanged merge base `main@aca39740039b4d7146afc9104f5c471bff7c7c46`.
- R6 server finding-ID construction, hash-based acknowledgement invalidation, frontend acknowledgement preservation, final continued-cell payload serialization, and initial multi-source import-flow regression.
- R9 `gcpl5` identity, persisted-layout classifier, startup reconciliation query, verified/withdrawn/unknown fail-closed behavior, publication guards and focused closure tests.
- Current 16-ID/53-byte binary-evidence documentation and canonical-cycling knowledge text.
- 041.6 implementation/verification record versus the latest coordination handoff.
- Import mutation success copy and the 041.4 no-cycling-job metadata-only lifecycle requirement.

### Reviewer did NOT execute

- Any Python/frontend test command.
- Preflight, TypeScript, Vite, or build command.
- Packaged Windows smoke.
- Browser/manual matrix.
- Private MPR parse in this review round.
- MPR/MPT semantic parity.

## Decision

**CHANGES REQUIRED — return R10 and R11.** R1-R9 are resolved at the code/review level. The branch is **not ready to merge**.

After R10/R11 are corrected, perform another reviewer-only re-review. Independently of those fixes, Parent 041 remains scientifically blocked until the required paired MPR/MPT evidence and resulting registered-source end-to-end scientific closure matrix are available or the user explicitly changes the locked requirement.