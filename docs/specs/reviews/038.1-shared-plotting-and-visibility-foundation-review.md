# Review 038.1 — Shared plotting and visibility foundation

Specification: [`../038.1-shared-plotting-and-visibility-foundation.md`](../038.1-shared-plotting-and-visibility-foundation.md)  
Parent: [`../038-analyses-feature-modularization.md`](../038-analyses-feature-modularization.md)  
Branch: `feature/analyses-feature-modularization`  
Merge base: `main` at `b6452a4d6691ef1f9b6acf3e353a6f05a5873ed7`  
Original implementation: `e8ee40ffb8d6d386c6b3bd72421b33226fc102ff`  
First review checkpoint: `b8a2fb39eb24b88b0ac646e64bf72bf44147a7b7`  
R1/R2 follow-up: `af5d0a5eba46352a3c39829b14494cfe9f4dbcc3`  
Second review checkpoint: `4673737f30ae58894a313fa73f93775189abd7be`  
Final reviewed integration checkpoint: `a1c7868b1cdb4d55f4ceb31bcfd88b9e055329fc`  
Status: **Review clean — 038.1 complete**

## Final review summary

The final 038.1 state satisfies the child specification and the parent ownership contract. The shared plotting/visibility foundation is in its locked location, the family-card dependency on `pages/AnalysisPage` is removed, the context-aware visibility policy has one owner, and no backend, API, persistent schema, cache identity, artifact version, or scientific calculation change was introduced.

The later 038.2 implementation is already present on the shared branch under explicit user direction. This final pass therefore reviews the 038.1-owned files at the integrated checkpoint rather than requiring the branch to contain only 038.1.

## Resolved findings

### R1 — Resolved: CSV UTF-8 BOM regression

The extracted CSV formatter now prefixes the generated text with the unambiguous runtime value `"\uFEFF"`. `frontend/tests/plotExport.test.ts` verifies:

- the first code point is U+FEFF;
- UTF-8 bytes begin `EF BB BF`;
- visible `ï»¿` is absent;
- the rounded sample row is unchanged.

### R2 — Resolved: stale 038/038.1 status metadata

The parent/child/index were updated to record the actual implementation checkpoint and review state rather than claiming that no child had been implemented.

### R3 — Resolved: extra `plotCsv.ts` violated the locked plotting tree

The extra production module was removed. `DataColumn`, decimal-place selection, and `buildDelimitedText` are again owned by the locked `frontend/src/features/analyses/editor/plotting/plotExport.ts` module. The plotting directory therefore returns to the 13-file target owned by 038.1.

The BOM regression test imports `buildDelimitedText` directly from the production `plotExport.ts` owner.

### R4 — Resolved: follow-up verification/preflight was not recorded

The final R3/R4 implementation record reports:

- frontend suite: `459/459` passed;
- `npx.cmd tsc --noEmit`: passed;
- `npx.cmd vite build`: passed;
- `python scripts\preflight.py`: passed, `5/5` stages;
- preflight included `459` frontend tests, `58` backend test modules, frontend type check, and production bundle;
- browser/manual matrix: **not run**.

The final integration checkpoint recorded for those results is `a1c7868b1cdb4d55f4ceb31bcfd88b9e055329fc`.

## Reviewer-independent verification

This review used ChatGPT + the GitHub connector only, not Work. I independently inspected the live branch, current `plotExport.ts`, the focused CSV regression test, the final R3/R4 implementation record, and the branch scope around the review checkpoints.

I did **not** independently execute Node, TypeScript, Vite, preflight, or browser tests. The command results above are implementer-reported.

## Manual verification

The six-family/style/export/zoom/visibility/light-dark browser matrix remains unrun by both implementer and reviewer. This does not block 038.1 because the child explicitly permits the manual matrix to be recorded as not run.

## Decision

**038.1 is review-clean and complete.**

No 038.1 follow-up remains open. The shared feature branch is **not ready to merge to `main`** because children 038.2–038.8 are part of the same parent feature and must complete their own review gates.