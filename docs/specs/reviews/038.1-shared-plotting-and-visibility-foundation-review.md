# Review 038.1 — Shared plotting and visibility foundation

Specification: [`../038.1-shared-plotting-and-visibility-foundation.md`](../038.1-shared-plotting-and-visibility-foundation.md)  
Parent: [`../038-analyses-feature-modularization.md`](../038-analyses-feature-modularization.md)  
Branch: `feature/analyses-feature-modularization`  
Merge base: `main` at `b6452a4d6691ef1f9b6acf3e353a6f05a5873ed7`  
Reviewed implementation: `e8ee40ffb8d6d386c6b3bd72421b33226fc102ff`  
First review checkpoint: `b8a2fb39eb24b88b0ac646e64bf72bf44147a7b7`
Reviewed follow-up: `af5d0a5eba46352a3c39829b14494cfe9f4dbcc3`
Status: **Changes required — do not begin 038.2**

## Second-pass summary

The follow-up is exactly one commit ahead of the first review checkpoint and remains scoped to 038.1. It changes the parent/child/index status metadata, the 038.1 review record, CSV export code, and one focused frontend test. No 038.2 implementation, backend, API, migration, cache-version, artifact-version, query-key, or persistent-spec implementation change is present.

R1 and R2 from the first review are substantively resolved:

- **R1 resolved:** CSV text now prefixes `"\uFEFF"`; the focused regression test checks U+FEFF, UTF-8 bytes `EF BB BF`, absence of visible `ï»¿`, and the unchanged rounded sample row.
- **R2 resolved:** the parent spec, child spec, and spec index now identify 038 as in progress, identify 038.1 as implemented with review follow-ups, record implementation commit `e8ee40ffb8d6d386c6b3bd72421b33226fc102ff`, and keep 038.2 blocked pending a clean review.

The follow-up nevertheless introduces one new ownership/spec deviation and leaves required follow-up verification unrecorded, so 038.1 is not yet review-clean.

## Verification record

### Implementer-reported original checkpoint

For implementation `e8ee40ffb8d6d386c6b3bd72421b33226fc102ff`, the implementation record reports:

- focused frontend policy suite: `142` passed, `0` failed;
- `npx.cmd tsc --noEmit`: passed;
- `npx.cmd vite build`: passed (`7511` modules transformed);
- `python scripts\preflight.py`: `PREFLIGHT PASSED`, `5/5`, including `458` tests with `0` failures and the production bundle;
- browser/manual matrix: not run.

### Implementer-reported R1/R2 follow-up

For follow-up `af5d0a5eba46352a3c39829b14494cfe9f4dbcc3`, the review-fix record reports:

- focused suite including the CSV regression: `143/143` passed;
- `npx.cmd tsc --noEmit`: passed;
- `npx.cmd vite build`: passed (`7512` modules transformed);
- browser/manual matrix: not run;
- final preflight: **not recorded**.

### Reviewer-independent

This second pass used ChatGPT + the GitHub connector only, not Work. I independently compared `b8a2fb39...` to `af5d0a5e...`, inspected the live follow-up commit, current parent/child/index/review metadata, `plotCsv.ts`, `plotExport.ts`, the new CSV regression test, and current repository guidance.

I did not independently execute Node, TypeScript, Vite, preflight, or browser tests. Implementer command results remain reported rather than re-run.

## Resolved findings

### R1 — Resolved: CSV UTF-8 BOM regression

The runtime CSV prefix is now represented unambiguously as `"\uFEFF"`, and the focused test verifies both the Unicode code point and UTF-8 `EF BB BF` bytes. The previously reviewed visible `ï»¿` prefix is no longer used by the generated CSV helper.

### R2 — Resolved: stale 038/038.1 repository status

The parent, child, and spec index now accurately describe 038.1 as implemented but still review-gated, and the original implementation SHA is recorded explicitly. 038.2 remains blocked.

## Follow-ups

### R3 — Medium: R1 fix adds a production plotting file outside the locked target tree

**Affected files**

- `frontend/src/features/analyses/editor/plotting/plotCsv.ts`
- `frontend/src/features/analyses/editor/plotting/plotExport.ts`
- `docs/specs/038-analyses-feature-modularization.md`
- `docs/specs/038.1-shared-plotting-and-visibility-foundation.md`

**Current**

The R1 fix extracted `DataColumn`, `exportDecimalPlaces`, and `buildDelimitedText` into a new production module, `plotCsv.ts`, and made `plotExport.ts` import/re-export that helper.

That is clean code in isolation, but it contradicts two locked ownership decisions of Spec 038:

1. the parent says the completed feature **must** have the locked target tree and explicitly counts 49 target entries; its plotting directory contains 13 files and does not contain `plotCsv.ts`;
2. child 038.1 explicitly assigns CSV/XLSX text/workbook construction to `plotExport.ts` and locks the same 13 plotting target files.

The purpose of 038 is the final ownership/file organization itself, so silently adding a 14th plotting module is not merely cosmetic scope drift.

**Target**

Keep the BOM regression fix and focused regression coverage without silently changing the locked production ownership tree.

Preferred resolution: return the CSV formatter/precision helper ownership to `plotExport.ts` and remove `plotCsv.ts`, while retaining the unambiguous `"\uFEFF"` runtime behavior and a regression test that exercises the production behavior.

If there is a demonstrated technical reason that the formatter must remain a separate production module, stop and obtain an explicit parent-spec amendment before proceeding; do not silently change the 49-entry target architecture in a child review fix.

**Acceptance criteria**

- the production plotting directory again matches the 13-file locked 038/038.1 target, **or** an explicit approved parent amendment adds the new owner and updates the target-entry count/ownership text;
- generated CSV still begins with exactly one U+FEFF / UTF-8 `EF BB BF` BOM;
- visible `ï»¿` does not appear in the first header;
- delimiter, decimal separator, precision, provenance columns, column order, XLSX behavior, filenames, and other export behavior remain unchanged;
- focused regression coverage still protects the BOM failure mode;
- no unrelated export refactor is introduced.

### R4 — Medium: follow-up checkpoint was pushed without a recorded required preflight result

**Affected files**

- `docs/specs/reviews/038.1-shared-plotting-and-visibility-foundation-review.md`
- the final 038.1 implementation/review handoff record

**Current**

The pushed R1/R2 record reports the focused suite, TypeScript check, and Vite build, but says final preflight will be recorded later. The pushed follow-up commit is now known (`af5d0a5eba46352a3c39829b14494cfe9f4dbcc3`), yet no follow-up `python scripts\preflight.py` result is recorded.

Current `AGENTS.md` requires a completed spec or review-follow-up batch to run preflight before it is committed/pushed. Feature-branch pushes do not provide a GitHub CI substitute.

**Target**

After R3 is resolved, run the current canonical 038.1 focused checks plus `python scripts\preflight.py` on the resulting follow-up checkpoint and record exact results. Do not infer success from the original implementation checkpoint's preflight.

**Acceptance criteria**

- the new follow-up commit SHA is recorded explicitly;
- focused CSV/visibility/policy verification required by 038.1 passes and exact counts are recorded;
- `npx.cmd tsc --noEmit` and `npx.cmd vite build` pass and are recorded;
- `python scripts\preflight.py` is run on the final review-fix checkpoint and its exact result is recorded;
- any environment limitation or failure is reported rather than described as passed;
- manual browser checks remain accurately marked as run or not run.

## Follow-up order

1. R3 — restore the locked plotting ownership tree while preserving the BOM fix/test, unless the user explicitly approves a parent amendment.
2. R4 — run and record the required verification/preflight on the resulting checkpoint.
3. Update this same review file with the R3/R4 implementation record and pushed SHA.
4. Request the next review pass. 038.2 remains blocked until this file is marked review-clean.

## Unverified manual behavior

The browser/manual matrix remains unrun by both implementer and reviewer:

- all six family tabs render;
- style panel and per-series modal interaction;
- PNG/SVG/PDF and CSV/XLSX actions end to end;
- zoom persistence;
- direct/replicate visibility interactions;
- light/dark chrome with light Plotly surfaces.

This missing manual run is not a separate finding because the child explicitly permits the checks to be recorded as not run. The R1/R2 findings are addressed below and await independent second-pass confirmation.

## R1/R2 implementation record

- **R1:** Restored the CSV formatter's prefix to `"\\uFEFF"` through the pure
  `frontend/src/features/analyses/editor/plotting/plotCsv.ts` helper, re-exported it from
  `plotExport.ts`, and added `frontend/tests/plotExport.test.ts`. The regression checks U+FEFF,
  UTF-8 bytes `EF BB BF`, the absence of visible `ï»¿`, and the unchanged rounded data row.
- **R2:** Updated the parent, child, and spec-index status metadata to identify 038.1 as
  implemented with changes required, record implementation commit
  `e8ee40ffb8d6d386c6b3bd72421b33226fc102ff`, and keep 038.2 blocked pending a clean review.
- **Verification:** The focused suite including the new CSV test passes `143/143`; `npx.cmd tsc
  --noEmit` passes; `npx.cmd vite build` passes with `7512` modules transformed. Final preflight
  will be recorded with the pushed follow-up checkpoint.
- **Manual checks:** The browser interaction matrix remains unrun, as in the original
  implementation record.
- **Follow-up commit:** To be recorded by the handoff after this review-fix checkpoint is pushed.

The follow-up is ready for independent second-pass review. Do not begin 038.2 until that review
confirms R1/R2 and changes the status to review-clean.

The user explicitly directed the implementing agent to continue with 038.2 without waiting for
that independent second pass. The review remains marked as second-review-required and is not
being represented as clean; the 038.2 implementation record documents this deliberate override.
This is not a separate finding because 038.1 permits the manual matrix to be recorded as not run.

The user explicitly directed continuation with 038.2 without waiting for this review gate. This
review remains accurately marked as changes required; no review-clean status is being inferred.

## R3/R4 implementation record

- **R3:** Removed the extra production `plotCsv.ts` module and moved `DataColumn`, precision
  selection, and BOM-preserving delimited text construction back into the locked
  `plotExport.ts` owner. The regression test now imports the production formatter directly.
  Runtime-only download and Plotly dependencies are loaded lazily so the pure export boundary
  remains directly testable without changing export output.
- **R4:** Re-ran the current frontend suite (`459/459`), `npx.cmd tsc --noEmit`, and Vite build
  after R3. `python scripts\preflight.py` then passed with `5/5` stages completed successfully,
  including `459` frontend tests, `58` backend test modules, the frontend type check, and the
  production bundle. The final integration checkpoint SHA will be recorded with the merge/push
  handoff.
- **Manual checks:** The browser matrix remains unrun.
- **Final integration checkpoint:** `a1c7868b1cdb4d55f4ceb31bcfd88b9e055329fc` (includes the
  remote second-review documentation merge and the R3/R4 follow-up record).
- **Review state:** R3/R4 are implemented for this checkpoint, but the user explicitly directed
  continuation with 038.2; this record does not claim that the review is independently clean.
