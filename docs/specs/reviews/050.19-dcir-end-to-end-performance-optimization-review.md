# Review — Spec 050.19: DCIR end-to-end performance optimization

Status: **Changes required**  
Spec: [`../050.19-dcir-end-to-end-performance-optimization.md`](../050.19-dcir-end-to-end-performance-optimization.md)  
Branch: `feature/runtime-performance-optimization`  
Initial implementation: `0fcb8cc84d92a186982465b5b3d410a2258cc1b3`  
R1 fix: `3f0df93b8c42fb66af7bf1bdd2ae967cd4fcc095`  
Merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`

## Round 2 summary

The core R1 duplication is fixed. `compute_dcir()` now creates one request-local prepared source representation per required source and reuses it across configured series. `prepare_dcir_frame()` owns source normalization, run boundaries, compact run metadata and adjacent-pair indexing; `per_occurrence()` performs target-specific pair selection and result calculation. The multi-series regression confirms that normalization/run preparation executes once while two DCIR series reuse the prepared source.

The implementer reports five-repetition scientific/order parity, exact-hit bypass, 27 focused tests passing and canonical preflight passing. The repaired six-Cell route is **139.27 ms p50** profiled (**138.67 ms** unprofiled median) versus the locked **404.74 ms** baseline. Six-Cell exact-hit p50 is **14.54 ms**.

R1 is not closed yet because one explicit acceptance point remains unmet. The repaired profile reports `scientific_compute` at **128.99 ms inclusive** with a **36.47 ms direct parent residual**. That residual is about **26% of the 139.27 ms route**, above the child’s materiality threshold, but the evidence does not decompose it into the remaining direct `compute_dcir()` work.

The selective raw/stitch boundary is adequately justified for retention in this round: one selective stitched read per Cell, canonical-cache ownership, ordered provenance, safe full-raw fallback, and no demonstrated benefit for a broader persistent/raw-frame cache rewrite.

## Implementer-reported verification

- focused tests: PASS (27 tests; DCIR, profiler, fallback parity, golden projection)
- Python compilation and `git diff --check`: PASS
- preflight: PASS (4/4; all 80 backend test modules)
- browser/manual: NOT RUN per explicit user instruction
- 1-Cell miss: 36.05 ms p50 profiled
- 6-Cell miss: 139.27 ms p50 profiled
- 6-Cell exact hit: 14.54 ms p50
- source preparation: 27.99 ms / 6 calls
- occurrence extraction: 3.23 ms / 12 calls
- selective `stitch_raw_steps`: 54.38 ms inclusive p50
- raw materialization: 41.64 ms p50
- scientific compute direct residual: 36.47 ms
- SQL: 17 statements
- response: 32,168 bytes

## Reviewer verification

Repository/code inspection only. I did not independently run the profiler, tests, golden suite or preflight.

I inspected the R1 fix commit, `backend/app/services/dcir.py`, the DCIR section of `backend/app/services/analysis_engine.py`, the multi-series reuse test, the DCIR profiler changes, and the R1 follow-up evidence in the spec.

Confirmed from code:

- source normalization/run preparation now scales with distinct source frames, not source × series;
- request-local `PreparedDcirFrame` reuse preserves record-index gaps, cycle/time reset boundaries, timestamp-first duration semantics, finite-value handling, median absolute pulse current, charge/discharge formula direction and deterministic order;
- exact persisted hits remain above scientific compute;
- selective raw access and full-raw fallback remain intact.

## Findings

### R1 — Medium — Partially resolved: source reuse is fixed, but the material direct scientific-compute residual remains unprofiled

Affected files:

- `backend/app/services/analysis_engine.py`
- `scripts/profile_analysis_families.py`
- `tests/test_profile_analysis_families.py`
- `docs/specs/050.19-dcir-end-to-end-performance-optimization.md`

**Current**

The original per-series source-preparation duplication is resolved: source preparation is now 6 calls for six sources while target occurrence extraction remains 12 calls for two series per Cell.

However, the final profile still leaves **36.47 ms** as direct `scientific_compute` residual. Direct `compute_dcir()` work outside instrumented helpers still includes source-frame partition/copy, target/series traversal, occurrence concatenation/projection, relative-DCIR calculation, `measurement_meta` construction and response/source/provenance assembly. The current evidence does not show which of these accounts for the residual.

**Target**

Add bounded opt-in timers around the remaining direct `compute_dcir()` boundaries so the 36.47 ms residual is decomposed enough to apply the child’s whole-family materiality rule. This is a measurement requirement first, not a demand for another rewrite.

If every exposed child is below materiality or necessary/unsafe to change, record that and stop. If a child is ~5% of route or ~10 ms and has a credible low-risk optimization, implement it and re-profile.

**Acceptance criteria**

- preserve six source preparations for the six-Cell/two-series workload;
- decompose the 36.47 ms direct scientific-compute residual into measured children plus a reconciled remainder;
- no unexplained remainder above materiality without a concrete stop reason;
- preserve scientific digest/order parity, exact-hit bypass, selective/full-raw fallback parity, provenance, SQL boundedness and no original-source reads;
- rerun the representative six-Cell workload, focused checks and canonical preflight.

## Child conclusion

**Changes required: R1 remains open, narrowed to residual attribution.** The duplication fix is correct, but 050.19 cannot close while ~26% of the route remains an unexplained direct scientific-compute residual. Do not begin 050.20 yet.
