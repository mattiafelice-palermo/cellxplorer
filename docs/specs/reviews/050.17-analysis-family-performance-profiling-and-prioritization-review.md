# Review — Spec 050.17: analysis-family performance profiling and optimization prioritization

Status: **Review-clean**  
Spec: [`../050.17-analysis-family-performance-profiling-and-prioritization.md`](../050.17-analysis-family-performance-profiling-and-prioritization.md)  
Normative amendment: [`../amendments/050.17-five-family-profiling-scope.md`](../amendments/050.17-five-family-profiling-scope.md)  
Branch: `feature/runtime-performance-optimization`  
Initial implementation commit: `22b8012ea8b15e6769086a5a988fd70654eeb5a4`  
Review-fix commit: `71fc95f8f26f73b239ed587021164dc449ee71f4`  
Implementation parent: `06b1494ec19c61dbc950b2837bf767516c2dcae5`  
Merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`

## Review summary

050.17 is **review-clean** after the R1-R4 correction tranche.

The final authoritative benchmark covers only **Cycles, Steps, DCIR, Chargeability and Rate Capability**, per the user-directed amendment. Time/Capacity is reference-only and was not rerun for the fix tranche.

The corrected profiler now preserves an explicit parent/child timing hierarchy, uses only non-overlapping direct children when reconciling scientific residuals, retains bounded SQL evidence, and exercises the actual production-route harness in focused tests. The final matrix therefore supports downstream optimization planning without hiding unexplained time through nested-timer double counting.

Rate Capability is now sufficiently decomposed to justify a concrete first optimization: its roughly 1.27 s six-Cell miss is dominated by execution extraction, especially repeated phase-row filtering (~658 ms) and measurement filtering/grouping (~225 ms). DCIR, Chargeability and Steps also have attributable material costs. Cycles is intentionally **not** assigned a speculative optimization: its corrected six-Cell profile leaves an approximately 73.5 ms / 43% direct scientific residual, so its downstream child must close that attribution gap before changing production code.

No new scientific defect, cache-identity defect, migration/version issue or ordinary-route behavior regression was found in the review-fix delta.

## Implementer-reported verification

Review-fix handoff:

- R1 hierarchy-safe attribution tests: PASS
- R2 measured Rate deep decomposition and reconciliation tests: PASS
- R3 retained SQL evidence and cleanup tests: PASS
- R4 real five-family route, exact-hit, clone and patch-restoration tests: PASS
- focused profiler/Rate tests: PASS (21)
- golden analysis and approval checks: PASS (33 tests; all eight fixtures SAME)
- amended five-family matrix: PASS (three repetitions, exact-hit controls, parity/order/provenance, SQL evidence, Rate growth evidence; 33.32 s)
- canonical preflight: PASS (4/4 stages; 80 backend modules; frontend checks skipped by unchanged-cache policy)
- browser/manual checks: NOT RUN (user explicitly requested no browser)

## Reviewer verification

Code and evidence inspection only. This reviewer environment did **not** execute the profiler, focused tests, golden suite or canonical preflight independently.

I independently inspected:

- review-fix commit `71fc95f8f26f73b239ed587021164dc449ee71f4` against reviewer parent `5108f52bc0b8f60ad00280acee65c663f6152941`;
- `scripts/profile_analysis_families.py`, including timer parentage, direct-child reconciliation, residual selection, SQL retention, exact-hit controls and five-family default scope;
- `backend/app/services/rate_capability.py`, including measurement grouping, phase-row and cutoff instrumentation and execution-parent reconciliation;
- `tests/test_profile_analysis_families.py`, including real route/hit/clone/cleanup coverage;
- the updated 050.17 implementation record and five-family scope amendment;
- current repository performance/cache guidance.

GitHub reports no combined status checks and no pull-request workflow runs for the review-fix commit, so command results remain implementer-reported rather than independently executed.

## Final authoritative five-family evidence

| Family | 1-Cell miss p50 | 6-Cell miss p50 | exact-hit p50 | dominant stage | dominant p50/share | disposition |
| --- | ---: | ---: | ---: | --- | --- | --- |
| Cycles | 35.88 ms | 170.99 ms | 22.45 ms | unresolved scientific compute residual | 73.50 ms / 43.00% | profile deeper before optimization |
| Steps | 41.41 ms | 193.85 ms | 14.17 ms | `step_block_extraction` | 65.49 ms / 33.78% | actionable |
| DCIR | 72.39 ms | 404.74 ms | 14.97 ms | `dcir_occurrence_extraction` | 152.16 ms / 37.60% | actionable |
| Chargeability | 37.65 ms | 195.51 ms | 17.89 ms | `protocol_reconstruction` | 120.69 ms / 61.70% | actionable |
| Rate Capability | 219.61 ms | 1267.82 ms | 14.99 ms | `execution_extraction` | 1091.84 ms / 86.12% | actionable; highest priority |

The Rate deep profile further localizes the six-Cell execution parent approximately to:

- phase-row filtering: ~658 ms;
- measurement filtering/grouping: ~225 ms;
- remaining direct execution residual: ~90 ms;
- cutoff/capacity/current/rate work: measured and materially smaller;
- protocol reconstruction: not the dominant Rate bottleneck.

This is sufficient to begin optimization work without conflating raw access, protocol reconstruction and repeated DataFrame scanning.

## Confirmed correct

- The profiler calls real production routes on disposable golden-fixture state.
- Multi-Cell scale points use real relational Cell/SourceFile/Test/TestFile clones with distinct cache identities.
- Forced result-cache misses and exact persisted hits remain separate.
- Exact hits prove no family scientific compute/raw access occurs because profiling is enabled.
- The stage model preserves observed parent-child relationships and does not subtract inclusive descendants twice.
- A dominant direct scientific residual can remain unresolved and blocks a speculative recommendation, as demonstrated by Cycles.
- SQL statement count/timing/by-kind/header-load evidence is retained without SQL text or parameters.
- Rate Capability's profiling hook remains opt-in and does not alter ordinary cache identity or scientific output.
- Time/Capacity is excluded from the authoritative matrix/ranking.
- No migration, `CALC_VERSION`, parser identity, raw-layout version or result-schema change is introduced by 050.17.

## Findings

### R1 — High: dominant-stage selection can double-count overlapping nested timers and hide an unattributed scientific remainder — **Resolved**

**Resolution**

The profiler now retains parent/child relationships and inclusive/exclusive elapsed values. Scientific residual selection subtracts only non-overlapping direct children of `scientific_compute`. The corrected result leaves Cycles explicitly unresolved because its direct remainder is actually larger than any named direct child.

**Acceptance evidence**

- hierarchy-safe regression: implementer-reported PASS;
- Cycles corrected to ~73.50 ms / 43% unresolved residual;
- Steps/DCIR/Chargeability dominant stages recomputed from the corrected hierarchy.

### R2 — High: mandatory Rate Capability deep profile does not yet support the recommended execution-extraction optimization — **Resolved**

**Resolution**

`extract_pair_executions()` now exposes measured children for measurement filtering/grouping, phase-row filtering, cutoff validation, capacity extraction, current extraction and rate normalization. The execution parent is reconciled with a direct residual rather than double-counting those children.

The corrected six-Cell profile shows repeated DataFrame filtering is materially responsible for the route: phase-row filtering alone is ~658 ms and measurement filtering/grouping ~225 ms, versus a much smaller protocol-reconstruction contribution.

**Acceptance evidence**

- Rate deep stage/call/reconciliation tests: implementer-reported PASS;
- one-/six-Cell growth evidence recorded;
- final optimization recommendation now follows measured child costs rather than code inspection alone.

### R3 — Medium: SQL/query evidence is collected and then discarded — **Resolved**

**Resolution**

Request-local SQL profiling is persisted into each instrumented sample and summarized as bounded counts/timing/by-kind/header-load evidence. Query text and parameters are not retained.

**Acceptance evidence**

- SQL emission/cleanup test: implementer-reported PASS;
- final five-family matrix contains SQL evidence.

### R4 — Medium: focused tests do not exercise several profiler contracts explicitly required by 050.17 — **Resolved**

**Resolution**

Focused coverage now runs the real profiling harness for all five in-scope families, verifies a real exact hit bypasses family compute/raw reads, validates relational/cache-distinct clones, asserts Rate deep fields/reconciliation, and verifies patch/instrumentation restoration.

**Acceptance evidence**

- 21 focused profiler/Rate tests: implementer-reported PASS;
- golden/approval checks: implementer-reported PASS.

## Child conclusion

**050.17 is review-clean. R1-R4 are resolved and no finding remains open.**

The five-family benchmark is sufficiently reliable to authorize dedicated family optimization children. Per the user's explicit instruction, each downstream child must optimize the **whole set of meaningful measured opportunities for that family**, not only the single largest stage, with complete-route re-profiling after each retained tranche.

The resulting scheduled sequence is:

1. `050.18` Rate Capability — largest absolute and best-attributed opportunity;
2. `050.19` DCIR;
3. `050.20` Chargeability;
4. `050.21` Steps;
5. `050.22` Cycles — profiling closure first, then evidence-backed optimization.

Time/Capacity remains out of scope for those children.
