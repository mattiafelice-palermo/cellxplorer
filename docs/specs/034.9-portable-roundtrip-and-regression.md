# 034.9 — Portable round-trip and multi-source regression closure

**Status:** Plan
**Parent:** [034 — Multi-source cell continuations](034-multi-source-cell-continuations.md)
**Depends on:** 034.1–034.8
**Scope:** Portable analysis fidelity, synthetic regression corpus, documentation, and final matrix.

## Portable export/import hazards

- `backend/app/services/portable_analysis.py::_cell_document` already serializes source IDs in
  `TestFile.position` order.
- `_analysis_sources` currently deduplicates by source ID and returns database-ID order, which can
  differ from scientific Cell/Test/source order.
- Import recreates TestFile positions by enumerating each serialized `source_ids` list.
- Source preflight correctly checks originals, but the package and embedded-download order must be
  proven for multi-source Cells.

## Portable requirements

- Serialize Cells in the analysis selection’s deterministic order.
- Within each Cell, serialize Tests by `Test.id` and sources by `TestFile.position`.
- `_analysis_sources`, source package documents, embedded bytes, preflight rows, and downloadable
  originals use the same deterministic scientific order.
- Preserve distinct filenames, hashes, per-source metadata, and Test membership.
- Never flatten a continued Cell into one source or multiple Cells.
- Import recreates exactly the same source order and identifies the same tracked tail.
- A partially overlapping import—some hashes already linked elsewhere, some new—continues to fail
  atomically with a clear conflict.
- Reimport may reuse an existing Cell only when the complete ordered source hash hierarchy matches.
  Same hashes in a different order are not equivalent.
- Embedded ZIP layout remains one folder per Cell with each original source as a separate file.
- Portable HTML remains untrusted/checksummed and never executes imported JavaScript.

## Synthetic regression corpus

Create a small committed synthetic description/fixture set, not Neware binaries copied from the
user. Use patched parser/cache frames and temporary files to cover:

1. normal continuation: local `1…3` + local `1…2`;
2. missing local label: `1,2,4`;
3. incomplete final cycle in the first source;
4. incomplete final cycle in the last source;
5. multi-day timestamp gap;
6. timestamp overlap;
7. reversed user order;
8. changed device/channel;
9. changed protocol signature and numeric step IDs;
10. exact duplicate hash;
11. source already linked to another Cell;
12. missing middle source cache;
13. reorder changes tracked tail and global cycles;
14. scheduled monitor checks only the new tail;
15. portable round-trip retains exact hierarchy.

Place reusable JSON/dataframe builders under `tests/fixtures/` or a focused test-support module.
Do not add generated cache artifacts. If the AGENTS tree becomes misleading, update it.

Do not modify the approved Spec 015 golden corpus or regenerate its expected files merely to absorb
additive multi-source output. Single-source golden behavior should remain stable; any intentional
schema addition must be narrowly normalized/approved and explained.

## End-to-end automated assertions

- Initial import and later attach create identical scientific order for equivalent inputs.
- Compatibility findings and acknowledgement survive the write revalidation contract.
- Cycles and time/capacity output global/local provenance and exclude downtime.
- Reorder invalidates cache keys/artifacts/thumbnails and changes portable order.
- Detach cannot empty a Test and changes the tracked tail when applicable.
- Scheduled versus manual source scopes differ exactly as 034.6 specifies.
- Four protocol-derived families fail closed without background jobs.
- Portable source preflight checks every original, not only the tail.
- Export without embedded originals still preserves source descriptors/provenance.
- Export with originals downloads each file separately under its Cell folder.
- Imported portable analysis produces the same ordered hashes and supported-plot result projection.

## Durable documentation

Update:

- `docs/agent-knowledge/architecture.md` — virtual continuation identity and order;
- `docs/agent-knowledge/change-playbooks.md` — safely changing source chains;
- relevant scientific analysis documents — supported/guarded family matrix;
- `docs/portable-analysis-html.md` — ordered multi-source packaging/download behavior;
- `AGENTS.md` — tree and implementation invariants;
- this parent and every child — implementation/review links and actual verification.

Do not turn knowledge documents into a chronological log.

## Files expected to change

- `backend/app/services/portable_analysis.py`
- `tests/test_portable_analysis.py`
- focused synthetic fixture/support files under `tests/fixtures/` or `tests/`
- integration tests from earlier children where final gaps are found
- `docs/agent-knowledge/architecture.md`
- `docs/agent-knowledge/change-playbooks.md`
- relevant scientific analysis knowledge documents
- `docs/portable-analysis-html.md`
- `AGENTS.md`
- this parent and its children for final implementation/review records

Do not use this closure child for unrelated refactors or to rewrite working implementation from
earlier children.

## Acceptance

- Complete ordered source hierarchy survives portable export/import.
- The synthetic corpus covers every listed scientific/lifecycle edge case.
- All earlier child acceptance items have automated evidence or an explicitly unrun manual check.
- Durable documents explain the final invariants without depending on chat history.
- No private source, real user data root, generated cache, or temporary report is committed.

## Verification

Run focused commands from each child first, then:

```powershell
python -m unittest discover tests
node --test frontend\tests\*.test.ts
cd frontend
npx.cmd tsc --noEmit
npx.cmd vite build
cd ..
python scripts\check_versions.py
python scripts\preflight.py --no-cache
```

Record exact counts/results and any pre-existing failures. Do not weaken the golden suite.

Because the user has asked agents not to test in the browser unless explicitly requested, provide
but do not execute this manual checklist without renewed permission:

- import two sources as one Cell;
- inspect suggested order/findings and reverse/restore it;
- add a third continuation to an existing Cell;
- verify tracked-tail labels and scheduled/manual scope;
- open Cycles and Time / capacity, save plots, view thumbnails, export image/CSV/Excel;
- verify Steps/DCIR/Chargeability/C-rate explanatory guards;
- export portable HTML with originals, import into disposable data root, and compare order;
- detach/reorder in disposable data and verify affected-analysis messaging.

Never run this matrix against `%USERPROFILE%\.cellxplorer` or `.cellxplorer-beta`; use an isolated
`CELLXPLORER_DATA`.

## Completion criteria

- All 034.1–034.8 acceptance items are covered by automated tests or explicitly recorded manual
  checks.
- Portable round-trip is exact for an ordered multi-source Cell.
- Synthetic corpus covers the major continuation failure modes without private sources.
- Full preflight passes or every failure is proven pre-existing and reported.
- Every child review is clean, pushed, and merged sequentially.
- The parent status is updated from Plan to Implemented only after this closure spec passes review.
