# Spec 050.11: execution-strategy composition and decision

Status: **Implementation complete — awaiting review**

Parent: [`050-runtime-performance-optimization.md`](050-runtime-performance-optimization.md)  
Promoted from: **050.P6** (split from the original broad 050.P3)  
Branch: `feature/runtime-performance-optimization`  
Depends on: **050.9 and 050.10 review-clean**  
Correct merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`  
Review document: [`reviews/050.11-execution-strategy-composition-and-decision-review.md`](reviews/050.11-execution-strategy-composition-and-decision-review.md)

## Goal

Combine only the execution mechanisms that proved useful independently in 050.9/050.10, determine whether their gains actually compose, and choose the **production architecture** for a later implementation child.

This remains a benchmark/decision child. The winner must not quietly become production behavior here; a later 050.12+ child is required.

## Implementation record

`scripts/profile_time_capacity_composition.py` implements the composition matrix without changing
the production executor. It reuses the 050.9 owner-resolved immutable read jobs and deterministic
response assembly, then measures sequential indexed reads or a bounded two-thread read pool with
a resident 050.10 Rust worker configured for one or four Rayon workers. The Rust response replaces
only the measured derivative numeric function or ordinary display-coordinate function inside the
benchmark request; Python remains responsible for relational state, cache plans, transforms not
under test, provenance, result assembly and serialization.

The candidate labels are:

| candidate | boundary |
|---|---|
| A0 | current sequential Python backend |
| R1 | sequential indexed reads + resident one-worker Rust |
| D0 | sequential indexed reads + resident four-worker Rust/Rayon |
| D1 | two concurrent indexed reads + resident one-worker Rust |
| D2 | two concurrent indexed reads + resident four-worker Rust/Rayon |

Each Rust candidate keeps its process and Rayon pool resident across five measured repetitions;
spawn, ready-handshake and first pool initialization are recorded in the lifecycle block and are
outside measured warm interactions. D3 is recorded as `NOT RUN` because it would duplicate the
read boundary and require one native boundary per Cell rather than the accepted one-coarse-request
Rust boundary. Persistent Python processes are not justified by the 050.9/050.10 gates.

## Required inputs

Read the accepted 050.9/050.10 implementation and review records first.

050.9 supplies current sequential decomposition plus isolated read-concurrency and whole-Cell Python-thread results, including small-job regressions, I/O contention, CPU and memory.

050.10 supplies isolated sequential-Rust and Rayon results, Python/native boundary cost, cold/warm lifecycle and build feasibility.

Do not repeat losing mechanisms by default. This child is about interaction between survivors.

## Locked decisions

1. **Combine only individually useful mechanisms.** A losing read-thread or Rust experiment does not enter a hybrid merely for completeness.
2. **Preserve attribution.** Compare each measured hybrid with the improvement expected from its isolated parts.
3. **Control nested threading.** Explicitly bound Python/PyArrow/BLAS/Rayon worker counts.
4. **Preserve deterministic order and scientific output.** Completion order never becomes result order.
5. **Exact result-cache hits stay direct** and bypass all worker/native machinery.
6. **No live ORM/session sharing across execution boundaries.**
7. **No scientific algorithm change.**
8. **No whole-Cell RAM cache, speculative prefetch or new persistent cache layout.**
9. **Python processes are optional control only**, never mandatory.
10. **Frontend/Plotly/adaptive zoom remain separate.** Measure their remaining share if evidence exists; do not modify them.

## Composition matrix

Use the representative scenarios shared by 050.9/050.10, especially broad multi-Cell Time/Capacity and the 10-Cell workload when data permit.

### D0 — best isolated control

Carry forward the fastest accepted isolated strategy: sequential Python, bounded Python threads, sequential Rust or Rust/Rayon depending on prior evidence.

### D1 — concurrent reads + sequential Rust

Run only when read concurrency was independently useful in 050.9 and sequential Rust was independently useful in 050.10. Use the smallest useful bounded read count and the one-worker Rust kernel.

### D2 — concurrent reads + bounded Rust/Rayon

Run only when both read concurrency and Rayon were independently useful. Use the smallest worker counts that captured most of each isolated gain; do not automatically combine 4 Python threads with 4 Rayon threads.

### D3 — whole-Cell Python threads + internally parallel Rust

Run only when this is architecturally distinct from D2 and oversubscription can be controlled. Otherwise record `NOT RUN` with the reason instead of manufacturing a redundant row.

## Optional process control

Benchmark persistent Python processes only if all are true:

- 050.9 shows weak thread scaling;
- material Python/GIL-bound CPU work remains;
- 050.10 shows Rust is infeasible, inferior or does not cover that stage;
- potential gain is large enough to justify Windows process/memory complexity.

If justified:

- E1 — persistent 2-process pool;
- E2 — persistent 4-process pool.

Never spawn per plot/Cell. Workers receive compact immutable descriptors and open exact cache data themselves; do not pickle whole DataFrames or pass SQLAlchemy state.

## Workloads

Carry forward only scenarios that discriminate surviving strategies. Include at minimum:

- single-Cell control;
- 3/6-Cell broad Time-axis request;
- **10-Cell All/Time** when representative data permit;
- broad capacity-axis case if still material;
- derivative-heavy case if the Rust kernel targets derivatives;
- narrow/small case to expose dispatch/boundary overhead;
- exact persisted result-cache hit;
- one viewport-like narrow range for later comparison with 050.P4.

Do not make a measured 10-Cell claim by extrapolating six Cells.

## Attribution

For every D/E candidate report:

- baseline wall time;
- relevant isolated component wall times from 050.9/050.10;
- simple expected combined saving if the isolated gains were independent;
- actual combined wall time/saving;
- interaction classification: **additive, sub-additive, neutral or negative**;
- explanation supported by I/O, CPU, memory bandwidth, dispatch and native-thread evidence.

The expected arithmetic is descriptive only; gains need not add linearly.

## Measurements

Record for every executed candidate:

- total backend wall time;
- per-Cell wall time where meaningful;
- thread/process/native worker counts;
- dispatch/queue time;
- Parquet read/decode and structural read counters;
- transform/kernel/derivative time;
- Python/native boundary cost when applicable;
- assembly/serialization time;
- CPU/effective cores;
- peak RSS/temporary allocation estimate where measurable;
- relevant PyArrow/NumPy/BLAS/Rayon settings;
- cold vs warm lifecycle for reusable pools/native extension;
- scientific parity and canonical ordering.

Use at least five comparable warm repetitions where practical; report median plus range/tail and alternate candidate order where cache warming could bias results.

## End-to-end boundary

If an installed-app/browser run is available for the best backend candidate, report separately:

- backend total;
- HTTP/fetch/parse;
- frontend trace/layout preparation;
- Plotly completion;
- total interaction time;
- Plotly trace/point count.

This identifies whether backend execution remains the dominant opportunity. It does not authorize frontend changes.

## Scientific parity

Every candidate preserves the same Cells/sources/cycles/protocols, algorithms/thresholds, existing exact/tolerance rules, finite/non-finite placement where applicable, provenance/continuation mapping, deterministic Cell/series ordering and saved/export/result-cache semantics.

Any speedup that changes accepted science is rejected.

## Required final architecture outcome

Finish with exactly one primary outcome:

- **A — remain sequential Python**: concurrency/native options do not produce material stable net gain.
- **B — bounded Python threads**: read-only or whole-Cell threading captures most useful gain with lowest complexity.
- **C — sequential Rust kernel**: native execution is useful but cross-Cell native parallelism adds little.
- **D — bounded Rust/Rayon**: Rust helps and Rayon adds material stable gain without Python read concurrency.
- **E — hybrid concurrent reads + Rust/Rayon**: both isolated mechanisms help and their composition remains best.
- **F — persistent Python processes**: only under the explicit process gate and only if they beat simpler/native alternatives after startup/IPC/memory.
- **G — backend execution no longer dominant**: transport/frontend/Plotly/adaptive resolution is now the larger user-visible boundary.

For the chosen outcome record recommended bounded worker count(s), measured representative behavior, memory/build/lifecycle consequences, exact later production scope and explicit exclusions.

## Results and architecture decision

The final command was:

```text
python scripts/profile_time_capacity_composition.py --repetitions 5 --skip-build --output tmp/050.11-composition-full-final.json
```

It covered 20 workload rows: ten controlled golden-fixture rows and ten rows from the real
saved `Performance analysis` data, including 6-, 10- and 11-Cell All/Time requests, a broad
capacity request, derivative-heavy requests, and small/narrow controls. Each row used five warm
repetitions for A0, R1, D0, D1 and D2, for 500 candidate runs total. All 500 candidates passed
scientific parity and deterministic Cell ordering. The two exact result-cache controls both
reported miss-then-hit state, equal scientific payloads, and no raw-access path on the hit.

Representative complete-backend medians in milliseconds were:

| suite / workload | A0 Python | R1 seq Rust | D0 seq read + 4 Rayon | D1 read2 + Rust1 | D2 read2 + 4 Rayon | D2 vs D0 |
|---|---:|---:|---:|---:|---:|---:|
| golden / Time 6 | 531.2 | 781.4 | 776.2 | 777.4 | 765.7 | -1.35% |
| golden / Time 10 | 925.0 | 1,361.6 | 1,340.8 | 1,316.6 | 1,339.6 | -0.09% |
| golden / Time 11 | 1,019.7 | 1,485.8 | 1,480.6 | 1,472.2 | 1,458.3 | -1.50% |
| golden / capacity 6 | 695.9 | 1,047.2 | 1,040.9 | 1,034.7 | 1,054.6 | +1.32% |
| golden / dQ/dV 6 | 1,576.3 | 1,101.2 | 1,074.6 | 1,081.3 | 1,048.0 | -2.47% |
| application / Time 6 | 597.6 | 872.3 | 870.4 | 864.0 | 863.3 | -0.82% |
| application / Time 10 | 977.3 | 1,420.0 | 1,421.6 | 1,404.9 | 1,408.7 | -0.91% |
| application / Time 11 | 1,090.5 | 1,580.7 | 1,581.1 | 1,564.5 | 1,570.0 | -0.70% |
| application / capacity 6 | 748.7 | 1,142.5 | 1,132.9 | 1,117.4 | 1,142.7 | +0.87% |
| application / dQ/dV 6 | 2,008.8 | 1,213.2 | 1,192.8 | 1,189.4 | 1,180.8 | -1.01% |

The composition result is **C — sequential Rust kernel**. Across representative derivative rows,
R1 was 34.9% faster than A0, but D0 was only 2.05% faster than R1 at the complete backend
boundary. Read concurrency likewise remained below the 5% decision threshold: D1 was only 1.89%
faster than R1 on the derivative representative set, and D2 was only 0.79% faster than D0.
Normal Time/Capacity remained on the current Python path; representative R1 was 45.8% slower
than A0. Small controls remained below the 5% promotion threshold, with the cross-suite D2-vs-D0
median at +3.48% (slower). Every workload's D2 interaction was classified `neutral` under the
specified 5% materiality rule; no hybrid was classified as a stable additive win.

The arithmetic attribution is descriptive, not a claim that percentages add linearly. On the
real application 6-Cell dQ/dV row, the medians were:

| candidate | read wall | numeric-buffer preparation | Rust boundary | remaining Python backend | complete backend |
|---|---:|---:|---:|---:|---:|
| R1 | 154.5 ms | 299.5 ms | 82.1 ms | 617.8 ms | 1,213.2 ms |
| D0 | 156.6 ms | 295.1 ms | 72.2 ms | 611.0 ms | 1,192.8 ms |
| D1 | 131.6 ms | 299.0 ms | 83.3 ms | 614.1 ms | 1,189.4 ms |
| D2 | 133.8 ms | 302.0 ms | 72.0 ms | 620.2 ms | 1,180.8 ms |

The two-thread read pool reduced the read wall but did not reduce the composed request by a
material amount. Four Rayon workers reduced the isolated Rust boundary, but numeric-buffer
preparation, the remaining Python backend/assembly path and serialization dominated the complete
request. The D3 whole-Cell-threaded/native variant was not run because it would duplicate this
one-coarse-request boundary and make per-Cell native transfer/oversubscription a different
experiment. The optional persistent-Python-process gate was not met: the accepted Rust path
covers the material derivative work and 050.9 showed mixed or harmful whole-Cell threading.

Each candidate Rust worker stayed resident across its five warm repetitions. On the real
application 6-Cell dQ/dV workload, spawn-to-ready ranged from 10.5–14.9 ms and first pool
initialization was 0.09–0.18 ms, all outside measured warm interactions. The one- and four-worker
Rust pools had warm working sets of about 5.4/6.7 MB and process-lifetime peaks of about
39.6/41.6 MB respectively; the two-thread-read/four-worker candidate was about 6.9 MB warm and
41.6 MB peak. This lifecycle/memory cost is acceptable for a later resident service, but the
additional Rayon/read concurrency did not produce a material complete-boundary gain.

The later 050.12+ production scope is therefore narrowly bounded: a resident sequential Rust
kernel may be evaluated for broad derivative requests only, with explicit workload gating,
fallback to the current Python derivative path, deterministic output ordering, and a complete
scientific parity suite. Normal Time/Capacity remains Python/NumPy; read-thread dispatch,
whole-Cell Python threads, Rayon multi-worker composition, persistent Python processes, cache
layout changes, frontend/Plotly changes and adaptive zoom are explicitly excluded from this
child and from the selected architecture.

## Production follow-up rule

050.11 does not implement the winner. Author one focused **050.12+** implementation child after review-clean evidence selects the architecture. That child owns real lifecycle/fallback/packaging/tests rather than carrying all alternatives forward.

## Acceptance criteria

- Only useful isolated mechanisms are combined.
- Every hybrid has attribution back to 050.9/050.10.
- Nested threading/storage contention are measured/controlled.
- Optional processes run only if justified.
- Multi-Cell and small-job behavior are both considered.
- Scientific output/order/provenance remain unchanged.
- One explicit architecture outcome A-G is recorded.
- No production executor/native path, adaptive zoom or Plotly redesign is implemented here.

## Verification

Completed verification for this benchmark-only child:

- `python -m py_compile scripts\profile_time_capacity_composition.py`: PASS.
- `python scripts\profile_time_capacity_composition.py --repetitions 5 --skip-build --output tmp/050.11-composition-full-final.json`: PASS (20 workloads, 500 candidate runs, 0 parity failures, exact cache-hit controls PASS; golden fixture plus real saved Performance analysis data).
- Rust protocol/build checks are inherited from the accepted 050.10 boundary: Rust tests 3/3 and release build PASS; the composition run used the same locked executable and exact numeric output parity.
- Browser/manual checks: NOT RUN (backend benchmark/decision child; no frontend or production request change).
- `git diff --check`: PASS.
- `python scripts\preflight.py`: PASS (4/4; all 77 backend modules; frontend unchanged and skipped by the canonical cache).

The durable report is disposable and remains under `tmp/`; it is not staged as source evidence.
Before handoff, rerun the focused checks and canonical preflight after any implementation edit:

```text
git diff --check
python scripts\preflight.py
```

Record browser/manual evidence only if actually run.

## Out of scope

- production integration of the winner;
- scientific formula changes;
- new persistent cache/data layout;
- whole-Cell RAM cache/prefetch;
- adaptive zoom / 050.P4;
- derivative Plotly trace consolidation;
- progressive streaming / 050.P2;
- cross-family indexed detail / 050.P7;
- migrations or scientific-version changes.
