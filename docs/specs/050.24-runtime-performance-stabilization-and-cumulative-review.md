# Spec 050.24: runtime performance stabilization and cumulative review

Status: **Scheduled — implement after 050.23 is review-clean**  
Type: **runtime stabilization / user-usage regression closure / cumulative integration review**  
Branch: `feature/runtime-performance-optimization`  
Parent: [`050-runtime-performance-optimization.md`](050-runtime-performance-optimization.md)  
Review document: [`reviews/050.24-runtime-performance-stabilization-and-cumulative-review-review.md`](reviews/050.24-runtime-performance-stabilization-and-cumulative-review-review.md)  
Depends on: **050.23 review-clean**  
Merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`

## 1. Goal

Spec 050 has accumulated a large amount of performance work across query/cache ownership, indexed Parquet access, Time/Capacity transforms and worker execution, adaptive Time/Capacity refinement, cross-family profiling, five family-specific serial optimizations, and shared persistent-worker promotion.

050.24 is the final **stabilization and cumulative-review child** before Parent 050 can close. Its purpose is not to invent another optimization programme. It exists to catch concrete regressions that emerge while the user exercises the integrated branch, fix any material cross-child integration defect found by cumulative review, and then perform the final whole-branch verification required for merge readiness.

This child is intentionally open-ended in the narrow sense that every real user-usage regression cannot be enumerated before the app is exercised. It is **not** an open feature bucket. New work belongs here only when it is plausibly caused, exposed, or made material by Spec 050 or is necessary to close the cumulative Spec 050 review.

The first locked stabilization issue is already known: Time/Capacity adaptive refinement currently flashes back to the coarse overview when the user zooms further from an already-refined view.

## 2. Review and closure threshold

Use a pragmatic closure threshold. Do not force additional implementation/review rounds for minor test-shape preferences, profiler polish, or theoretical micro-optimizations when code inspection plus existing verification gives strong confidence and there is no concrete user/runtime defect.

A finding should block 050.24 or Parent 050 closure when it can plausibly affect one or more of:

- scientific values or result meaning;
- provenance, parser/calc identity, cache identity or deterministic ordering;
- data integrity, migration safety or source ownership;
- exact-hit correctness or stale-result behavior;
- user-visible selection/visibility semantics;
- crashes, hangs, lost work, broken export, or persistent UI state;
- a material performance regression in an ordinary supported workflow;
- the validity of a performance claim that determined production architecture.

Pure verification-strength improvements or low-value micro-optimizations should be recorded as non-blocking observations unless a real regression reproduces. This child should reduce risk, not create review churn for its own sake.

## 3. Locked decisions

1. 050.24 is the final numeric stabilization child for the current Spec 050 branch unless the user explicitly schedules another child.
2. Do not add unrelated product features.
3. Do not reopen already-accepted scientific algorithms merely because a different implementation could be cleaner.
4. Do not rerun broad historical benchmark matrices unless a current defect can invalidate their production decision.
5. Targeted remeasurement is required only when a fix changes a performance-critical path or a cumulative-review finding makes an existing conclusion suspect.
6. Preserve the local-first SQLite + Parquet architecture, source/parser/calc identity, deterministic ordering, export fidelity, and existing result-cache semantics.
7. No relational migration is expected. Never edit a released migration.
8. No `CALC_VERSION` bump is expected unless scientific output meaning changes. Performance-only or presentation-only fixes do not justify a bump.
9. Existing exact persisted-result hits must remain above expensive scientific/worker/refinement work.
10. Preserve the one application-owned persistent process pool and the accepted worker bounds from 050.14/050.23 unless a concrete defect requires a narrowly scoped correction.
11. Time/Capacity full-resolution scientific/export data remains independent from display-only overview/refinement LoD.
12. Every user-discovered issue added to this child must be written into the implementation record as a numbered stabilization item with reproduction, cause, target, verification and final disposition.
13. Browser automation is not required by default. User-observed installed-app behavior is valid manual evidence. Do not claim manual verification unless it was actually performed.

## 4. Current repository anchors

Re-open current branch code before editing. The following are the current known anchors at the 050.23 review-clean checkpoint.

### 4.1 Time/Capacity refinement UI

`frontend/src/features/analyses/editor/families/time-capacity/TimeCapacityPlotCard.tsx`

Current state/refs:

- `refinedResult`;
- `refinementTimerRef`;
- `refinementAbortRef`;
- `refinementGenerationRef`;
- `refinementViewportRef`;
- `currentResultRef`.

Current cancellation helper:

```ts
const cancelRefinement = useCallback(() => {
  refinementGenerationRef.current += 1;
  if (refinementTimerRef.current !== null) {
    window.clearTimeout(refinementTimerRef.current);
    refinementTimerRef.current = null;
  }
  refinementAbortRef.current?.abort();
  refinementAbortRef.current = null;
  refinementViewportRef.current = null;
  setRefinedResult(null);
}, []);
```

Current display selection:

```ts
const activeRefinedResult =
  refinedResult &&
  timeCapacityRefinementRequestIsCurrent(
    refinedResult,
    currentResult,
    String(refinementGenerationRef.current),
  )
    ? refinedResult
    : null;
const plotResult = activeRefinedResult ?? currentResult;
```

Current zoom path in `handlePlotRelayout(...)`:

- remembers Plotly zoom through `useZoomMemory`;
- derives viewport and cycle range;
- calls `cancelRefinement()` before scheduling the next request;
- waits 150 ms debounce;
- sends `/api/analyses/{analysis_id}/time-capacity/refine`;
- generation-gates the returned response before `setRefinedResult(response)`.

The present combination means a second zoom from a refined view increments the generation and clears the displayed refinement immediately, so the plot falls back to the coarse `currentResult` until the next refinement completes.

### 4.2 Refinement policy

`frontend/src/features/analyses/editor/families/time-capacity/timeCapacityRefinementPolicy.ts`

Current anchors:

- `timeCapacityOverviewExtent(...)`;
- `timeCapacityCycleRangeForViewport(...)`;
- `timeCapacityRefinementWorthwhile(...)`;
- `timeCapacityRefinementRequestIsCurrent(...)`;
- `timeCapacityRefinementEligible(...)`;
- `timeCapacityRefinementCanSchedule(...)`.

`timeCapacityRefinementRequestIsCurrent(...)` currently combines two separate concepts:

1. whether an **arriving request response** belongs to the current request generation;
2. whether an **already displayed refinement** is still compatible with the current overview result.

050.24 should separate those concepts rather than weakening stale-response protection.

### 4.3 Plot runtime

`frontend/src/features/analyses/editor/plotting/plotRuntime.ts`

Current ordinary interactive line traces use `scattergl` where WebGL is supported. Filled bands remain ordinary scatter/SVG where needed. Any refinement transition must respect this existing rendering strategy and must not introduce expensive per-frame array reconstruction.

### 4.4 Plot wrapper

`frontend/src/components/Plot.tsx`

This remains the shared Plotly wrapper. Do not add a global animation abstraction unless the Time/Capacity change proves it is genuinely reusable and low-risk. Prefer a local, bounded transition inside the Time/Capacity family.

### 4.5 Shared analysis workers

`backend/app/services/analysis_family_workers.py`

Current accepted production policy after 050.23:

- promoted families: Cycles, Steps, DCIR, Rate Capability;
- Chargeability serial;
- P4 threshold: 4 Cells for promoted families;
- one application-owned pool reused from `time_capacity_workers`;
- exact hits bypass worker work;
- no ORM/session/full DataFrame worker payloads;
- Cycles worker dispatch requires exact ready parser/calc cycle cache;
- P8 rejected.

These are cumulative-review anchors, not an invitation to rebenchmark them without a concrete reason.

## 5. Stabilization issue protocol

Maintain a numbered issue ledger in the implementation record, for example `I1`, `I2`, ... . Each item must contain:

- **Observed / reproduction** — what the user or cumulative review actually sees;
- **Scope** — affected feature, files and current code anchor;
- **Cause** — the verified technical cause, not a guess;
- **Target** — exact intended behavior;
- **Risk** — why it matters and whether it can affect correctness, provenance, result validity, or only UX/performance;
- **Implementation** — concise description of the chosen fix;
- **Verification** — focused automated tests, targeted measurement, and/or actual manual user evidence;
- **Disposition** — fixed, rejected, non-blocking observation, or moved out of scope.

Do not pre-populate speculative issues merely to make the child look comprehensive.

## 6. I1 — Time/Capacity LoD continuity during repeated zoom

### 6.1 Observed behavior

The user can start from a Time/Capacity view that has already received an adaptive refinement, then zoom further. The current UI briefly shows a much coarser polyline with visibly fewer points before the next detailed refinement arrives.

The visible sequence is:

```text
refined LoD A
    -> user zooms further
    -> coarse overview suddenly reappears
    -> debounce + network + backend refinement
    -> refined LoD B appears
```

This looks like the plot lost detail and then recovered, even though the old refinement was still a perfectly useful visual source while the next request was pending.

### 6.2 Verified cause

`cancelRefinement()` currently performs three jobs at once:

1. invalidates the pending request generation;
2. cancels timer/HTTP work;
3. discards the displayed refined result.

In addition, `activeRefinedResult` validates the already displayed result against the **current pending generation**. Therefore merely incrementing `refinementGenerationRef` makes the old displayed refinement ineligible even if `setRefinedResult(null)` were removed.

### 6.3 Target behavior

For a compatible zoom-in refinement transition:

```text
refined LoD A
    -> user zooms further
    -> existing LoD A remains visible under the new Plotly axis range
    -> next refinement is requested in background
    -> LoD B replaces A when ready
```

At no point should a compatible already-refined view fall back to the coarse overview solely because a newer refinement is pending.

### 6.4 Required state separation

Split the current cancellation/display concepts. Exact naming is implementation-owned, but the behavior should be equivalent to:

- `cancelPendingRefinement()`:
  - increments/changes request generation;
  - clears debounce timer;
  - aborts pending HTTP request;
  - clears pending-request viewport state as appropriate;
  - **does not discard a still-compatible displayed refinement**.

- `clearDisplayedRefinement()`:
  - removes the currently displayed transient refinement only when it is no longer semantically/display compatible.

The helper currently named `timeCapacityRefinementRequestIsCurrent(...)` should remain strict for **accepting a newly arriving response**. Do not relax stale-response generation gating. Instead, add or use a separate compatibility test for whether the already displayed refinement can remain visible.

### 6.5 When displayed refinement must be cleared

Clear the displayed refinement when the underlying overview or rendering semantics genuinely invalidate it, including as applicable:

- overview `data_signature` changes;
- selection/data-source identity changes;
- cycle range/configuration changes that alter returned data;
- voltage channel or Time/Capacity view changes;
- x-axis/time-unit/display-mode semantics change;
- refinement eligibility becomes false;
- the tab is deactivated if the existing lifecycle intentionally discards ephemeral refinement on inactive tabs;
- Plotly autorange/full overview is explicitly restored.

Do not retain an old refinement merely because it exists.

### 6.6 Viewport compatibility

The main required case is **zooming further into the already refined region**. If the user pans or jumps to a viewport that is not covered by the displayed refinement, it is acceptable to fall back to the stable overview while a new refinement is prepared.

If current response metadata is insufficient to determine whether a previous refinement covers the new viewport, add only the smallest ephemeral client/request metadata needed for that decision. Do not change scientific cache identity or persist viewport refinements.

### 6.7 Stale-response safety

Rapid A -> B -> C zooms must retain generation safety:

- B may remain displayed while C is pending if B covers C;
- a late A or B network response must never overwrite C after C becomes the active request generation;
- aborts/network errors leave the best currently valid displayed data visible;
- no stale refinement can become an export/full-resolution scientific source.

### 6.8 Acceptance criteria

Starting from an already-refined Time/Capacity view, zooming further into that refined region must **not** temporarily replace the detailed trace with the coarse overview while the next refinement request is pending. The previous compatible LoD remains visible until the replacement is ready, then the new refinement replaces it. Obsolete responses remain generation-gated and cannot overwrite the newer viewport.

Add focused tests covering at minimum:

- refined A -> zoom B -> A remains displayed while B is pending;
- rapid A -> B -> C where a late B response cannot replace C;
- autorange/full overview clears refinement;
- semantic/data-signature change clears refinement;
- incompatible pan/jump falls back safely if the prior refinement cannot cover the new viewport;
- refinement failure/abort leaves the best valid current display rather than flashing coarse unnecessarily.

## 7. I1 optional polish — short refinement transition

The user prefers a short visual effect in which the immediately zoomed existing LoD is shown first and the finer detail then appears smoothly when the new response arrives, **provided there is no meaningful performance penalty**.

This is optional polish, not a correctness requirement.

### 7.1 Preferred effect

When a compatible new refined trace arrives, use a very short transition of approximately **100–180 ms**; ~120–150 ms is a sensible starting point.

The semantic effect should be "resolution appears" rather than "scientific values move".

A practical approach is a bounded old/new trace opacity transition:

```text
t0        old 100% / new 0%
~75 ms    old 50%  / new 50%
~150 ms   old 0%   / new 100%
```

Do not implement expensive coordinate-by-coordinate interpolation between old and new arrays. The two LoDs may contain different point counts and do not have a stable one-to-one mapping.

### 7.2 Performance constraints

The transition must:

- not delay refinement request dispatch;
- not block zoom/pan interaction;
- not perform scientific transforms in animation frames;
- not rebuild thousands of coordinates per frame in React;
- be immediately superseded/cancelled by a new user interaction;
- respect `prefers-reduced-motion` with an atomic swap;
- remain bounded for `scattergl` traces;
- be dropped if targeted profiling shows meaningful main-thread/rendering regression or visible frame loss.

If the animation is not cheap and robust, retain the LoD continuity fix and use an atomic replacement. Do not hold 050.24 open merely to preserve the animation idea.

## 8. Cumulative Spec 050 review

After active stabilization issues are addressed, perform a fresh cumulative review of the entire branch against the correct merge base `1dc3525ec42571504ed6d9bdb9a0668d35df309b`.

The cumulative review must read actual current code and tests; do not merely concatenate earlier child reviews.

### 8.1 Scope reconciliation

Confirm:

- all implemented numeric children and the withdrawn 050.8 history are represented truthfully;
- no proto-child/planning-only experiment accidentally became production scope;
- no unrelated branch work was modified/reset/discarded;
- the final implementation matches the parent-level objective of improving normal Analysis responsiveness while preserving scientific behavior.

### 8.2 Scientific/result contract

Inspect cross-child interactions for:

- exact numerical semantics where performance paths were rewritten;
- NaN/null handling;
- selection and replicate-group semantics;
- protocol filters and protocol-derived families;
- continuation/source ordering;
- normalization by mass/area where relevant;
- deterministic Cell/series order;
- provenance and source/parser/calc identity;
- export/full-resolution behavior;
- persisted result-schema compatibility.

Do not reopen an already-accepted algorithm without a concrete current defect.

### 8.3 Cache/data ownership

Confirm:

- list endpoints remain relational and bounded;
- no request-path source reparsing or expensive cache rebuild was accidentally introduced;
- pending/write-behind cache ownership remains process-safe;
- selective raw/cycle reads fail closed to valid fallbacks;
- worker processes read only canonical cache artifacts and do not own SQLite/ORM state;
- exact persisted result hits still bypass scientific/worker work;
- display/refinement data never becomes a scientific cache/export authority.

### 8.4 Worker lifecycle and concurrency

Confirm final integrated ownership after 050.14 and 050.23:

- one application-level persistent process pool;
- Time/Capacity retains its accepted active-worker behavior;
- promoted non-Time families use only measured P4 thresholds;
- Chargeability remains serial;
- P8 remains unpromoted;
- pool-not-ready/broken infrastructure has safe serial fallback;
- scientific worker exceptions are not silently hidden;
- no ORM/session/full DataFrame crosses the process boundary.

Do not rerun the complete historical concurrency matrices unless code inspection finds something that can invalidate those decisions.

### 8.5 Frontend/runtime integration

Review only the frontend areas materially touched by Spec 050, especially:

- query-cache identity and placeholder behavior;
- Time/Capacity refinement lifecycle;
- Plotly trace preparation and zoom-memory interaction;
- installed performance controls;
- saved-plot/export readiness;
- abort/stale-response handling;
- loading/error/empty behavior and keep-mounted tab lifecycle.

### 8.6 Documentation and durable knowledge

Bring current repository documentation into alignment with the final accepted architecture. Do not document rejected experiments as production behavior.

At minimum verify/update as needed:

- `docs/agent-knowledge/state-and-performance.md`;
- Parent 050 status/child summary;
- active child/review status;
- any workflow/context document made materially stale by final architecture.

## 9. Verification strategy

Use the current canonical commands from `AGENTS.md` and topic-specific agent knowledge.

Expected final shape:

```powershell
# focused tests for actual 050.24 fixes
python -m unittest ...

# frontend focused tests/type/build if I1 or another frontend issue is touched
node --test ...
# and current canonical frontend commands as required by AGENTS.md/preflight

python scripts\preflight.py
```

Do not rerun every historical profiler simply for confidence. Run a targeted profiler only if the stabilization fix materially changes a measured path whose accepted performance conclusion could move.

For I1, a focused browser/user check is useful because the defect is visual. If the user validates the installed app manually, record exactly what was observed. If no manual check is run, state `NOT RUN`; automated state-policy tests can still close the code path if they fully prove continuity semantics.

## 10. Implementation order

1. Re-read current branch, `AGENTS.md`, `docs/agent-knowledge/README.md`, `docs/agent-knowledge/state-and-performance.md`, Parent 050, 050.15, 050.23 and their current reviews.
2. Create the 050.24 implementation-record issue ledger.
3. Implement I1 LoD continuity with strict stale-response generation safety.
4. Evaluate the optional short refinement transition only after the continuity behavior is correct; retain it only if cheap and robust.
5. Add any additional user-discovered/cumulative-review issue only when it meets the scope rule in this spec.
6. For each added issue, verify cause before changing code and record focused evidence.
7. Perform the fresh cumulative branch review against merge base `1dc3525ec42571504ed6d9bdb9a0668d35df309b`.
8. Fix only material cumulative defects. Record low-risk polish/test-shape observations as non-blocking rather than forcing another cycle.
9. Update durable documentation to the actually accepted final architecture.
10. Run focused checks and one final canonical `python scripts\preflight.py`.
11. Hand 050.24 to the reviewer with explicit merge-readiness evidence.

## 11. Acceptance criteria

050.24 is complete when:

1. the known Time/Capacity repeated-zoom LoD flash is fixed: a compatible current refinement remains visible until its replacement is ready;
2. stale/aborted refinement responses cannot overwrite the newest viewport;
3. semantic/data incompatibility still clears refinement correctly;
4. optional transition polish, if retained, is short, interruptible, reduced-motion safe and shows no meaningful rendering penalty;
5. every additional stabilization issue discovered during ordinary use or cumulative review is recorded with reproduction/cause/target/verification/disposition;
6. no unresolved issue remains that can materially affect scientific correctness, provenance/cache identity, deterministic semantics, data integrity, export correctness, persistent UI state, crashes/hangs, or a production performance decision;
7. low-risk micro-optimization/test-polish observations do not block closure without a concrete regression;
8. the cumulative branch review is performed against the correct merge base and covers actual integrated code, not only earlier review summaries;
9. cache, parser/calc version, worker ownership, result-hit and migration consequences are explicitly reconciled;
10. durable documentation reflects the final accepted architecture;
11. focused tests for actual fixes pass;
12. canonical `python scripts\preflight.py` passes;
13. the final reviewer states explicitly whether the feature branch is ready to merge.

## 12. Out of scope

Unless a concrete material regression makes one necessary for closure:

- new analysis families or plot features;
- broad Analysis-page redesign;
- progressive Time/Capacity streaming;
- new permanent worker pools;
- P8 production promotion;
- Rust/native production integration;
- speculative RAM caches or prefetch;
- new persistent scientific cache formats;
- broad Time/Capacity rebenchmarking;
- broad five-family rebenchmarking;
- unrelated UI polish;
- migration/schema redesign;
- scientific formula changes;
- refactoring merely to satisfy style preference;
- additional verification rounds for theoretical issues that do not establish a concrete correctness, user-regression or performance-validity risk.

## 13. Reviewer focus

The reviewer should prioritize material integrated risk. Inspect code first, then evidence. Block closure for concrete defects that can prejudice results, provenance/cache identity, deterministic semantics, user-visible correctness, stability, or the validity of an accepted performance architecture.

Do not create another 30-minute implementation cycle solely because a test could be more end-to-end, a profiler field could be cleaner, or a micro-optimization is theoretically possible when the current behavior is correct and verified sufficiently for its risk.

The final review must still be rigorous where it matters: scientific/data correctness and truthful performance claims remain non-negotiable.
