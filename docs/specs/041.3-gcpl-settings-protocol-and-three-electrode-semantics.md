# 041.3 — GCPL settings, protocol metadata and three-electrode semantics

**Status:** Plan  
**Parent:** [041-biologic-mpr-gcpl-support.md](041-biologic-mpr-gcpl-support.md)  
**Implementation baseline:** post-040 `main`; reverify every anchor before editing  
**Shared branch:** `feature/biologic-mpr-gcpl-support`  
**Depends on:** 041.1 and 041.2 implemented, pushed and review-clean  
**Review document:** `reviews/041.3-gcpl-settings-protocol-and-three-electrode-semantics-review.md`

## Goal

Complete the BioLogic GCPL adapter's source metadata and declared-protocol semantics, including the three-electrode/full-cell voltage roles needed by GCPL6.

At completion, a supported BioLogic GCPL `.mpr` parsed directly through backend services must provide:

- normalized source/header metadata;
- acquisition start and channel/device/software facts when present;
- reference-electrode metadata when explicitly stored;
- a truthful CellXplorer declared protocol reconstructed from GCPL sequence settings;
- canonical voltage-role capabilities;
- `working_potential_v`, `counter_potential_v`, and correct primary `voltage_v` for supported three-electrode files;
- operational cutoffs/limits sufficient for existing analyses when the source actually provides them;
- explicit capability warnings for unsupported/absent protocol semantics.

This child still does not expose `.mpr` in the user import workflow; that belongs to 041.4.

## Required reading

Read before editing:

- Parent 041;
- review-clean 041.1/041.2 plus `docs/biologic-mpr-format.md`;
- Parent 040 canonical contract/multi-voltage docs;
- `AGENTS.md`;
- `docs/specs/README.md`;
- `docs/agent-knowledge/canonical-cycling-data.md`;
- `docs/agent-knowledge/architecture.md`;
- `docs/agent-knowledge/state-and-performance.md`;
- `docs/agent-knowledge/change-playbooks.md`;
- `docs/agent-knowledge/dcir-analysis.md`;
- `docs/agent-knowledge/chargeability-analysis.md`;
- `docs/agent-knowledge/rate-capability-analysis.md`;
- current `backend/app/services/protocol.py` and its tests;
- current normalized metadata/header conventions landed by 039/040;
- current three-electrode Time/Capacity capability contract from 040.4;
- official BioLogic Application Note 58 / Technical Note 30 for GCPL6/full-cell-control semantics;
- paired `.mpr`/`.mpt` settings/header output when available.

Do not copy settings-offset tables from GPL parser source. Independently establish only the offsets/structures required by the supported GCPL corpus.

## Source metadata ownership

The full decoded source metadata belongs once in:

```text
SourceFile.header_meta
```

through the existing normalized header path.

Curated Cell-level metadata remains bounded. Do not expand every GCPL setting/sequence field into `CellMetadata` rows.

## Settings module decoding strategy

Extend the independent format notebook and parser with the minimal verified `VMP Set` structures needed for:

- technique/GCPL-family identity;
- sequence count/order;
- per-sequence control/current/C-rate setting;
- current direction/sign setting where needed;
- voltage limit/cutoff;
- optional CV/hold/current cutoff behavior;
- rest duration;
- loop/goto/cycle repeat behavior;
- record interval/settings needed for scientific validation;
- active material/battery capacity/electrode area/reference text when present in independently verified header fields.

### Variable layout

If GCPL settings parameter blocks can appear at different offsets/software versions, do not hard-code a guessed list of offsets copied from another project.

Instead:

1. collect at least the supplied file and paired/other privacy-approved GCPL files;
2. identify the parameter-block structure independently from known settings exported by EC-Lab;
3. document the supported layout/version discriminator in `docs/biologic-mpr-format.md`;
4. fail clearly for an unrecognized settings layout.

The first supported contract may cover only the verified modern layout(s). Broad historical EC-Lab support is not required.

## Log module decoding

Independently decode only verified fields needed by CellXplorer, such as:

```text
channel number / channel serial if present
acquisition start timestamp
original filename
host/instrument address if useful
ec-lab software version
firmware/server/interpreter version where available
device serial
```

Do not parse every unknown log byte.

If absolute start timestamp is absent or unreliable:

```text
absolute_timestamps = false
```

and leave canonical `timestamp` null/NaT rather than using file mtime.

## Normalized metadata result

Reuse Parent 040/039 normalized keys. At minimum expose when available:

```text
source_format = biologic_mpr
remarks/comments
channel
device_info
start_time
active_mass_mg
nominal_capacity_mah
electrode_area_cm2
reference_electrode
software/version identifiers
operational charge/discharge cutoff(s)
capabilities
voltage_roles
raw/full source metadata
```

Do not overload Neware-specific fields such as `nda_version` with BioLogic versions. If existing API has a generic parser/source-software field after 040, use it; otherwise add a bounded BioLogic-specific normalized field only where needed.

## Declared protocol target

Use CellXplorer's existing declared protocol schema. Do not add a separate BioLogic protocol API.

Parent 040/039 may still use a Neware-like internal protocol grammar before producing a normalized protocol response. Either:

- build that existing internal grammar truthfully from BioLogic GCPL sequence settings; or
- use a format-neutral canonical protocol builder if Parent 040 introduced one.

Follow the actual post-040 owner.

### Programmed sequence identity

Protocol step IDs must correspond deterministically to the `step_index` emitted by 041.2.

If 041.2 normalizes BioLogic `Ns` from 0-based to 1-based, protocol steps must use the same normalization.

### Step type mapping

Map independently verified GCPL sequence behavior to existing canonical protocol operation types, e.g.:

```text
CC charge/discharge
CV charge/discharge
CCCV charge/discharge
Rest
Cycle/loop control where represented
End
```

Do not map a voltage limit to CV merely because a limit exists; distinguish “CC until cutoff” from “CCCV with hold” using actual GCPL settings/control semantics.

### Limits/cutoffs

Map operational voltage/current/capacity/time limits only when explicit in the sequence settings.

Do not confuse:

```text
instrument protection/safety limits
```

with:

```text
programmed operational cutoff for a GCPL sequence
```

Current Rate Capability and other recognizers rely on this distinction.

### Loop/goto behavior

If GCPL sequence settings expose goto/repeat/cycle information, preserve it sufficiently for protocol structure and repeated step selection.

Do not invent Neware condition expressions/global-user variables.

## Protocol capability flags

Expose explicit capability facts so downstream recognition can degrade honestly.

At minimum distinguish:

```text
declared_protocol_available
explicit_rate_available
operational_cutoffs_available
loop_structure_available
semantic_conditions_available
```

The exact field names follow Parent 040's capability schema.

If BioLogic GCPL does not express the same semantic condition grammar used by current Chargeability recognition, set that capability false and let Chargeability return not-applicable/no-match rather than fabricating a Neware formula.

## Three-electrode / GCPL6 voltage semantics

Official BioLogic documentation for GCPL6 establishes:

```text
Ewe   = working electrode vs reference
Ece   = counter electrode vs reference
Ecell = working-to-counter full-cell voltage
```

### Canonical mapping

For verified GCPL6/full-cell-control data:

```text
working_potential_v = decoded Ewe
counter_potential_v = decoded Ece
voltage_v            = decoded Ecell, if explicitly stored and verified
```

If the supported MPR does not contain a separate Ecell column but contains synchronized Ewe and Ece under a common reference:

```text
voltage_v = working_potential_v - counter_potential_v
```

### Sign verification

The supplied sample/paired `.mpt` must prove the subtraction order. Test several rows, not only one, and compare to EC-Lab full-cell voltage when available.

Do not assume `abs(Ewe - Ece)`; sign is scientifically meaningful.

### Measured vs derived metadata

Record:

```text
voltage_v role = cell
voltage_v origin = measured | derived_working_minus_counter
working_potential role = working_vs_reference
counter_potential role = counter_vs_reference
```

### Reference electrode

If EC-Lab settings explicitly store reference electrode text, preserve it.

Do not infer `Li/Li+` merely because the cell is a Li-ion battery or the official application note uses lithium.

### Positive/negative labels

Backend canonical roles stay working/counter.

If source settings independently establish which side is battery positive/negative, metadata may expose those roles. Otherwise the frontend should say Working/Counter, not Positive/Negative.

## Two-electrode GCPL behavior

Do not require auxiliary potential columns for normal supported two-electrode GCPL.

For a two-electrode file:

```text
voltage_v = verified source primary cell/working voltage
working_potential_v/counter_potential_v absent unless genuinely measured
```

Capability metadata drives the UI.

## Timestamp completion

If 041.2 deferred absolute timestamps, finish here:

```text
timestamp = acquisition_start + total_time_s
```

Use one explicit timezone interpretation based on EC-Lab/log semantics and current application normalization.

Required tests:

- known first timestamp;
- known later timestamp;
- DST/timezone edge only if source/log format actually requires it;
- missing log leaves NaT with capability false.

## Header-only performance

`read_header_metadata(.mpr)` must avoid decoding the entire 5,483/500k-row data block when metadata/settings/log are sufficient.

Preferred path:

```text
map/walk modules
→ decode VMP Set + VMP LOG + VMP data column header only if needed
→ normalized metadata
```

Do not run full `np.frombuffer` record decode merely to show import metadata.

Record a focused timing comparison on the supplied/large synthetic file showing header-only metadata path is materially cheaper than full parse. Use evidence, not an arbitrary strict millisecond target.

## Protocol-aware analysis compatibility

### Rate Capability

If GCPL sequence settings expose explicit C-rate/current magnitude and operational cutoffs in a way current Rate Capability can consume truthfully, map them to the existing protocol representation and add a positive synthetic test in 041.6.

If not, set capability false; do not reverse-engineer rate from executed current/active mass just to force recognizer success unless current Rate Capability already supports such canonical input format-neutrally.

### Chargeability

Do not invent Neware formula/condition metadata. Expected initial BioLogic behavior may be not-applicable if the current matcher fundamentally requires semantics GCPL settings do not carry.

### DCIR

DCIR primarily needs correct canonical raw rest/pulse structure and protocol step identity. Map rest/current sequence metadata truthfully; positive end-to-end test belongs to 041.6.

## Parser revision

If 041.3 changes canonical raw voltage/timestamp/status output relative to 041.2, bump the MPR adapter revision before creating/reusing caches.

Metadata-only additions that do not alter canonical raw data may or may not require parser-identity bump depending on whether analysis/protocol provenance semantics change. Follow Parent 040's parser identity rule: bump whenever persisted parser-derived scientific/protocol meaning changes.

Do not leave pre-fix caches/provenance current after a semantic correction.

## Tests

Add focused tests for:

1. supported GCPL settings layout decodes known synthetic sequence values;
2. unsupported settings layout/version fails closed;
3. sequence numbering matches 041.2 `step_index`;
4. CC-until-cutoff versus CCCV-hold protocol types are distinguished correctly;
5. operational cutoff is not confused with safety/protection limit;
6. rest/loop/goto metadata maps truthfully;
7. normalized active mass/capacity/electrode area/reference metadata units are correct when present;
8. log acquisition timestamp reconstructs canonical timestamps;
9. missing log yields explicit timestamp capability false;
10. header-only metadata path does not full-decode record array;
11. three-electrode synthetic Ewe/Ece/Ecell mapping is exact;
12. derived `voltage_v = working - counter` matches known values;
13. subtraction sign is tested;
14. two-electrode GCPL remains valid without auxiliary potentials;
15. reference electrode is present only when source says so;
16. declared protocol result uses current canonical schema, not BioLogic-specific response;
17. absent unsupported semantic condition data produces explicit capability false;
18. existing Rate/Chargeability recognizers receive truthful protocol/no-match behavior in focused unit tests where practical;
19. parser revision advances if raw/protocol semantics changed from 041.2;
20. no GPL parser/table imports/copies appear in production code.

## Private sample acceptance

When the supplied GCPL6 file is locally available, record:

```text
header/module metadata read succeeds
full parse still = 5,483 rows
working potential present? yes/no + field evidence
counter potential present? yes/no + field evidence
full-cell voltage measured or derived
reference electrode text present? exact source value / absent
acquisition start reconstructed? yes/no
program sequence count and mapped protocol summary
```

When paired `.mpt` exists, compare Ewe/Ece/Ecell arrays/headers at representative/all rows within text-rounding tolerance.

Do not commit the private files.

## Explicitly out of scope

- user import exposure — 041.4;
- frontend UX — 041.5;
- EIS/other techniques;
- fabricated condition formulas;
- positive/negative labels without explicit evidence;
- DB migration/`CALC_VERSION` change.

## Implementation order

1. Extend independent format notebook with verified settings/log structures.
2. Implement minimal settings/log decoders and header-only path.
3. Build normalized metadata/capability result.
4. Build canonical declared protocol from GCPL sequences.
5. Finalize two-/three-electrode raw voltage mapping and timestamps.
6. Re-run canonical/calc compatibility if raw output changed; bump parser revision if required.
7. Add focused protocol/metadata/three-electrode tests and timing evidence.
8. Probe private/paired files locally if available.
9. Run preflight, update implementation record, commit/push/review.

## Verification

At minimum:

```text
041.1/041.2 tests
new settings/protocol/three-electrode tests
protocol/DCIR/rate/chargeability focused tests affected by mapping
canonical/cache tests if parser revision/raw output changes
header-only vs full-parse timing probe
paired/private real-file probe when available
python scripts\preflight.py
```

## Acceptance criteria

041.3 is review-clean only when:

- GCPL settings/log metadata are independently decoded for the verified contract;
- declared protocol uses CellXplorer's canonical schema;
- step IDs align exactly with 041.2 raw `step_index`;
- operational limits/loops are truthful and safety limits are not misrepresented;
- three-electrode Ewe/Ece/Ecell mapping is sign-correct and capability-driven;
- two-electrode files remain supported without fake auxiliary channels;
- reference/timestamp metadata are never invented;
- metadata/header read remains cheap and does not full-parse data;
- unsupported semantic conditions degrade explicitly;
- parser revision/provenance is advanced if semantic output changed;
- no GPL dependency/copied implementation, migration or `CALC_VERSION` bump occurs;
- preflight passes and real-file/timing evidence is recorded truthfully.

## Implementation record

_To be filled by the implementing agent. Include independently verified settings/log layout(s), protocol mapping, capability flags, voltage-role/derived-cell-voltage decision, reference/timestamp results, parser revision decision, timing evidence, real paired/sample checks, commands/results, implementation SHA and review link._
