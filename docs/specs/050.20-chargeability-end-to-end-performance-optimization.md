# Spec 050.20: Chargeability end-to-end performance optimization

Status: **Implementation ready — review pending**
Type: **backend runtime performance / Chargeability**  
Branch: `feature/runtime-performance-optimization`  
Parent: [`050-runtime-performance-optimization.md`](050-runtime-performance-optimization.md)  
Profiling source: [`050.17-analysis-family-performance-profiling-and-prioritization.md`](050.17-analysis-family-performance-profiling-and-prioritization.md)  
Baseline implementation head when this child was authored: `71fc95f8f26f73b239ed587021164dc449ee71f4`  
Merge base with `main`: `1dc3525ec42571504ed6d9bdb9a0668d35df309b`  
Depends on: **050.19 review-clean**

## 1. Goal

Reduce Chargeability warm result-cache-miss latency across the complete route. The first measured target is protocol reconstruction, but this child must re-profile after that work and continue through every secondary boundary where a meaningful end-to-end gain remains justified.

Preserve safe expression evaluation, protocol/candidate semantics, occurrence/reference-capacity science, provenance and exact-hit behavior.

Time/Capacity is out of scope.

## 2. Locked baseline

050.17 authoritative Chargeability measurements:

| Workload | p50 |
| --- | ---: |
| 1 Cell forced miss | **37.65 ms** |
| 6 Cells forced miss | **195.51 ms** |
| 6 Cells exact persisted hit | **17.89 ms** |
| dominant stage | `protocol_reconstruction` |
| dominant p50/share | **120.69 ms / 61.70%** |

This strongly suggests repeated protocol reconstruction is the first opportunity. Do not assume the remaining ~75 ms is irrelevant: once reconstruction is reduced, occurrence extraction, raw access, candidate logic, assembly or ORM work may become dominant.

## 3. Current production anchors

### Route/cache

`backend/app/routers/analyses.py:compute_chargeability_analysis(...)`

- `engine.build_analysis_request_context(...)`
- protocol/canonical guards
- `analysis_cache.result_key(...)`
- direct `load_result_body("chargeability", key)` exact hit
- `chargeability.compute(...)`
- result store/serialization.

### Service

`backend/app/services/chargeability.py`

- `parse_linear_expression(...)`
- `_assignment_steps(...)`
- `detect_candidates(...)`
- `_reference_capacity(...)`
- `_occurrence_rows(...)`
- `compute(...)`

### Shared protocol/data

- `backend/app/services/protocol.py:reconstruct_protocol(...)`
- `backend/app/services/analysis_engine.py:AnalysisRequestContext`
- `backend/app/services/cache.py:load_raw(...)`
- current cross-family profiler.

Chargeability intentionally retained full raw access in 050.16 because raw materialization was not the dominant measured cost. Do not introduce selective raw complexity unless the post-reconstruction profile changes that conclusion.

## 4. Locked scientific and safety contracts

Preserve:

- the restricted AST grammar and no-arbitrary-code behavior of `parse_linear_expression`;
- candidate recognition and assignment-step ownership;
- nominal-capacity dependence of protocol reconstruction;
- reference-capacity selection;
- occurrence grouping and cycle/source identity;
- current/capacity/voltage/time calculations;
- chargeability expression semantics;
- configured filters;
- deterministic ordering/IDs;
- provenance/parser versions;
- multi-source fail-closed behavior.

Do not memoize protocol results under an identity that omits any input affecting reconstruction.

## 5. Required opportunity inventory

Before implementation, profile Chargeability only on the current head.

Separate:

1. owner/context and scalar metadata;
2. cache-key/exact-cache work;
3. source/header access;
4. protocol reconstruction;
5. candidate detection;
6. raw materialization;
7. reference-capacity calculation;
8. occurrence-step filtering/grouping;
9. numeric conversion/derived quantity work;
10. expression evaluation;
11. cross-Cell aggregation/comparison if any;
12. result/provenance assembly;
13. cache persistence;
14. serialization;
15. complete route.

Record SQL count/time and source-header lazy loads. This matters because protocol reconstruction consumes `header_meta`, which is deliberately deferred on many other warm paths.

Use the common meaningful-work rule: inspect stages around >=5% of the six-Cell route or >=10 ms, or related repeated work combining above that level.

## 6. Optimization tranche A — exact protocol reuse within the request

First determine **why** protocol reconstruction is ~121 ms for six Cells.

Preferred low-risk direction is request-local reuse, not a speculative persistent cache.

Investigate whether the route currently reconstructs the same source protocol more than once across:

- guards;
- cache-key/provenance work;
- `chargeability.compute(...)`;
- candidate derivation;
- repeated selection units referring to the same Cell/source.

Where exact inputs are identical, build/reuse a request-local protocol reconstruction map keyed by all scientific inputs required for equivalence, at minimum source identity/parser/header state and nominal capacity as appropriate to the current `protocol.reconstruct_protocol()` contract.

Possible ownership locations:

- extend `AnalysisRequestContext` with lazily populated protocol facts;
- add a narrow request-scoped helper/cache object consumed by the guard and family service;
- another simpler existing pattern if current code already owns protocol reuse elsewhere.

### Hard constraints

- no process-global mutable protocol cache;
- no stale result after metadata/nominal-capacity/source change;
- no cache-key collision;
- no persistent schema just to memoize Python objects;
- do not eagerly reconstruct protocols on exact persisted result hits if the current hit can remain cheaper without it.

If the dominant 050.17 timing comes from unavoidable unique reconstructions rather than duplicate reconstruction, benchmark alternative reconstruction implementation only after proving that fact.

## 7. Optimization tranche B — protocol reconstruction itself

If unique reconstruction remains material after request-local reuse, profile inside `protocol.reconstruct_protocol()` for the Chargeability workload and optimize only measured expensive operations.

Candidates may include:

- repeated header normalization/parsing;
- repeated string/regex scans;
- repeated nominal-capacity rate derivation;
- rebuilding immutable structures that can be derived once per call.

Any shared protocol-service change affects DCIR/Rate/other consumers. It therefore requires focused protocol safety tests and must preserve output exactly for all supported parsers, not only the Chargeability fixture.

## 8. Optimization tranche C — occurrence/raw/science path

Re-profile after protocol work.

If now material, optimize:

- `_reference_capacity(...)`;
- `_occurrence_rows(...)` step filtering/grouping;
- repeated `pd.to_numeric` / timestamp conversion;
- per-occurrence scalar extraction;
- safe expression evaluation setup when the same compiled expression can be reused request-locally;
- full raw materialization, but only if it becomes a meaningful share;
- result/provenance assembly and serialization.

Prefer single-pass/reused arrays over new concurrency.

## 9. Expression handling

`parse_linear_expression(...)` is a security boundary.

If expression parsing/evaluation is optimized:

- cache only validated/compiled safe representations;
- key by the exact expression text/config;
- do not broaden the accepted AST;
- do not call `eval` on untrusted unrestricted input;
- preserve numerical output and error behavior.

Security correctness outranks latency here.

## 10. Concurrency gate

Chargeability's current six-Cell route is ~196 ms. Do not introduce process/thread complexity unless the optimized serial path still contains a sufficiently large independent per-Cell CPU cost and complete-route controls prove a win. Startup/IPC/RSS and small-workload regression must be counted.

## 11. Cache/version policy

Expected: no migration, no `CALC_VERSION` bump, no result-schema bump.

Exact persisted hits must remain direct and must not reconstruct protocol merely because the miss path now has a request-local protocol cache.

## 12. Implementation order

1. Capture current 1/6-Cell baseline + call counts for protocol reconstruction.
2. Prove duplicate vs unique reconstruction ownership.
3. Implement request-local exact reuse where duplicate work exists.
4. Re-profile complete route.
5. If unique reconstruction remains material, profile/optimize the shared reconstruction internals with cross-family safety coverage.
6. Re-profile and optimize occurrence/raw/expression/assembly secondary costs where meaningful.
7. Consider concurrency only under its gate.
8. Run final family profile, golden analysis, protocol safety and canonical preflight.
9. Record rejected candidates.

## 13. Performance acceptance

Because ~62% of the six-Cell route is currently protocol reconstruction, the child should achieve a material route reduction if duplicate/redundant work is present.

Minimum target: **25%** reduction of the six-Cell forced-miss p50 from 195.51 ms, nominally **146.6 ms or better**, unless profiling proves that the 120.69 ms consists of scientifically necessary unique reconstruction that cannot be reduced safely.

Do not stop merely on crossing the target if additional measured low-risk costs remain.

Report:

- 1/6-Cell miss p50/min/max;
- exact-hit p50;
- protocol reconstruction call count and p50 before/after;
- source-header lazy-load count;
- SQL count/time;
- raw rows;
- occurrence counts;
- response bytes;
- final stage distribution.

## 14. Required tests

Include:

- request-local protocol reuse returns exact protocol structures;
- distinct nominal capacities do not share incompatible reconstruction;
- changed source/header/parser identity cannot reuse stale results;
- exact persisted hit remains compute/protocol/raw bypass where current contract allows;
- safe linear expression grammar unchanged;
- candidate/reference/occurrence scientific parity;
- NaN/missing data paths;
- multi-source fail-closed behavior;
- golden Chargeability regression;
- shared protocol tests if protocol internals change.

Before handoff:

```powershell
python scripts\preflight.py
```

## 15. Out of scope

- Time/Capacity;
- relaxed expression security;
- persistent/global protocol caches without separate evidence;
- source-file reads on interactive requests;
- migrations;
- frontend redesign.

## 16. Handoff evidence

Provide measured protocol call ownership, retained/rejected optimizations, complete-route before/after, final stage distribution, scientific/security parity, exact-hit proof and tests actually run.

### Implementation and benchmark evidence

The required current-head baseline was captured at commit `96ac133` after
050.19 became review-clean:

```powershell
python scripts\profile_analysis_families.py --families chargeability --skip-time-specialist --repetitions 5 --output tmp\050.20-baseline.json
```

The six-Cell forced-miss baseline measured 188.46 ms p50 (180.25–196.54 ms)
with six protocol reconstructions consuming 119.24 ms p50, or 63.27% of the
route. The one-Cell baseline was 36.15 ms p50 (34.30–44.76 ms). The baseline
had six full raw loads, 122,946 rows materialized, 17 SQL statements, six
source-header lazy loads, and a 50,441-byte response for six Cells.

The first implementation tranche owns a request-local protocol reconstruction
list inside one `chargeability.compute()` call. A cached result requires exact
equality of the pinned parser identity, nominal capacity, and immutable header
metadata. The source hash is intentionally not an additional scientific input:
`protocol.reconstruct_protocol()` consumes only those three dimensions, so
distinct source files with identical header state can safely share the same
request-local result. A changed header, parser identity, or nominal capacity
cannot reuse it. No process-global or persistent protocol cache was added, and
exact persisted hits still bypass the service entirely.

After protocol reuse, the final route additionally reads only candidate and
reference steps through the existing indexed `cache.load_raw_step_rows()` path,
requesting the columns used by `_occurrence_rows()`. Missing, invalid, busy, or
incomplete sidecar metadata falls back to `cache.load_raw()` so older caches and
optional timestamp layouts retain the original scientific path. The final
five-repetition profile was:

```powershell
python scripts\profile_analysis_families.py --families chargeability --skip-time-specialist --repetitions 5 --output tmp\050.20-selective-raw.json
```

| forced-miss measurement | 1 Cell | 6 Cells |
| --- | ---: | ---: |
| complete route p50 (range) | **35.13 ms (34.30–47.59)** | **83.17 ms (82.34–92.36)** |
| exact persisted-hit p50 | **12.56 ms** | **12.61 ms** |
| protocol reconstruction p50 / calls | **19.53 ms / 1** | **19.88 ms / 1** |
| raw-load calls | **1** | **6** |
| rows materialized / physical rows read | **269 / 4,096** | **1,614 / 24,576** |

The six-Cell route is a 55.9% reduction from the measured current-head
baseline and is below the 146.6 ms acceptance target. The final six-Cell stage
distribution was: selection context 3.36 ms, protocol reconstruction 19.88 ms,
indexed data access 29.53 ms, candidate detection 3.10 ms, occurrence
extraction 13.67 ms, cache persistence 4.73 ms, JSON serialization 0.10 ms,
and 1.19 ms unattributed route residual. Candidate/reference output remained
one candidate and one match per Cell (six each); response bytes remained 50,441.
SQL remained 17 statements with 0.57 ms cumulative p50, and source-header lazy
loads remained six.

The indexed read adds sidecar validation work, but reduces materialized rows by
98.7% and physical Parquet rows by 80.0% for this workload while retaining the
route reduction after the protocol tranche. The remaining serial data-access
and occurrence stages were measured after the change; no low-risk concurrency
or shared-protocol-internal rewrite was justified. The serial route, safe
linear-expression grammar, candidate/reference calculations, NaN handling,
multi-source guard, provenance, exact-hit bypass, and result schema are
unchanged. No migration, cache-version, parser-identity, or CALC_VERSION change
was needed.

Focused Chargeability, protocol, profiler, and golden projection checks passed
(53 tests); Python compilation and `git diff --check` passed. Canonical
preflight and the explicit browser/manual `NOT RUN` status are recorded in the
handoff entry for this child.
