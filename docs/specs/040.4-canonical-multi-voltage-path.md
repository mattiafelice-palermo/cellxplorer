# 040.4 — Canonical multi-voltage path and Time/Capacity exposure

**Status:** Plan  
**Parent:** [040-canonical-cycler-data-architecture.md](040-canonical-cycler-data-architecture.md)  
**Implementation baseline / merge base:** `main` at `562c2edff1277fef71789244c95e3b17abc586fa`; reverify every anchor before editing  
**Shared branch:** `feature/spec-040-canonical-cycler-data-architecture`  
**Depends on:** 040.1–040.3 implemented, pushed and review-clean  
**Review document:** `reviews/040.4-canonical-multi-voltage-path-review.md`

## Goal

Extend the canonical raw-data/cache/API/Time-Capacity path so one source can carry synchronized electrode-potential channels in addition to the existing primary `voltage_v`, while preserving ordinary two-electrode behavior exactly.

This child establishes the reusable three-electrode-capable data path **without adding BioLogic `.mpr` support yet**.

At completion:

- the canonical raw table may include `working_potential_v` and `counter_potential_v`;
- source metadata/capabilities can describe which voltage roles are available and what `voltage_v` means;
- Parquet cache, selective raw loading, source stitching and Time/Capacity APIs preserve those optional channels;
- Time/Capacity users can select the primary voltage or available electrode potentials as the plotted/exported Y quantity;
- existing `voltage_v` remains the default everywhere;
- Cycles, DCIR, Chargeability and Rate Capability do **not** silently switch to auxiliary electrode channels;
- two-electrode Neware sources do not gain confusing empty UI controls.

## Required reading

Read before editing:

- Parent 040 and review-clean 040.1–040.3;
- `AGENTS.md`;
- `docs/specs/README.md`;
- `docs/agent-knowledge/canonical-cycling-data.md`;
- `docs/agent-knowledge/architecture.md`;
- `docs/agent-knowledge/state-and-performance.md`;
- `docs/agent-knowledge/change-playbooks.md`;
- `docs/agent-knowledge/visual-style-guide.md`;
- `docs/portable-analysis-html.md`;
- current `backend/app/services/parsing.py` and adapter facade;
- current `backend/app/services/cache.py`;
- current `backend/app/services/stitch.py`;
- current Time/Capacity backend functions in `analysis_engine.py`/routers;
- current `frontend/src/features/analyses/editor/families/time-capacity/` implementation;
- current shared plotting/export/saved-artifact/portable figure builders used by Time/Capacity;
- current `frontend/src/api.ts`;
- Time/Capacity backend/frontend tests and saved/portable plot tests.

## Locked canonical names

Use exactly these canonical raw column names:

```text
voltage_v
working_potential_v
counter_potential_v
```

`voltage_v` remains required for normal cycling-capable sources and remains the default voltage quantity.

Do not introduce vendor names such as `Ewe`/`Ece` into downstream canonical APIs.

## Voltage-role semantics

### Two-electrode source

```text
voltage_v = measured primary/cell voltage
working_potential_v = absent
counter_potential_v = absent
```

### Three-electrode full-cell source

```text
voltage_v = full-cell voltage between working and counter electrodes
working_potential_v = working electrode potential vs reference
counter_potential_v = counter electrode potential vs reference
```

When the adapter derives `voltage_v = working - counter`, that derivation belongs to the **source adapter** and must be recorded in metadata/capabilities. This child only preserves/exposes already-canonical channels.

### Half-cell / single-reference-potential future case

Parent 040 does not force a source with only one electrode potential versus reference to fabricate both auxiliary channels. The adapter may still set `voltage_v` to its primary measured potential with a metadata role such as `working_vs_reference` if that source is scientifically supported.

The UI must label based on capability/role metadata, not infer electrode meaning from the mere presence of a column.

## Metadata/capability contract

Extend/reuse the bounded source capability representation established by 039/040.1.

At minimum the normalized header result must be able to express:

```text
primary voltage available
working potential available
counter potential available
role of voltage_v
reference electrode text when explicitly provided by source
whether voltage_v was measured directly or derived from electrode potentials
```

Conceptual representation only:

```json
{
  "capabilities": {
    "primary_voltage": true,
    "working_potential": true,
    "counter_potential": true
  },
  "voltage_roles": {
    "voltage_v": "cell",
    "working_potential_v": "working_vs_reference",
    "counter_potential_v": "counter_vs_reference"
  },
  "reference_electrode": "Li/Li+"
}
```

Requirements:

- preserve full source header once in `SourceFile.header_meta`;
- keep user-facing metadata bounded;
- do not create one DB row per signal or record;
- do not invent reference-electrode text when absent.

## Synthetic source strategy for 040

There is no BioLogic parser in this parent, so test the multi-voltage path with deterministic synthetic canonical frames and/or a generated parser test double at the source-adapter boundary.

A representative three-electrode frame should satisfy:

```text
working_potential_v = known values
counter_potential_v = known values
voltage_v = working - counter
```

This proves the application path without importing GPL-derived code or committing private BioLogic data.

## Cache behavior

Flat raw Parquet naturally supports optional additional columns. Preserve that design.

Required checks:

- `cache.build`/write-behind preserves the columns returned by a canonical parser;
- `load_raw` returns them;
- `load_raw_columns` can request them selectively;
- cache identity does not change merely because an optional column is absent from a source; parser identity already captures adapter semantics;
- no separate voltage-cache file is introduced.

No `CALC_VERSION` bump is required because existing derived formulas continue using `voltage_v`.

## Stitch behavior

`stitch_raw` must preserve optional voltage columns across source concatenation.

Pandas concatenation may yield NaN where one source has an auxiliary channel and another does not. That is acceptable at the raw frame level, but the API/UI must treat availability carefully:

- a plotted series for a Cell should only be offered when the relevant source/selection has meaningful data under current Time/Capacity selection rules;
- do not draw long zero-filled traces for missing sources;
- source-boundary/provenance remains unchanged.

Do not alter `stitch_cycles`: cycle summaries remain based on primary `voltage_v` unless a future scientific feature explicitly defines electrode-specific cycle statistics.

## Time/Capacity backend API

### Quantity selection

Reverify the current Time/Capacity request/response contract. Extend the existing Y-quantity model rather than add a separate endpoint.

Preferred stable internal quantity IDs:

```text
voltage              -> voltage_v
working_potential    -> working_potential_v
counter_potential    -> counter_potential_v
```

Exact existing naming conventions should be followed.

### Availability

The backend should provide enough capability/quantity metadata for the frontend to know which voltage quantities are available for the selected analysis samples.

For multi-sample analyses, use a conservative rule consistent with current family semantics:

- if a requested channel is missing for a sample, do not fabricate it;
- either omit that sample's trace for that quantity with a clear existing empty/unavailable treatment, or mark the quantity unavailable for mixed selections if the current UI requires common quantities;
- choose one rule based on the actual current Time/Capacity architecture and lock it in the implementation record/tests.

Do not silently fall back from `working_potential` to `voltage`.

### Downsampling/export

Display downsampling remains presentation-only. CSV/XLSX/other scientific data export must use the full selected canonical channel values under the current export contract.

### Derivatives

Current derivative views such as `dQ/dV` or `dV/dQ` have scientific meaning tied to the selected voltage axis.

For this child:

- if the implementation already computes derivatives generically from the selected Y voltage, allow auxiliary potentials only when the existing formula naturally follows the selected channel and tests can prove it;
- otherwise keep derivatives restricted to primary `voltage_v` and explicitly disable/hide them for auxiliary electrode potentials.

Do **not** guess. Inspect the current backend/frontend ownership and choose the smallest scientifically safe behavior. Record the decision in the child implementation record and durable docs.

## Frontend Time/Capacity UI

Expose voltage channels in the existing quantity selector/settings control.

Recommended labels:

```text
Cell voltage (V)            # when voltage_v role = cell
Working potential vs ref (V)
Counter potential vs ref (V)
```

If the role of `voltage_v` is not `cell`, use the truthful role-specific label supplied by capability metadata rather than hard-coded “Cell voltage.”

For ordinary current Neware two-electrode sources, the selector should look/behave as before except for any unavoidable internal refactor. Do not show disabled WE/CE entries merely to advertise a feature unavailable for that source.

### Reference-electrode context

If the source explicitly provides a reference electrode, a compact label/tooltip may append it, e.g.:

```text
Working potential vs Li/Li+ (V)
```

Do not make the selector excessively wide. Follow compact Mantine geometry and truncation/tooltip patterns from the visual style guide.

## Saved plots and persistence

Time/Capacity saved plots must persist the selected voltage quantity using the same family settings mechanism as existing quantities.

Requirements:

- old saved plots lacking the new setting still resolve to current default `voltage_v` behavior;
- opening/updating/copying a saved plot retains the selected channel;
- thumbnails, hover previews and full artifacts render the selected channel;
- artifact/query keys include the quantity setting if they do not already;
- no hidden fallback causes an old artifact for primary voltage to be shown for a newly selected electrode potential.

If a new setting changes the saved plot spec shape, use current normalization/defaulting rather than a relational migration.

## Portable reports

Portable Time/Capacity figures must preserve the selected voltage channel and axis label through the existing serialized Plotly/frozen SVG path.

Do not redesign portable security or source embedding.

## Other analysis families

### Cycles

Remain primary-voltage only. Existing mean/first/last voltage cycle metrics continue to derive from `voltage_v`.

### DCIR

Remain primary-voltage only. Do not compute electrode-specific DCIR in Parent 040.

### Chargeability

Remain primary-voltage only.

### Rate Capability

Remain primary-voltage/cutoff behavior only.

### Steps

If the current generic Steps quantity selector automatically exposes arbitrary raw voltage columns, do not expand it accidentally. Parent 040's user-visible alternate-potential support is deliberately scoped to Time/Capacity. A future spec may add electrode-specific Steps views.

## API/type tests

Required backend/frontend tests include:

1. synthetic canonical raw cache preserves both auxiliary potentials exactly;
2. selective raw-column load returns them;
3. stitched raw frame preserves them and uses NaN rather than fabricated values for sources that lack them;
4. Time/Capacity primary voltage request remains unchanged;
5. Time/Capacity working-potential request returns correct values/labels;
6. counter-potential request likewise;
7. unavailable channel fails/returns the chosen truthful availability behavior without fallback;
8. current two-electrode fixture exposes only current quantities;
9. saved plot default normalization maps old specs to primary voltage;
10. saved plot with working/counter selection round-trips;
11. query/artifact keys distinguish voltage-channel selection;
12. CSV/data export contains full selected-channel values;
13. portable figure roundtrip preserves channel/label;
14. Cycles/DCIR/Chargeability/Rate Capability existing tests remain unchanged;
15. dark/light, loading/error/empty and keyboard behavior of the selector follow existing family patterns.

## Documentation

Update `docs/agent-knowledge/canonical-cycling-data.md` with:

- primary versus auxiliary voltage rules;
- working/counter terminology;
- derived `voltage_v` ownership in source adapters;
- default-analysis restrictions;
- Time/Capacity availability behavior chosen during implementation.

Update architecture docs only where the new capability is durable and cross-cutting.

## Explicitly out of scope

- BioLogic `.mpr` parsing;
- electrode-specific DCIR, cycle means, Chargeability or Rate Capability;
- EIS/half-cell impedance;
- arbitrary analog-signal UI;
- positive/negative electrode assignment without explicit metadata;
- generic signal-selection framework;
- DB migration or `CALC_VERSION` bump.

## Implementation order

1. Reverify Time/Capacity raw-column request and saved-plot settings ownership.
2. Extend canonical metadata/capability representation with voltage roles.
3. Add synthetic multi-voltage raw/cache/stitch tests.
4. Extend backend Time/Capacity quantity selection/response.
5. Update API types and compact frontend selector behavior.
6. Update saved plots/artifacts/portable path.
7. Audit other analysis families for accidental auxiliary-channel use.
8. Update docs, run focused tests and preflight.
9. Update implementation record, commit, push, review.

## Verification

At minimum run current equivalents of:

```text
canonical/caching/stitch tests
Time/Capacity backend tests
Time/Capacity frontend policy/figure tests
saved plot/artifact/portable tests affected by quantity selection
Cycles/DCIR/Chargeability/Rate Capability regression tests
TypeScript check
Vite build
python scripts\preflight.py
```

Manual browser matrix should be recorded as RUN or NOT RUN. If run, cover at least one two-electrode source and one synthetic/dev three-electrode-capable result in light/dark mode.

## Acceptance criteria

040.4 is review-clean only when:

- optional working/counter potentials survive the complete canonical raw/cache/stitch/API path;
- `voltage_v` remains the existing default everywhere;
- two-electrode Neware UI/scientific behavior remains unchanged;
- Time/Capacity can explicitly select available electrode potentials without fabricated fallback;
- saved plots/artifacts/exports/portable figures preserve the selected voltage channel;
- other analysis families remain primary-voltage only;
- reference/electrode labels are truthful and bounded;
- no DB migration or `CALC_VERSION` bump occurs;
- required tests/preflight pass and manual checks are truthfully recorded.

## Implementation record

**Status: implemented, pending review.**

### Files changed

Backend:

- `backend/app/services/canonical_cycling.py` — added `VOLTAGE_QUANTITIES`
  (`voltage`/`working_potential`/`counter_potential` → canonical column),
  `DEFAULT_VOLTAGE_QUANTITY`, `voltage_capabilities(...)` (pure, bounded
  capability representation) and `voltage_quantity_label(...)`.
- `backend/app/services/parsing.py` — `read_header_metadata` now includes a
  `voltage_capabilities` block (via `canonical_cycling.voltage_capabilities()`)
  for every recognized format, not just Excel's unrelated
  `Excel.Capabilities.*` block.
- `backend/app/services/analysis_engine.py` — `time_capacity_settings` gained
  `voltage_channel` (defaults to `"voltage"`, rejects unrecognized values
  back to the default); `compute_time_capacity` selects the canonical column
  for the requested channel instead of hardcoding `voltage_v`, computes
  per-selection data-driven `voltage_channels` availability, and returns it
  in the result. `_derivative_curve` is unchanged (still reads `voltage_v`
  directly).
- `backend/app/services/analysis_cache.py` — `RESULT_SCHEMA_VERSIONS["time_capacity"]`
  2 → 3 (payload gained `voltage_channels` and `settings.voltage_channel`).

Frontend:

- `frontend/src/api.ts` — `computation.time_capacity.voltage_channel` field;
  `TimeCapacityResult.voltage_channels` field.
- `frontend/src/features/analyses/editor/families/time-capacity/TimeCapacityPlotCard.tsx` —
  `voltage_channel` default, label helpers (`voltageChannelLabel`,
  `voltageChannelShortLabel`), a "Voltage quantity" selector shown only
  inside the voltage/current plot mode and only when the current result
  reports an available electrode potential, channel-aware Y-axis title/style
  placeholder/header quantity name, `voltage_channel` added to the
  live-query data signature (so switching channel refetches).
- `frontend/src/features/analyses/editor/AnalysisEditor.tsx` — lifts
  `voltage_channels` from the plot card up to the settings sidebar (new
  `onVoltageChannelsChange`/`timeCapacityVoltageChannels` state, mirroring
  the existing `onReadyChange` pattern), channel-aware `plotSubtitle`.

Tests: `tests/test_canonical_cycling.py`, `tests/test_stitch.py`,
`tests/test_analysis_engine.py`, `tests/test_analysis_cache.py`,
`tests/test_parser_dispatch.py`. Golden fixtures refreshed (see below).

Docs: `docs/agent-knowledge/canonical-cycling-data.md`.

### Final capability/`voltage_channels` metadata shape

Source-level (static, format-neutral; `parsing.read_header_metadata(...)["voltage_capabilities"]`,
via `canonical_cycling.voltage_capabilities()`):

```json
{
  "capabilities": {"primary_voltage": true, "working_potential": false, "counter_potential": false},
  "voltage_roles": {"voltage_v": "cell"},
  "reference_electrode": null,
  "voltage_v_derived": false
}
```

Every current format (binary and Excel Neware) returns exactly this default,
because neither adapter populates the auxiliary columns. A future adapter
passes `working_potential_available=True`, `counter_potential_available=True`,
`reference_electrode="Li/Li+"`, etc. to get a genuinely different shape;
`voltage_roles` only ever names channels that are actually available.

Selection-level (data-driven; `TimeCapacityResult.voltage_channels`, from
`analysis_engine.compute_time_capacity`):

```json
{
  "voltage": {"available": true, "label": "Cell voltage (V)", "role": "cell"},
  "working_potential": {"available": false, "label": "Working potential vs ref (V)", "role": "working_vs_reference"},
  "counter_potential": {"available": false, "label": "Counter potential vs ref (V)", "role": "counter_vs_reference"}
}
```

`available` is computed by checking, across every selected cell's stitched
raw frame (before cycle-range filtering, so the offered options do not
flicker as filters change), whether the corresponding canonical column has
any finite value anywhere. This is the shape the frontend selector actually
reads to decide what to offer — never the static per-format declaration —
because it must reflect the samples actually selected in this analysis.

### Mixed-sample availability rule (evidence-based, locked by tests)

**Decision:** when some selected cells have the requested channel and others
do not, cells without it get an **omitted (all-`None`) trace**; the quantity
itself stays available at the selection level if *any* selected cell has it.

**Evidence for this choice over "mark the whole quantity unavailable":** the
existing Time/Capacity architecture already treats a missing column this way
for every other case — e.g. before this child, a cell whose raw cache lacked
`voltage_v` entirely already produced `np.full(len(raw), np.nan)` for that
cell's voltage array, and the frontend already omits a plotted segment via
`hasFinitePoint(segment.voltage)`
(`frontend/.../TimeCapacityPlotCard.tsx`). There was no existing "disable the
whole selection" behavior anywhere in this family to extend; per-cell
omission is the established pattern, so the multi-voltage path reuses it
rather than inventing a new, inconsistent rule.

**Test that locks it:**
`tests/test_analysis_engine.py::MultiVoltageTimeCapacityTests
::test_mixed_selection_omits_per_cell_rather_than_disabling_whole_quantity` —
selects one three-electrode and one two-electrode synthetic cell, requests
`working_potential`, and asserts the three-electrode cell's trace has finite
values, the two-electrode cell's trace is all-`None`, and
`voltage_channels["working_potential"]["available"]` is still `true`.

### Derivative decision (evidence-based, locked by tests)

**Decision:** derivatives (dQ/dV, dV/dQ) stay restricted to primary
`voltage_v` only; `voltage_channel` has no effect on them.

**Evidence:** `analysis_engine._derivative_curve` was inspected directly —
it reads `frame["voltage_v"]` unconditionally
(`if mode not in {"dqdv", "dvdq"} or "voltage_v" not in frame.columns: ...`,
`voltage = frame["voltage_v"].to_numpy(...)`), never the selected Y channel.
It was never routed through a selectable quantity even before this child, so
"the implementation already computes derivatives generically from the
selected Y channel" (the spec's condition for allowing aux channels into
derivatives) is false; the smallest scientifically safe behavior is to leave
it untouched. The frontend enforces this at the UI level too: the voltage
quantity selector only renders inside the `view === "voltage_current"` block
of `TimeCapacitySettings`, which is exactly the same scoping the existing X
axis/display-mode controls already use — switching to dQ/dV or dV/dQ simply
does not show a channel control, so there is no way to select an electrode
potential while a derivative view is active.

**Test that locks it:**
`tests/test_analysis_engine.py::MultiVoltageTimeCapacityTests
::test_derivative_view_stays_restricted_to_primary_voltage` — computes the
same `view: "dvdq"` spec against a three-electrode cell once with
`voltage_channel: "voltage"` and once with `voltage_channel:
"working_potential"`, and asserts the two `derivative_y` arrays are
byte-identical.

### Saved plots / artifact keys

Old saved plots have no `voltage_channel` key in their persisted
`computation.time_capacity`. `time_capacity_settings()` defaults a missing
or unrecognized value back to `"voltage"`
(`tests/test_analysis_engine.py::TimeCapacitySettingsVoltageChannelTests`),
and the frontend's `timeCapacityConfig()`/`DEFAULT_TIME_CAPACITY` merge does
the same client-side — this is the same additive-field pattern every other
Time/Capacity setting already uses; no spec-version bump or migration was
needed.

Artifact/query keys already distinguish the selection with no extra
plumbing: `analysis_cache._scientific_spec` hashes the entire
`spec["computation"]` dict (including `time_capacity.voltage_channel`) into
`result_key`'s digest, and the frontend's `savedPlotPreviewSignature`/
`plotViewSignature` do the same with `previewSpec.computation`. Locked by
`tests/test_analysis_cache.py
::test_time_capacity_voltage_channel_changes_scientific_cache_spec`
(`voltage`, `working_potential`, `counter_potential` and "key absent" all
produce distinct `_scientific_spec` digests) and
`::test_time_capacity_result_schema_version_only_invalidates_time_capacity_results`
(the `RESULT_SCHEMA_VERSIONS` bump invalidates only `time_capacity` result
caches, not `cycles`/etc.).

### Other families audited

Cycles/DCIR/Chargeability/Rate Capability compute services
(`calc.py`, `dcir.py`, `chargeability.py`, `rate_capability.py`) all read
`voltage_v` by a hardcoded literal column name, never a caller-selected
quantity — grepped directly, confirmed unchanged, and their existing test
suites pass unmodified. Steps' quantity selector
(`step_blocks.BLOCK_COLUMNS`, `frontend/.../StepsPlotCard.tsx`'s
`QUANTITIES` array) is a static, explicitly enumerated list on both
backend and frontend — not a generic raw-column enumeration — so it could
not have picked up the new canonical columns even accidentally; verified by
inspection rather than assumed.

### Golden fixture refresh — reported before applying

`tests.test_golden_analysis` initially failed with a strict key-set mismatch
on `time_capacity_baseline`/`time_capacity_derivative`
(`compute_time_capacity`'s new `voltage_channels` field and
`settings.voltage_channel` are additive response fields the committed
fixtures predate). Per this branch's hard rule, the refresh was generated and
inspected *before* being applied:

```
python scripts/build_golden_analysis_corpus.py refresh-expected --output tmp/golden-0404-refresh --diff-report tmp/golden-0404-refresh/diff-report.json --replace
```

```
DIFF time_capacity_baseline.json    — changed paths: 2
  $.voltage_channels: added (expected=None -> actual={...})
  $.settings.voltage_channel: added (expected=None -> actual='voltage')
DIFF time_capacity_derivative.json  — changed paths: 2 (same two keys)
SAME cycles_baseline.json, cycles_normalization.json, steps_baseline.json,
     dcir_baseline.json, chargeability_baseline.json, rate_capability_baseline.json
```

A full `diff` of both refreshed JSON files against the committed originals
confirmed exactly and only those two additive keys changed — no numeric
value anywhere in either file moved. This is expected: this child's entire
deliverable is adding those two fields. The refreshed files were then copied
into `tests/fixtures/golden_analysis/expected/`. **The reviewer should
independently re-derive or at minimum re-diff this** rather than trust the
description here, consistent with 040.3's verification method.

### Verification

All commands run from the repo root unless noted; exact results below.

```
python -m unittest tests.test_canonical_cycling tests.test_stitch tests.test_analysis_cache tests.test_parser_dispatch
  -> Ran 106 tests (39+21+27+19 across the four modules) ... OK

python -m unittest tests.test_analysis_engine
  -> Ran 57 tests ... OK

python -m unittest tests.test_dcir tests.test_chargeability tests.test_rate_capability tests.test_golden_analysis tests.test_golden_approval_checkpoints
  -> Ran 51 tests ... OK (golden corpus: all 8 cases match, including the
     refreshed time_capacity_baseline/time_capacity_derivative)

python -m unittest discover tests
  -> Ran 971 tests in 155.6s ... OK (full backend suite)

cd frontend && npx tsc -b --force
  -> exit 0, no diagnostics (run explicitly, not relying on preflight's
     unchanged-since-last-run skip, since this child changes frontend/src)

cd frontend && npm run build
  -> vite build succeeded (pre-existing >5000kB single-chunk warning,
     unrelated to this change)

python scripts/preflight.py --no-cache
  -> PREFLIGHT PASSED, 5/5 stages: Version consistency, Backend tests
     (16 workers, all 63 modules), Frontend policy tests (495/495, the
     `frontend/tests/*.test.ts` suite — this project has no `frontend/src`
     colocated component test framework), Frontend type check, Frontend
     production bundle. --no-cache forces every stage to actually run
     rather than self-skip, confirmed by grepping the log for "SKIP" (no
     matches).
```

No frontend component-level tests exist for `TimeCapacityPlotCard.tsx`
specifically (this codebase's frontend test suite lives in `frontend/tests/*.test.ts`
as policy/pure-function tests, not component tests, and none currently cover
this file) — the new UI logic (selector visibility, channel-aware labels) is
therefore verified only by TypeScript compilation, the backend contract
tests above, and manual inspection, not an automated frontend unit test.
This is a pre-existing gap in the project's test infrastructure, not
something this child could close without introducing a new test framework,
which is out of scope.

### Manual browser checks

**NOT RUN.** No running dev instance with real or synthetic three-electrode
data was set up in this session; the two-electrode regression path was
verified through the automated test suite and TypeScript/build checks only,
not through interactive browser inspection. The reviewer should run at
least one two-electrode source (confirm the selector is absent, plot/export
unchanged) and, if practical, a synthetic three-electrode cache injected
directly (bypassing the absent BioLogic parser) in light and dark mode.

### Ambiguities / decisions made

1. The spec's "Metadata/capability contract" section describes a source-level
   capability shape but leaves its exact home unspecified beyond
   `SourceFile.header_meta`. Given no adapter varies it, this child exposes
   it as a transient field on `read_header_metadata`'s return value
   (`voltage_capabilities`) rather than persisting it into
   `SourceFile.header_meta` (which already stores the full flattened raw
   header verbatim, satisfying "preserve full source header once"). Nothing
   currently reads a persisted capability field, so persisting a value that
   is always the same default for every source today would be inert
   plumbing; a future adapter's child can add persistence together with the
   first real non-default value.
2. `RESULT_SCHEMA_VERSIONS["time_capacity"]` was bumped 2 → 3 even though the
   change is purely additive and could not, by itself, cause a stale-payload
   correctness bug (a missing `voltage_channels` field degrades to "no
   selector shown", not wrong data). Bumped anyway for consistency with the
   040.3 precedent (accepted there as "conservative and correct") and so the
   reviewer never has to reason about whether a specific stale cache entry
   happens to be safe.

### Implementation SHA

_Recorded in a follow-up commit after this one, per the project's established
pattern (see `4a86773` for 040.3's equivalent)._
