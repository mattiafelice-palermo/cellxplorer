# 040.1 — Canonical cycling data contract and validation

**Status:** Plan  
**Parent:** [040-canonical-cycler-data-architecture.md](040-canonical-cycler-data-architecture.md)  
**Implementation baseline / merge base:** `main` at `562c2edff1277fef71789244c95e3b17abc586fa`; reverify every anchor before editing  
**Shared branch:** `feature/spec-040-canonical-cycler-data-architecture`  
**Depends on:** Parent 039 complete/review-clean/merged; Parent 040 only  
**Review document:** `reviews/040.1-canonical-cycling-data-contract-and-validation-review.md`

## Goal

Turn CellXplorer's existing implicit Neware-shaped raw-data expectations into an explicit, testable **CellXplorer canonical cycling contract** without changing existing scientific results.

At the end of this child:

- the repository contains one durable document describing the canonical row model and field meanings;
- production code has a small validation/normalization boundary that can assert whether a parser output is structurally safe for downstream cycling calculations;
- existing Neware binary and post-039 Neware Excel parser outputs satisfy the contract;
- no user-visible import/analysis behavior changes;
- no parser dispatch/provenance redesign from later children is pre-implemented.

This child answers one question first:

> What exact data structure must every future cycler adapter produce so that downstream CellXplorer code can remain format-neutral?

## Required reading

Read before editing:

- Parent 040 in full;
- Parent 039 and its final review/implementation records;
- `AGENTS.md`;
- `docs/specs/README.md`;
- `docs/agent-knowledge/README.md`;
- `docs/agent-knowledge/architecture.md`;
- `docs/agent-knowledge/state-and-performance.md`;
- `docs/agent-knowledge/change-playbooks.md`;
- `docs/agent-knowledge/scientific-regression-testing.md`;
- `docs/parser-capacity-findings.md`;
- `docs/neware-excel-variant-findings.md`;
- current `backend/app/services/parsing.py`;
- current Neware Excel parser module landed by 039 and its dialect follow-up;
- current `backend/app/services/calc.py`;
- current `backend/app/services/step_blocks.py`;
- current `backend/app/services/stitch.py`;
- current `backend/app/services/protocol.py`;
- current parser/calc/cache tests.

## Current behavior to reverify

At the locked merge base, `backend/app/services/parsing.py` maps binary Neware output through `RAW_COLUMNS` and dispatches structured `.xlsx` to `backend/app/services/neware_excel.py`. The Excel parser is now revision `6` and emits the same core meanings plus `total_time_s` and `power_w` where available. Its normalized status aliases include `Rest`, `CC_Chg`, `CC_DChg`, `CV_Chg`, `CCCV_Chg`, and `CCCV_DChg`; reverify this list against the current module rather than trusting it, because the post-039 dialect work extended workbook variant handling.

That dialect work also introduced a unitless "record clock" variant. Its step-summary duration is reconciled against the record's step-relative elapsed column instead of the timestamp span, which can include a paused/restored gap. This does **not** redefine `time_s`, which remains step-relative in both dialects, but the documentation this child writes must describe both variants truthfully. Read `docs/neware-excel-variant-findings.md` alongside the parser module.

Important current downstream assumptions include:

- `calc.per_cycle` requires `cycle` and uses canonical capacity/energy fields when present;
- current phase classification recognizes Neware-like charge/discharge status strings;
- per-cycle charge/discharge totals are derived from per-executed-step counter deltas because Neware counters may reset at step boundaries;
- step duration calculation assumes `time_s` is step-relative;
- Steps analysis distinguishes programmed `step_index` from executed `step`;
- stitching expects cycle labels to be numeric/integer-like and preserves source order;
- raw Parquet caches are flat `pandas.DataFrame` tables.

Do not change those meanings in this child.

## Target production owner

Create one small backend contract owner. Preferred path:

```text
backend/app/services/canonical_cycling.py
```

A different similarly narrow name is acceptable only if current branch ownership establishes a clearer owner.

The module should own only canonical raw-data definitions/validation, not parsing, caches, protocol recognition or scientific calculations.

Suggested public surface:

```python
CANONICAL_RAW_VERSION = 1

REQUIRED_CYCLING_COLUMNS = (...)
STANDARD_OPTIONAL_COLUMNS = (...)

class CanonicalCyclingError(ValueError):
    pass


def validate_raw_timeseries(
    df: pd.DataFrame,
    *,
    require_cycling: bool = True,
) -> None:
    """Fail clearly when canonical raw structure is unsafe for downstream use."""
```

Exact helper names are not locked. Keep the implementation small and dependency-light.

## Canonical field contract

Document and test the parent meanings exactly.

### `record_index`

Requirements:

- numeric/integer-like after normalization;
- no missing values for normal supported cycling sources;
- unique within one source;
- stable acquisition order;
- validation may sort-check but must **not silently reorder parser output** unless the parser explicitly owns that normalization.

### `cycle`

Requirements:

- numeric/integer-like;
- finite when present;
- logical source-local cycle label;
- gaps are allowed because `stitch.observed_local_cycles` already maps observed labels densely across source boundaries;
- validation must not invent missing cycles.

### `step_index`

Meaning:

```text
programmed protocol step identity
```

Repeated loop executions reuse the programmed step number.

### `step`

Meaning:

```text
unique executed step occurrence within the source
```

Requirements:

- numeric/integer-like when present;
- rows from one concrete execution block share the same `step`;
- a repeated execution of the same `step_index` uses a different `step`;
- do not derive `step` in the canonical validator; source adapters own execution-boundary reconstruction.

### `status`

Meaning:

```text
CellXplorer semantic operation/status
```

The canonical layer adopts existing Neware-like meanings rather than vendor-native arbitrary labels.

At minimum document the semantics already used by production calculations, such as:

```text
Rest
CC_Chg
CC_DChg
CV_Chg
CV_DChg
CCCV_Chg
CCCV_DChg
```

Only include values verified to exist/mean something in the current code. If the current implementation has additional established values, document them rather than silently dropping them.

Unknown/unsupported vendor operations may be preserved as a clearly non-recognized canonical status or fail format-specific capability validation; do not teach downstream scientific code arbitrary vendor strings in this child.

### `time_s`

Locked meaning:

```text
seconds elapsed since current executed step began
```

Requirements:

- numeric;
- finite where available;
- may reset when `step` changes;
- must not be redefined as whole-source elapsed time;
- validation should allow small source rounding effects but reject structurally impossible values such as clearly negative elapsed time when the parser claims normal cycling capability.

### `total_time_s`

Standard optional meaning:

```text
seconds elapsed since source acquisition began
```

When present it should be numeric and non-decreasing within normal acquisition order, allowing only tiny floating-point tolerance. It is auxiliary; current scientific code must continue to use `time_s` where it expects step-relative time.

### `voltage_v`

Locked meaning:

```text
primary voltage for ordinary existing CellXplorer analyses
```

040.1 does not add three-electrode fields yet. It only records their reserved names in the standard optional contract so later children do not invent incompatible names.

Reserved:

```text
working_potential_v
counter_potential_v
```

### `current_ma`

Document the current sign convention by measuring/asserting the existing Neware parser behavior in focused tests. Do **not** assume the sign from memory.

The implementation record must state the observed convention in plain language, e.g. whether canonical positive current corresponds to charge or discharge.

Once observed, add that convention to `docs/agent-knowledge/canonical-cycling-data.md` and lock it for future adapters.

### Capacity and energy

Canonical fields:

```text
charge_capacity_mah
discharge_capacity_mah
charge_energy_mwh
discharge_energy_mwh
```

The contract deliberately allows counters that reset at executed-step boundaries because current `calc.per_cycle` handles them by summing per-step deltas.

Validation must therefore check data type/finiteness/obvious negative-invalid states but must not require monotonicity across a whole cycle.

### `timestamp`

When present:

- normalize to pandas datetime-compatible values;
- absolute timestamps may be unavailable for some future formats;
- absence or NaT is a capability issue, not a reason to fabricate a date;
- do not infer a calendar date from file modified time.

## Canonical capability vocabulary

Define/document a small bounded vocabulary for facts later adapters can expose. Do **not** build the full metadata implementation yet.

At minimum reserve these meanings:

```text
cycling_rows
absolute_timestamps
declared_protocol
primary_voltage
working_potential
counter_potential
```

If Parent 039 already established a capability representation, extend/reuse it rather than creating a competing schema.

## Validation boundary

The validator is a scientific safety check, not a data-cleaning engine.

It may:

- assert required column presence;
- coerce/check obvious numeric compatibility only where parsers already promise canonical values;
- reject non-finite/duplicate identifiers that make downstream calculations ambiguous;
- verify `step`/`step_index` structural consistency at a basic level;
- verify `total_time_s` monotonicity when present;
- verify reserved optional voltage columns have the same row count/index alignment by virtue of being DataFrame columns.

It must not:

- reconstruct cycles;
- reconstruct executed steps;
- infer charge/discharge from current;
- integrate capacity/energy;
- patch missing timestamps;
- rename vendor columns;
- change scientific values;
- perform file I/O.

Those are adapter responsibilities.

## Where validation runs

Add validation at the **full parse / cache-build boundary**, not on list/import enumeration.

Preferred behavior after this child:

```text
parser produces canonical DataFrame
→ validate_raw_timeseries(...)
→ cache/calculation code consumes it
```

If both Neware binary and Excel parsing already funnel through `parsing.parse_timeseries`, validate there or immediately inside cache build, whichever avoids duplicate validation while preserving direct-parser tests.

Do not make `read_header_metadata` perform a full parse.

## Documentation

Create:

```text
docs/agent-knowledge/canonical-cycling-data.md
```

It must explain in plain language:

1. why the model is intentionally Neware-like;
2. programmed `step_index` versus executed `step`;
3. `time_s` versus `total_time_s`;
4. current sign convention verified from current data/tests;
5. primary voltage and reserved auxiliary electrode potentials;
6. capacity/energy reset semantics;
7. adapter versus downstream scientific ownership;
8. how a future source format should map into the contract.

Link it from `docs/agent-knowledge/README.md` and add the important location to `AGENTS.md` if appropriate.

## Tests

Add focused tests to a new or existing canonical contract test module, e.g.:

```text
tests/test_canonical_cycling.py
```

Required cases:

1. minimal valid canonical cycling frame passes;
2. duplicate `record_index` fails;
3. non-integer/ambiguous `cycle` fails;
4. repeated `step_index` across distinct executed `step` values is accepted;
5. one executed `step` inconsistently maps to multiple programmed `step_index` values fails if that state is scientifically impossible under the current model;
6. `time_s` reset at an executed-step boundary is accepted;
7. decreasing `total_time_s` fails beyond tolerance;
8. missing optional energy/timestamp/auxiliary voltage columns remains valid when capability rules allow it;
9. malformed/non-numeric capacity/current/voltage values fail clearly;
10. current real/synthetic Neware binary parser output satisfies validation;
11. Parent 039 generated Excel parser output satisfies validation;
12. existing `calc.per_cycle` numerical output before/after validator insertion is identical for representative fixtures.

Use existing privacy-approved fixtures or generated sources. Do not commit new private source files.

## Explicitly out of scope

- parser registry/dispatch redesign — 040.2;
- cache/provenance key changes — 040.3;
- adding actual multi-electrode columns to Neware/BioLogic parsing — 040.4/041;
- UI changes;
- changing analysis formulas;
- `CALC_VERSION` bump;
- database migration;
- generic parser plugin API.

## Implementation order

1. Reverify raw columns and current sign/status/time semantics against `main@562c2edf…` plus any already review-clean 040 changes.
2. Write `canonical-cycling-data.md` with observed current facts.
3. Add small production contract constants/error/validator.
4. Route current full parser output through validation at one non-duplicated boundary.
5. Add focused synthetic tests.
6. Run current Neware binary + Excel parser/calc tests and golden projections relevant to raw semantics.
7. Update child implementation record, commit, push, hand off to reviewer.

## Verification

Use the current repository commands after 039 merges. At minimum:

```text
focused canonical-cycling tests
current binary parser/calc/cache tests
Parent 039 Excel parser/cache tests
existing stitch/step-block tests affected by validation
python scripts\preflight.py
```

Do not claim browser/packaging tests; this child has no UI/runtime dependency change.

## Acceptance criteria

040.1 is review-clean only when:

- one explicit canonical raw contract exists in code and durable docs;
- current Neware binary and Excel outputs satisfy it;
- current sign convention is verified from actual current implementation/tests and documented;
- `step_index`/`step` and `time_s`/`total_time_s` meanings are explicit and tested;
- validator failure messages identify the broken canonical property;
- no list/import enumeration performs a full parse because of validation;
- no existing scientific numerical result changes;
- no parser dispatch/provenance/multi-voltage feature from later children is pre-implemented;
- no migration or `CALC_VERSION` bump occurs;
- required focused verification and preflight are recorded truthfully.

## Implementation record

**Branch:** `feature/spec-040-canonical-cycler-data-architecture` (continued; no new branch created).
**Locked merge base:** `562c2edff1277fef71789244c95e3b17abc586fa` (reverified before editing — see anchors below).

### Files created

- `backend/app/services/canonical_cycling.py` — the contract owner named by the spec's
  "Target production owner" section (no deviation): `CANONICAL_RAW_VERSION`,
  `REQUIRED_CYCLING_COLUMNS`, `STANDARD_OPTIONAL_COLUMNS`, `KNOWN_STATUS_VALUES`,
  `CANONICAL_CAPABILITIES`, `CanonicalCyclingError`, `validate_raw_timeseries`. Owns only the
  contract/validator; no parsing, caching, protocol recognition, or scientific calculation.
- `docs/agent-knowledge/canonical-cycling-data.md` — the durable narrative contract, covering all 8
  numbered points the spec's Documentation section requires, plus the verified status vocabulary and
  reserved capability vocabulary.
- `tests/test_canonical_cycling.py` — all 12 required cases plus explicit boundary-wiring and
  no-mutation/no-numerical-change tests (30 tests total).

### Files modified

- `backend/app/services/cache.py` — `build()` and `build_write_behind()` each call
  `canonical_cycling.validate_raw_timeseries(raw)` immediately after `parsing.parse_timeseries(...)`
  and before `calc.per_cycle(...)`. `build()` only validates when `parsed_from_source` is true (a
  frame already on disk was validated when first written). This is the single, non-duplicated
  full-parse / cache-build boundary; see "Validation boundary placement" below for why it moved here
  instead of `parsing.parse_timeseries`.
- `docs/agent-knowledge/README.md` — links the new doc from the topic index.
- `AGENTS.md` — adds `canonical_cycling.py` to "Important locations" and the maintained repository
  tree (`backend/app/services/` and `tests/`).
- This spec file — implementation record filled in.

### Naming/placement decisions the spec left open

- Module name/path: used the spec's own preferred path verbatim
  (`backend/app/services/canonical_cycling.py`); no alternative was better justified.
- `REQUIRED_CYCLING_COLUMNS` does **not** include `timestamp`, even though Parent 040's "Required
  core columns" table lists it. Binary parsing (`parsing.parse_timeseries`) only adds a `timestamp`
  column when `NewareNDA.read(...)`'s own output happens to include one
  (`if "timestamp" in df.columns:`), so nothing in current code actually guarantees the column
  exists for every supported binary source. Making it validator-required risked rejecting an
  already-supported source with no scientific basis for the new failure, which the spec's own
  acceptance criteria forbid ("no existing scientific numerical result changes" /
  "no user-visible import/analysis behavior changes"). `timestamp` is validated as a standard
  *optional* column instead (`STANDARD_OPTIONAL_COLUMNS`), matching the 040.1 child text's own
  "When present:" phrasing for this field. Documented as a resolved ambiguity in
  `docs/agent-knowledge/canonical-cycling-data.md` point 1 (constants section) and below.
- `step_index` has no explicit "Requirements" list in the spec text (unlike `record_index`, `cycle`,
  `step`), but is still given the same numeric/integer-like/finite check as `step` for consistency
  with how every current parser already treats it (`int64` in both `parsing.RAW_COLUMNS` and
  `neware_excel._INT_COLUMNS`). This does not reject any currently-supported output.
- `CANONICAL_CAPABILITIES` is a plain tuple of reserved names with docstring meanings, not wired into
  `neware_excel.py`'s existing `Excel.Capabilities.*` schema or `parsing.read_header_metadata`'s
  output — per the spec, 040.1 only reserves the vocabulary; it does not build the metadata
  plumbing (that is 040.4, and it should extend the existing schema rather than compete with it).

### Observed canonical current-sign convention (measured, not assumed)

**Positive `current_ma` means charge; negative means discharge.**

Evidence:

1. Real production parse of `tests/fixtures/golden_analysis/sources/cycles_time_steps.ndax` through
   `parsing.parse_timeseries` (binary `NewareNDA.read` path), grouped by `status`:
   `CCCV_Chg` mean `+19.25 mA` (range `+2.49..+146.21`), `CC_Chg` mean `+3.68 mA`
   (range `+2.50..+75.90`), `CC_DChg` mean `-15.92 mA` (range `-75.90..-2.50`), `Rest` `0.0`.
2. Same check repeated across all four golden binary corpus sources
   (`chargeability_source.ndax`, `cycles_time_steps.ndax`, `dcir_source.ndax`,
   `rate_capability_source.ndax`) via `parsing.parse_timeseries` — same sign pattern.
3. A synthetic structured-Excel workbook (Spec 039 shape) parsed through
   `neware_excel.parse_timeseries` independently confirms the same convention:
   `CC_Chg` → `+1.0 mA`, `CC_DChg` → `-1.0 mA`.

This matches (but was not derived from) the sign convention already implicit in
`tests/test_calc_and_cache.py`'s existing CV-detection fixtures. Locked and documented in
`docs/agent-knowledge/canonical-cycling-data.md` point 4; `validate_raw_timeseries` deliberately
does not enforce this relationship (that would be inferring charge/discharge from current, which the
validation boundary forbids) — it is documented and tested, not validated structurally.

### Actual status vocabulary found in current code

`Rest`, `CC_Chg`, `CC_DChg`, `CV_Chg`, `CCCV_Chg`, `CCCV_DChg` — verified against
`neware_excel._STATUS_ALIASES` and the observed normalized `status` values across all four golden
binary corpus sources. **`CV_DChg` does not currently exist** in `neware_excel._STATUS_ALIASES` or
in any observed binary output, despite appearing in Parent 040's illustrative table. Documented as
`canonical_cycling.KNOWN_STATUS_VALUES` (documentation-only; the validator never rejects an
unrecognized status string) and in `docs/agent-knowledge/canonical-cycling-data.md`.

### Validation boundary placement

Validation runs in `cache.py`'s `build()` and `build_write_behind()`, immediately after
`parsing.parse_timeseries(source_path)` and before `calc.per_cycle(...)`. It was **not** placed
inside `parsing.parse_timeseries` itself (the initial attempt) because
`tests/test_neware_excel.py::NewareExcelParserTests::test_parser_dispatch_preserves_binary_and_excel_boundaries`
calls `parsing.parse_timeseries` directly with deliberately minimal/mocked frames to test dispatch
mechanics only, and failed once validation ran there. `cache.build`/`cache.build_write_behind` are
the only production callers of `parsing.parse_timeseries` (verified by repo-wide grep), so moving
validation to those two call sites is still a single, non-duplicated full-parse boundary while
preserving that direct-parser test, matching the spec's explicit guidance ("validate there or
immediately inside cache build, whichever avoids duplicate validation while preserving
direct-parser tests").

Verified `read_header_metadata`/list/import paths do not trigger a full parse or validation:

- `backend/app/services/parsing.py::read_header_metadata` never calls `parse_timeseries`; it
  dispatches to `neware_excel.read_metadata` / `NewareNDA.read_metadata` / the direct NDAX XML
  reader only.
- `backend/app/services/scanner.py` calls `parsing.read_header_metadata` for metadata reads and
  `cache.build(...)` only in its explicit parse/cache-rebuild paths (`parse_file`, stable-source
  replacement) — never on list/enumeration paths.
- `backend/app/services/import_inspection.py` calls `parsing.read_header_metadata` only; it never
  calls `parse_timeseries` or `cache.build`.
- Repo-wide grep confirms every call to `parsing.parse_timeseries` and `cache.build`/
  `cache.build_write_behind` originates from `cache.py` itself, `routers/files.py` (import cache
  worker / preview build-write-behind), `scanner.py` (explicit parse/rebuild paths), and
  `portable_analysis.py` (explicit cache-build-then-load path) — none of which are list/import
  enumeration endpoints.

### Commands run and results

- `python -m unittest tests.test_canonical_cycling -v` — 30 tests, OK.
- `python -m unittest tests.test_parsing tests.test_calc_and_cache tests.test_neware_excel tests.test_stitch tests.test_step_blocks tests.test_canonical_cycling` — 163 tests, OK.
- `python -m unittest tests.test_golden_analysis` — 30 tests, OK (candidate-vs-committed digest diffs
  in the log are 0 changed paths each, i.e. no scientific drift).
- `python -m unittest tests.test_golden_approval_checkpoints tests.test_analysis_engine tests.test_continuations tests.test_multi_source_regression tests.test_protocol_analysis_safety` — 79 tests, OK.
- `python -m unittest tests.test_import_flow tests.test_scientific_preparation tests.test_source_monitor tests.test_import_inspection tests.test_source_and_replicates tests.test_analysis_cache tests.test_analysis_staleness tests.test_analysis_lifecycle tests.test_cache_maintenance tests.test_diagnostic_cycles tests.test_library_edit tests.test_background_jobs tests.test_portable_analysis` — 284 tests, OK.
- `python -m unittest discover tests` — 881 tests, OK (full backend suite).
- `python scripts\preflight.py` — PASSED, 5/5 stages (version consistency, backend tests via 16
  workers, frontend policy tests, frontend type check, frontend production bundle).

Empirically measured the current-sign convention and status vocabulary against real fixtures before
writing the contract (see above), rather than trusting the spec's illustrative text.

### Not run

- No browser/manual UI verification (this child has no UI/runtime dependency change, per the spec).
- No packaged/installer build (not requested and not applicable to this child).
- `python scripts\check_versions.py` was not run standalone (preflight's version-consistency stage
  covers it and passed); no version bump was made because this child has no user-facing behavior
  change.

### Ambiguous/contradictory spec points and resolutions

1. **`timestamp` required-core vs. standard-optional** — resolved as standard-optional in the
   validator; see "Naming/placement decisions" above.
2. **`step_index` requirements text is empty** — resolved by applying the same light numeric check as
   `step`, for consistency with existing parser behavior, without inventing a new requirement beyond
   what real data already satisfies.
3. **Validation boundary: `parsing.parse_timeseries` vs. `cache.build`/`build_write_behind`** — the
   spec explicitly allowed either; empirical testing against the existing suite (not guesswork)
   determined `cache.py` was the only option that avoided breaking an existing direct-parser test.
4. No other contradiction or impossibility was found; the parent's locked decisions (no `CALC_VERSION`
   bump, no migration, no dispatch/provenance/multi-voltage pre-implementation) were all
   straightforwardly followed since this child never touches `calc.py`'s formulas, `models.py`, or
   `parsing.py`'s dispatch surface beyond leaving it unchanged.

**Implementation SHA:** see the commit immediately following this record on
`feature/spec-040-canonical-cycler-data-architecture` (reported in the handoff message to the
reviewer).
**Review document:** `docs/specs/reviews/040.1-canonical-cycling-data-contract-and-validation-review.md` (not yet created).
